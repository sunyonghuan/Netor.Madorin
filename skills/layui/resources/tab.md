# 选项卡 Tab

## 说明

旧版 `element` 模块中的 tab 组件，属于兼容性方案。

## API

- `element.render('tab', filter)`
- `element.tabAdd(filter, options)`
- `element.tabDelete(filter, layid, force)`
- `element.tabChange(filter, layid, force)`
- `element.tab(options)`

## 事件

- `element.on('tab(filter)', callback)`
