param(
    [string]$serverId,
    [string]$authType = 'auto'
)

throw "Legacy script disabled. Use the dedicated SSH plugin tools instead: ssh_connect, ssh_session_exec/ssh_run_once, and ssh_disconnect."
