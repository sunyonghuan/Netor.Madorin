param(
    [string]$serverId,
    [string]$remotePath,
    [string]$localPath
)

throw "Legacy script disabled. Use SSH plugin SFTP tools: ssh_sftp_stat, ssh_sftp_download, and ssh_sftp_transfer_poll."
