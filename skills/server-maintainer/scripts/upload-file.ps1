param(
    [string]$serverId,
    [string]$localPath,
    [string]$remotePath
)

throw "Legacy script disabled. Use SSH plugin SFTP tools: ssh_sftp_stat/ssh_sftp_mkdir, ssh_sftp_upload, and ssh_sftp_transfer_poll."
