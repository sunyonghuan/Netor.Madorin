# 阶段 8：验收发布与质量门禁 : 0%

> 阶段状态：待执行 · 总进度：0%
>
> 前置阶段：[07-CLI-ClientSDK与参考宿主](../备档/执行步骤/07-CLI-ClientSDK与参考宿主.md)

## 0. 进度跟踪

| 类别 | 已完成 | 总数 | 进度 |
| --- | ---: | ---: | ---: |
| 执行任务 | 0 | 88 | 0% |
| 测试要求 | 0 | 9 | 0% |
| 完成标准 | 0 | 14 | 0% |

标记说明：[×] 未完成；[√] 已完成。每完成一个任务，同时更新所属章节、阶段总进度和本表计数。

## 1. 目标

执行 V1 全量验收、性能与稳定性测试、Native AOT 多平台发布、安全检查、协议兼容报告、安装升级回退验证，并形成可发布制品。此阶段不得用“已知问题”豁免数据错配、重复副作用、权限绕过或无法终止任务。

## 2. 前置门禁

- 阶段 1-7 的完成标准均有可追溯验收记录。
- CLI、Client SDK、三模式、Provider、工具和恢复没有占位实现。
- `madorin` 独立启动、首次配置、Agent 管理和基本专家对话没有占位实现。
- 两个 `memory.md`、CLI 记忆命令、`builtin.memory` 和 Agent 自动注入没有占位实现。
- 所有数据库迁移、协议版本和包版本已进入发布冻结窗口。
- 已知问题清单中没有未分级条目。

## 3. 涉及范围

本阶段覆盖 `Madorin.AI.Runtime.slnx` 全部源码、测试、SampleHost、发布配置和使用/运维文档。任何项目不得以“内部项目”为由跳过分析器、AOT 或依赖边界检查。

## 4. 执行步骤 : 0%

### 4.1 建立发布候选基线 : 0%

- [×] 固化 Runtime 程序版本、协议版本、数据库 Schema 版本和 Provider Adapter 版本。
- [×] 记录 .NET SDK、RID、MAF、MEAI、Provider SDK 和 `Netor.Anthropic` 精确版本。
- [×] 生成依赖清单、许可证清单、漏洞扫描结果和第三方通知。
- [×] 锁定测试配置、随机种子、性能机器规格和外部 Provider 冒烟测试窗口。
- [×] 发布候选期间只接受阻断修复；任何协议/Schema 变化重新开始兼容验收。

### 4.2 执行协议与序列化验收 : 0%

- [×] 运行全部协议 JSON 快照并与上一个兼容版本比较。
- [×] 验证旧 Client 连接新 Runtime、新 Client 连接旧 Runtime 的协商行为。
- [×] 验证未知可选字段/事件、能力缺失和协议无交集。
- [×] 验证路径、URL、中文、引号、换行、首尾空白、大整数、十进制、时间和空值无损往返。
- [×] 验证大内容自动转 Blob，长度与 SHA-256 完全一致。
- [×] 确认 Release 配置禁用反射 JSON 默认路径。

### 4.3 执行正常业务矩阵 : 0%

- [×] 专家模式连续多轮、工具、Selection 更新、单轮 Override 和跨 Provider。
- [×] 会议模式多参与者、Selector、主持、HITL、动态成员、轮次摘要和恢复。
- [×] 工作模式计划、依赖、子智能体、后台步骤、工具委派和最终汇总。
- [×] 同一 Session 默认串行，不同 Session 并行；多 Run 事件按 GSN 有序分流。
- [×] 当前流期间更新 Provider/模型/Agent/Prompt，当前 Invocation 不变，下一轮同 Session 生效。
- [×] 三 Adapter 完成统一文本、reasoning、工具、用量、完成原因和错误场景。
- [×] 无宿主、无 IPC 时，`madorin` 使用用户目录配置完成基本专家多轮对话；宿主路径继续完成三模式全矩阵。
- [×] 同一系统用户下同时运行两个不同工作区的独立 CLI，验证运行数据、Session、日志、取消和退出互不串用。
- [×] 同一规范化工作区使用不同实例 ID 或 `--data-dir` 启动第二个写实例仍被拒绝，且不会破坏首个实例的数据和锁租约。
- [×] 一个宿主同时启动至少三个 Runtime，两个宿主进程再分别启动至少两个 Runtime，并与独立 CLI 同时运行；所有实例绑定不同工作区且持续并行收发。
- [×] 在多个 Runtime 中故意使用相同 `runId`、相同数值 GSN 和交错 Delta，验证事件、确认、查询、取消和终态按 `runtimeInstanceId` 隔离。
- [×] 分别终止任一宿主、Client 句柄或 Runtime 子进程，验证其他宿主和 Runtime 的认证、事件循环、运行数据及进程生命周期不受影响。
- [×] Expert、Meeting、Work 的每个 Agent 启动时都按全局后项目顺序获得记忆，项目规则更具体。
- [×] 真实 Provider 可由 LLM 直接调用 `builtin.memory.read/append`，工具结果返回后继续同一 Invocation。

### 4.4 执行取消、超时和竞争矩阵 : 0%

- [×] 在 Preparing、Running、WaitingForTool、WaitingForApproval、WaitingForCredentials、Persisting 分别取消。
- [×] Provider、工具、审批、凭据和总 Run 超时分别验证。
- [×] 完成与取消、超时与响应、断线与确认并发竞争重复执行。
- [×] 每个 Run 始终只有一个终态，取消单个 Run 不影响其他 Run。
- [×] Ctrl+C、Client Dispose、宿主进程退出和 Runtime 优雅关闭行为一致。

### 4.5 执行断线、崩溃和重放矩阵 : 0%

- [×] 分别断开控制通道、事件通道和两者，并从最后确认 GSN 重连。
- [×] 验证重复事件在 Host 侧不重复展示/落库，`delta.dropped` 可通过 `run.query` 补全。
- [×] 在 Outbox、JSONL、SQLite 索引、工具意图、会议轮次、工作步骤的每个提交边界强制终止 Runtime。
- [×] 强制终止 Host，验证租约内重连和租约到期策略。
- [×] Runtime 重启后无虚假 Running；所有 Run 为终态或 Interrupted。
- [×] 已执行工具响应丢失时查询并复用原结果，不重复副作用。

### 4.6 执行持久化与恢复矩阵 : 0%

- [×] 三模式按 sessionId 恢复历史、Selection、AgentSnapshot、附件、检查点和模式专属状态。
- [×] Prompt 未存储时 rehydrate 握手完整；哈希不匹配拒绝执行。
- [×] JSONL 末行损坏自愈，中间损坏停止并报告；索引可从消息文件重建。
- [×] Blob 缺失/篡改、Schema 过新、迁移中断、数据库损坏产生明确诊断。
- [×] 跨日期长会话保持单一 sessionId；列表/过滤/分页不扫描日期目录。
- [×] 宿主直接修改 `.madorin` 不属于支持路径，Runtime 能检测关键不一致。
- [×] 记忆正文不写入 CanonicalHistory、SQLite 或 Checkpoint；恢复后的下一 Invocation 重新读取当前两个文件。

### 4.7 执行工具与安全矩阵 : 0%

- [×] 同一 callId 在重试、重连和重启后最多执行一次。
- [×] 子智能体默认不继承权限；显式委派不能扩大工具、路径、网络、程序、有效期和风险。
- [×] 路径逃逸覆盖 `..`、符号链接、联接点、UNC、设备路径、ADS 和竞态替换。
- [×] Agent 无法读取、覆盖、移动或删除 `.madorin`。
- [×] Restricted 不允许任意进程/PowerShell；UnrestrictedShell 明确报告非沙箱。
- [×] Process 取消回收进程树；HTTP 防止重定向/DNS 绕过网络策略。
- [×] 错误认证、Pipe 抢占、PID 不符、HMAC 重放和非当前用户连接均失败。
- [×] 日志和诊断包扫描不包含密钥、token、Authorization、完整 Prompt 或敏感工具结果。
- [×] `config show`、配置错误、Agent 错误和首次向导不回显完整 Key；`config.json` 只允许当前用户读取。
- [×] 普通文件工具不能访问 `memory.md`；`builtin.memory.append` 未经用户审批不能写入，也不存在自动记忆提取路径。
- [×] 两个记忆工具在当前进程执行，不被错误路由到宿主/MCP，也不允许模型获得任意 `.madorin` 文件访问能力。

### 4.8 执行性能与稳定性基线 : 0%

- [×] 测量首事件/首 Token 延迟、IPC 附加延迟、总耗时、取消确认延迟和重连时间。
- [×] 测量不同 Run/Session 并发、Provider 限流、事件洪峰和 Blob 传输吞吐。
- [×] 运行长时间稳定性测试，观察 Run Registry、事件缓冲、Session、DI Scope、HttpClient 和文件句柄增长。
- [×] 在大量 Session/消息上验证列表和消息游标分页的稳定延迟及索引命中。
- [×] 测量 WAL checkpoint、JSONL append、Outbox、摘要和恢复耗时。
- [×] 固化内存 32 MiB/磁盘 512 MiB 事件缓冲等默认值，或依据实测形成变更决策。
- [×] 正常负载下取消确认目标不超过 1 秒；未达标必须有阻断分析或正式阈值决策。
- [×] 明确各指标的通过阈值：首事件延迟 ≤ 500ms（空载）；取消确认 ≤ 1s；重连时间 ≤ 3s；10 并发 Run 下 P99 事件延迟 ≤ 200ms。如实测结果与此不符，须在验收前以评审决议形式更新阈值。

### 4.9 执行构建与发布矩阵 : 0%

- [×] Debug/Release 全解决方案构建为 0 警告、0 错误。
- [×] 运行全部单元、协议、Provider、持久化、模式和端到端测试。
- [×] 发布并验证 `win-x64` 完整功能。
- [×] 发布并验证 `osx-x64`、`osx-arm64`、`linux-x64`、`linux-arm64` 的启动、传输、安全、基本 Run 和退出。
- [×] 每个 RID 的 Native AOT 发布必须 0 条 trimming/AOT 警告。
- [×] 对发布产物做冷启动、无 SDK 环境运行、目录只读/无权限和缺配置测试。
- [×] CLI 命令和可执行文件保持 `madorin`/`madorin.exe`，程序集/包保持 `Madorin.AI.Runtime.*`。
- [×] 从干净用户目录验证首次向导、已有配置启动、多个 Provider/模型、多个 Agent 和损坏配置修复。
- [×] Windows 验证双击 `madorin.exe`；所有平台验证安装后新终端可从 PATH 直接运行 `madorin`。
- [×] 从干净用户目录和工作区分别验证全局/项目 `memory.md` 的创建、编辑、自动注入、大小限制和文件权限。

### 4.10 验证安装、升级和回退 : 0%

- [×] 在独立目录安装，不依赖仓库或当前 Madorin 内部程序集，并正确配置当前用户 PATH。
- [×] 升级采用替换式策略：停止旧实例（排空 Run 后退出），原地替换可执行文件，再启动新实例；不支持同一工作区两个版本同时运行。
- [×] 旧实例排空已有 Run，新实例接收新 Run；不覆盖运行中二进制。
- [×] 数据库迁移前自动备份并支持预检；失败后旧版本能恢复到兼容数据。
- [×] 新版本健康检查失败时 Client/Host 可回退上一可用版本。
- [×] 卸载不默认删除用户 `~/.madorin` 或工作区 `.madorin`；配置和数据清理由明确命令执行。

### 4.11 生成发布交付物 : 0%

- [×] 生成各 RID 发布包、SHA-256 校验值、版本清单和构建来源。
- [×] 生成协议兼容报告、数据库迁移说明、已知限制和安全说明。
- [×] 生成安装、升级、回退、诊断、备份恢复和 Provider 配置文档。
- [×] 生成 Client SDK/Provider Adapter SDK 包及 API 文档。
- [×] 参考宿主、协议一致性工具、最小 `config.json`、Agent 示例和两个 `memory.md` 示例随发布说明提供。

### 4.12 召开发布门禁评审 : 0%

- [×] 逐项审阅本文完成标准和原实施方案 V1 发布门槛。
- [×] 阻断问题必须关闭并有回归测试，不能接受延期豁免。
- [×] 高风险问题需要负责人、影响范围、缓解和明确版本计划。
- [×] 记录发布/不发布决定、签署人、制品哈希和回退版本。
- [×] 发布后建立协议、Provider、错误率、资源和恢复失败监控指标。

## 5. 测试要求 : 0%

- [×] 全部测试必须基于发布候选制品或同一提交构建，不得混用本地旧二进制。
- [×] 离线自动化套件必须可重复运行；真实 Provider 冒烟测试单独记录账户、模型、区域和时间。
- [×] 故障注入记录注入点、预期状态、实际状态、恢复耗时和数据校验结果。
- [×] 性能测试至少预热、重复采样并报告 P50/P95/P99、最大值、样本量和机器规格。
- [×] 多平台验收使用同一协议向量和 SampleHost 场景，平台差异必须形成明确兼容说明。
- [×] 独立 CLI 验收使用隔离的临时用户目录，不能读取开发机已有 `.madorin` 配置。
- [×] 记忆验收使用隔离的用户目录和工作区，分别记录实际路径、注入顺序和 InvocationSnapshot 哈希。
- [×] 所有失败必须关联诊断 ID、日志位置和可复现步骤；不允许只保留测试框架摘要。
- [×] 发布前最后一次全矩阵必须从干净输出目录执行，并归档测试报告与制品哈希。

## 6. 完成标准 : 0%

V1 只有同时满足以下条件才可发布：

- [×] 所有协议消息有版本、文档和兼容规则。
- [×] Client SDK/Runtime 不共享内部实现程序集。
- [×] 三个 Provider Adapter 通过一致性测试。
- [×] 多 Run、取消、工具、审批、断线和重放端到端测试通过。
- [×] 三模式的启动、执行、取消、持久化和恢复通过。
- [×] 工具幂等、子智能体最小权限、委派和审计测试通过。
- [×] `.madorin`、游标分页、Blob 完整性和保留目录保护通过。
- [×] SampleHost 只依赖 Client SDK 完成完整链路。
- [×] 独立安装、PATH、Windows 双击启动、并行版本、升级和回退验证通过。
- [×] `madorin` 可完成首次配置、配置修改、Agent 管理和无宿主基本专家对话。
- [×] 全局/项目 `memory.md` 可以由 CLI 管理并在所有 Agent 启动时自动注入，LLM 可以快速调用内置 `builtin.memory` 工具，且未实现任何自动提取或向量存储。
- [×] 宿主运行不依赖个人 CLI 配置，配置隔离、认证和敏感信息脱敏测试通过。
- [×] 所有目标 RID 发布成功且 Native AOT 为 0 警告。
- [×] 已知问题中不存在数据错配、重复副作用、权限绕过、不可恢复损坏和无法终止任务。

## 7. 发布后决策记录

V1 验收数据用于决定 V2 的 PerRun/PerSession Worker、Provider 插件/独立 Worker、Gemini/多模态、远程传输、跨版本检查点和冷归档。任何 V2 决策都不得破坏 V1 已发布的任务、事件、工具和恢复协议。

## 8. 风险与禁止事项

- 不得把外部 Provider 不稳定当作跳过离线一致性测试的理由。
- 不得以“仅警告”为由接受 trimming/AOT、数据一致性或权限问题。
- 不得只验证正常流程而跳过故障注入和竞争测试。
- 不得发布包含真实凭据、测试密钥、用户 Prompt 或工作区数据的制品。
- 不得覆盖运行中版本或在没有可验证备份时迁移生产数据。
