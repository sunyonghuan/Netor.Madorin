# 阶段 6C：工作模式与故障恢复 : 48.1%

> 阶段状态：执行中 · 总进度：48.1%
>
> 前置阶段：[06B-会议模式与摘要](./06B-会议模式与摘要.md)

## 0. 进度跟踪

| 类别 | 已完成 | 总数 | 进度 |
| --- | ---: | ---: | ---: |
| 执行任务 | 35 | 64 | 54.7% |
| 测试要求 | 2 | 8 | 25% |
| 完成标准 | 0 | 5 | 0% |

标记说明：[×] 未完成；[√] 已完成。每完成一个任务，同时更新所属章节、阶段总进度和本表计数。

## 1. 目标

实现工作模式的总经理、计划、步骤、子智能体、权限等待和后台作业，并闭合步骤/工具两级幂等、宿主租约、Runtime 崩溃判定和三模式统一恢复。

## 2. 前置门禁

- 专家和会议模式已证明共用 Invocation、历史、工具和恢复基础可用。
- 工具调用在响应丢失和重启后不会重复副作用。
- GSN Outbox、Session Selection、Prompt rehydrate 和 ContextProjection 已稳定。

## 3. 涉及项目

| 项目 | 本阶段职责 |
| --- | --- |
| `Madorin.AI.Runtime.Modes.Work` | 总经理、计划、步骤、子智能体和恢复编排 |
| `Madorin.AI.Runtime.Orchestration.Abstractions` | 父子 Invocation、步骤调度和检查点接口 |
| `Madorin.AI.Runtime.Persistence.Sqlite` | Work Session、Step、Tool Intent 和恢复查询 |
| `Madorin.AI.Runtime.Persistence.Files` | 工作消息、计划和检查点关联 |
| `Madorin.AI.Runtime.Services` | 租约、恢复、并发和权限等待协调 |
| `Madorin.AI.Runtime.Modes.Tests` | 计划、依赖、循环、幂等和恢复测试 |
| `Madorin.AI.Runtime.EndToEnd.Tests` | 宿主/Runtime 崩溃、重连和副作用测试 |

## 4. 执行步骤 : 54.7%

### 4.1 固化工作模式配置 : 100%

- [√] `WorkOptions` 包含总经理 Agent、可用子智能体数组、WorkflowPolicy 和上下文策略。
- [√] WorkflowPolicy 至少定义最大深度、最大步骤数、并发、失败策略、循环检测和人工确认阈值。
- [√] 建议默认 `maxAgentDepth`、`maxStepsPerRun` 有保守上限，`loopDetection=Strict`。
- [√] 子智能体使用稳定 agentId 和能力声明；同一 ID 的定义更新走 Selection 版本。
- [√] 工作 Session mode 不允许中途改为其他模式。

### 4.2 建立工作模式 Schema : 83.3%

- [√] 建立 `work_sessions`，保存状态、总经理、策略版本和计划 messageId。
- [√] 建立 `work_steps`，保存 stepId、planVersion、依赖/父步骤、Invocation、Agent、深度、输入哈希和状态。
- [√] 建立 `work_tool_intents` 或通过共用工具意图表关联 stepId。
- [√] 步骤状态至少包含 Pending、Running、Completed、Failed、Interrupted。
- [√] 为 session、run、依赖、Pending/Running 状态、agent 和 stepId/planVersion 建索引。
- [×] 计划/步骤正文仍写 CanonicalHistory，结构表只保存可查询状态和引用。

### 4.3 实现总经理计划生成 : 50%

- [×] 总经理是父 Invocation，接收用户目标、可用 Agent/工具能力和 WorkflowPolicy。
- [√] 计划输出必须通过结构化 Schema 验证后才创建步骤。
- [√] 每个计划步骤拥有稳定 stepId、目标、依赖、输入引用、目标 Agent 和 planVersion。
- [×] 创建/修订计划时事务性保存计划消息与步骤集合，禁止部分计划进入 Running。
- [×] 修订计划递增 planVersion；已完成步骤保留历史，新版本明确引用复用或替代关系。
- [√] 无效依赖、环、超出深度/数量和未知 Agent 在执行前拒绝。

### 4.4 实现步骤调度器 : 71.4%

- [√] 只调度依赖已满足的 Pending 步骤。
- [√] 同深度不代表必然并行；仍受依赖、全局并发、Provider 限流和 Session 策略约束。
- [×] 步骤启动前事务性更新 Running、创建 Invocation 和检查点。
- [√] 子步骤 Invocation 设置 `parentInvocationId`，形成可追踪调用树。
- [√] 步骤完成前确认其所有工具意图进入终态，并持久化输出引用。
- [×] 失败策略明确为 Stop、Continue、Retry 或 AskHost；Retry 有上限和退避。
- [√] V1 自行实现必要调度，不直接依赖 Experimental MAF Workflows Runtime。

### 4.5 实现两级幂等 : 100%

- [√] 步骤键由 `stepId + planVersion + serializedStepInputs` 计算 `stepInputHash`。
- [√] 同一步骤重试保持 stepId/planVersion，但每次尝试生成新的 invocationId。
- [√] 工具调用使用独立 callId，并关联所属 stepId；一个步骤可以没有、一个或多个工具调用。
- [√] 步骤 Completed 只有在全部必要工具调用和输出持久化后成立。
- [√] Interrupted 恢复时先查询所有 Sent 工具意图结果，再决定继续、重试或人工介入。
- [√] 已完成有副作用工具不得因步骤重试再次发送。

### 4.6 实现子智能体与后台作业 : 66.7%

- [√] 子智能体每次执行都是独立 AgentInvocation，具有自己的 Snapshot、取消和事件。
- [√] 总经理只通过调度/结果引用协调，不共享可变 Agent 实例。
- [×] 后台作业有稳定 job/step 关联、状态查询、超时和取消。
- [√] Run 取消向活动子 Invocation 传播，但已持久化结果保留。
- [√] 子智能体失败不直接崩溃 Runtime；由步骤失败策略决定 Run 行为。
- [×] 检测重复目标、无进展循环和递归深度；触发阈值时暂停并发起审批。

### 4.7 强制子智能体权限边界 : 0%

- [×] 每次权限请求记录实际子 agentId、parentAgentId、stepId 和调用链。
- [×] 子智能体默认没有总经理 Grant。
- [×] 显式委派时有效权限取 root Grant、父级可委派范围、子 Agent 允许项和 Runtime 策略交集。
- [×] 继续委派必须再次显式允许，且深度、Agent 和作用域都在 Grant 范围内。
- [×] Grant 撤销立即阻止尚未发送的后代调用；已发送结果返回后不得继续使用。
- [×] 审计可以从工具结果追溯到 callId、stepId、Invocation、父 Invocation 和 rootGrantId。

### 4.8 实现工作模式 ContextProjection : 40%

- [√] 默认 `preservePlan=true` 和 `preserveCurrentStep=true`。
- [×] 当前步骤输入、依赖输出引用、工具调用/结果和总经理计划不可被拆散。
- [×] 早期已完成步骤的大型工具结果优先转 Blob 或摘要，不删除 CanonicalHistory。
- [√] 子智能体只获得完成当前步骤所需的最小投影，不能默认看到全部工作区历史和其他 Agent 私有内容。
- [×] 投影策略、保留项和裁剪量记录到 InvocationSnapshot/调整事件。

### 4.9 实现宿主租约与断线行为 : 0%

- [×] 控制通道断开后，纯模型/本地安全步骤可按配置继续并缓冲事件。
- [×] 等待宿主工具、审批或凭据的步骤保持等待并启动租约倒计时。
- [×] 宿主以同一 hostInstanceId 和 GSN 游标重连后恢复等待请求和事件重放。
- [×] 租约到期按策略取消等待步骤或整个 Run，形成明确终态。
- [×] 重连不得重复创建步骤、审批或工具意图。

### 4.10 实现 Runtime 崩溃恢复 : 42.9%

- [×] 启动时先恢复数据库、Outbox、工具意图和检查点，再开放连接/新 Run。
- [×] 无终态 Run、Running Invocation 和 Running Step 标记 Interrupted。
- [√] 对每个 Sent 工具意图执行 `tool.result.query`，把结果写回后再判定步骤。
- [√] 已持久化 Completed/Failed 步骤不重复执行。
- [√] 无法安全判定的副作用步骤返回 RequiresManualIntervention，不自动重试。
- [×] 恢复后所有历史 Run 必须处于明确终态或 Interrupted，不存在永久 Running。
- [×] WAL checkpoint、锁和保留目录在异常退出后可诊断恢复。

### 4.11 完成三模式统一恢复 : 40%

- [√] 专家恢复 Agent 和历史，会议恢复参与者/轮次，工作恢复计划/步骤/工具意图。
- [×] 三种模式统一返回 Selection、neededDefinitions、附件/Blob、检查点和 lastGsn。
- [×] rehydrate Ready 前不启动新 Invocation。
- [√] 恢复行为使用 sessionId 和数据库索引，不扫描日期目录。
- [×] 数据损坏、Blob 哈希不一致和 Schema 不兼容均返回明确诊断。

## 5. 测试要求 : 25%

- [√] 计划覆盖线性、分支、并行依赖、空计划、循环、未知 Agent 和超限深度/步骤。
- [×] 在计划事务、步骤 Running、工具 Sent、结果落库、步骤 Completed 各边界强制终止 Runtime。
- [√] 恢复后有副作用工具最多执行一次，无法判定时进入人工介入而非自动重发。
- [×] 子智能体默认无法使用总经理 Grant，显式委派仍不能扩大权限。
- [×] 宿主断线/重连不重复审批、步骤、Invocation 或事件业务结果。
- [×] 取消父 Run 能终止活动子 Invocation 和进程工具，不影响其他 Run。
- [×] 三模式批量恢复后无虚假 Running，GSN 继续单调。
- [×] 长计划投影保留计划和当前步骤，同时控制 token 上限。

## 6. 完成标准 : 0%

- [×] 工作模式可完成计划、步骤、子智能体、工具、审批和最终汇总闭环。
- [×] 步骤级与工具调用级幂等边界清晰并通过故障注入。
- [×] 宿主崩溃、Runtime 崩溃和任一通道中断后均能明确判定状态。
- [×] 三种模式均可按 sessionId 恢复到最近安全状态。
- [×] 不存在永久 Running、重复副作用、权限继承扩大或无界递归。

## 7. 风险与禁止事项

- 不得把 `stepInputHash` 与工具 `argumentsHash` 合并为一个幂等键。
- 不得让总经理/子智能体回到宿主进程执行。
- 不得在 V1 直接采用 MAF Workflows 的内部持久化替代 `work_steps`。
- 不得对无法判定的副作用步骤自动重跑。
- 不得用完整 CanonicalHistory 默认填充每个子智能体上下文。
