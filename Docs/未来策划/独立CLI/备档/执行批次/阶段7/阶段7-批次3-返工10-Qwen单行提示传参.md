# 阶段 7 批次 3 返工 10：Qwen 单行提示传参

## 失败会话

- Qwen session：`22d8362d-cd9b-4299-a32c-a34d66eb0032`
- 结果：即使 PowerShell 变量显式引用，Qwen 仍只收到提示词第一行；未读取文件、未调用工具、未修改文件。
- 原因：`qwen.cmd` 的批处理参数解析不能可靠传递包含换行的 `--prompt` 值。

## 新会话要求

- 不使用 `--resume`，启动全新 Qwen 会话。
- `--prompt` 必须为不含换行的单个字符串，任务细节从返工 8 文档读取。
- 提示中必须包含当前工作目录、策划目录、项目目录、任务文档和允许修改范围。
- 执行要求继续以 `阶段7-批次3-返工8-Qwen生产实现工具预算.md` 为准。

## 允许修改范围

- `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Server/RuntimeServer.cs`
- `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Client/IRuntimeClient.cs`
- `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Client/RuntimeClient.cs`

禁止修改其他文件；禁止运行构建和测试；完成编辑后立即停止。
