# 阶段 7 批次 3 返工 11：Qwen 禁止检索直接编辑

## 失败会话

- Qwen session：`89a9845e-dd3a-4bde-acec-512d5fb9f128`
- 结果：正确读取返工 8 文档，但第 41 次工具调用时超过 `--max-tool-calls 40`，未编辑文件。
- 原因：在任务文档已经给出类型和接入点后，仍反复调用 `grep_search` 和读取无关类型。

## 已确认信息

- 返工 8 文档给出的接入点、类型、签名和行为要求全部有效。
- `SessionListParameters`、`SessionListItem`、`SessionListResult`、`SessionListQuery` 已存在。
- cursor 格式由仓储实现确定为 `updatedAt|sessionId`，Server 应按仓储既有格式生成下一页 cursor。
- 上一会话没有调用编辑工具，允许范围内的三个文件没有产生本次会话的新修改。

## 新会话硬性要求

- 不使用 `--resume`，启动全新 Qwen 会话，工具预算提高到 100。
- 读取返工 8 和本文档后，不得再使用 `grep_search`、glob、递归搜索或读取 Contracts/Entities/Persistence 文件。
- 只允许对三个目标文件做必要的短区间读取，并在首次短区间读取后立即使用编辑工具。
- 必须完成返工 8 的全部四项编辑；不得运行构建或测试；编辑完成后立即停止。

## 允许修改范围

- `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Server/RuntimeServer.cs`
- `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Client/IRuntimeClient.cs`
- `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Client/RuntimeClient.cs`
