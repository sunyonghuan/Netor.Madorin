# 阶段 7 批次 3 返工 4：兼容性与排序语义

## 工作目录

- 仓库根目录：`E:\Netor.me\Madorin\Netor.Madorin`
- 执行计划目录：`E:\Netor.me\Madorin\Netor.Madorin\Docs\未来策划\独立CLI`
- 待修改项目目录：`E:\Netor.me\Madorin\Netor.Madorin\Src\Madorin.Ai.Runtime`
- 原任务文档：`E:\Netor.me\Madorin\Netor.Madorin\Docs\未来策划\独立CLI\备档\执行批次\阶段7\阶段7-批次3-Session列表与筛选.md`
- 本返工文档：`E:\Netor.me\Madorin\Netor.Madorin\Docs\未来策划\独立CLI\备档\执行批次\阶段7\阶段7-批次3-返工4-兼容性与排序语义.md`

## 已确认问题

1. 旧重载 `ListSessionsAsync(string? cursor, int pageSize, ...)` 委托给 `SessionListQuery` 时继承了 `Status = Active` 默认值，破坏旧接口返回所有 Session 状态的既有行为。
2. `TrySetInitialSessionTitleAsync` 写入初始标题时同时更新 `sessions.updated_at`，导致仅补充标题的操作改变列表顺序和 keyset 分页位置。

## 返工要求

- 旧重载委托时必须显式传入 `Status: null`，保持原有全状态列表语义。
- 初始标题写入只更新 `title`，不得更新 `updated_at`。
- 增加持久化测试，分别证明旧重载包含非 Active Session，以及设置标题不会改变 `updated_at`。
- 保持新结构化查询默认 `Status = Active` 不变。
- 不修改 Schema 14、DTO、Server 或 Client；不重写或格式化整个文件。
- 不使用子代理，不修改允许范围之外的任何文件。

## 允许修改范围

- `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Persistence.Sqlite/SqliteSessionRepository.cs`
- `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.Persistence.Tests/SessionListStage7Tests.cs`

## 验收

- 新增测试通过。
- 原 `ListSessionsAsync_100kSessions_UsesKeysetIndexWithinBaseline` 测试通过。
- Persistence.Sqlite 与 Persistence.Tests 严格 Debug 构建为 0 警告、0 错误。
- `git diff --check` 通过，且实际修改范围符合白名单。
