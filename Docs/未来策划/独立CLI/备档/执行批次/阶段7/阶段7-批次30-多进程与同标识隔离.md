# 阶段 7 批次 30：多进程与同标识隔离 : 100%

## 目标与结论

- [√] 补齐一个宿主启动多个 Runtime、多个宿主分别启动多个 Runtime，以及宿主与独立 CLI 同时运行的完整进程拓扑。
- [√] 故意让两个经过真实 Named Pipe 双向认证的 Runtime 测试替身使用相同 `runId`、相同数值 GSN 和相同 `callId`，验证 Client 操作始终限定于实例绑定。
- [√] 联合真实多宿主进程测试验证关闭或回收一个 Runtime 不影响其他 Runtime、宿主和独立 CLI。
- [√] 本批不修改生产实现、协议 DTO、协议版本或数据库 Schema。

## 实施证据

- [√] `TwoHostsWithTwoRuntimes_IsolatedAndCrossConnectionsFail` 在两个独立 SampleHost 进程各自管理两个 Runtime 的同时，启动第五工作区的独立 `madorin memory add project` CLI 进程；完整拓扑同时包含 2 个 Host、4 个 Runtime 和 1 个 CLI。
- [√] 独立 CLI 通过 `ProcessStartInfo.ArgumentList` 接收路径参数、通过 stdin 接收记忆正文，退出码为 0，项目记忆落盘；随后四个宿主 Runtime 均可重新连接。
- [√] 真实拓扑继续覆盖 secret、Pipe、`runtimeInstanceId` 和 PID 交叉连接拒绝；回收 Host A 的一个 Runtime 后，其余三个 Runtime 仍可连接。
- [√] `BoundClients_SameRunIdAndGsn_KeepAllOperationsInstanceScoped` 建立两套独立控制/事件 Pipe、独立 secret、独立 `hostInstanceId` 和 `runtimeInstanceId`，完成 Client Hello、Runtime Hello、Client Confirmation、认证控制帧和事件通道证明。
- [√] 两个 Runtime 同时发送 `runId=shared-run-id`、`gsn=17` 的事件，Client 分别校验事件实例归属和实例专属载荷。
- [√] 两个 Client 使用相同 `runId` 查询得到不同 Session；取消和 `events.acknowledge` 的计数/水位只进入目标 Runtime。
- [√] 两个 Runtime 使用相同 `runId` 和 `callId` 发起宿主权限回调，各 Client 只执行自身注册的回调；Dispose Client A 后 Client B 仍可查询和处理回调。
- [√] 同 ID 测试替身不宣称进程级证据；进程所有权、关闭和回收隔离由真实 `MultiInstanceProcessTests` 与既有 Client 生命周期测试联合证明。

## 测试与门禁

- [√] 批次 30 两项定向测试 2 / 2 通过。
- [√] `Madorin.AI.Runtime.EndToEnd.Tests` 259 / 259 通过。
- [√] 全解决方案 836 项通过，3 个真实 Provider 用例因缺少外部凭据跳过，0 失败。
- [√] Debug/Release `--no-restore -warnaserror -m:1` 均为 0 警告、0 错误。
- [√] 协议保持 `1.1`，SQLite Schema 保持 `14`；批次 25 的 Windows `win-x64` Release Native AOT 无警告证据继续有效。
- [√] CodeMap workspace `session` 刷新 208 个文件、1916 个符号，overlay revision 为 `167`。

## 进度与剩余边界

- [√] 07 测试要求由 10 / 14 更新为 12 / 14，总清单由 91 / 96 更新为 93 / 96（96.9%）。
- [√] 执行步骤总览由 764 / 912 更新为 766 / 912（84.0%）。
- [√] 阶段 7 执行计划保持 46 / 54（85.2%）；本批关闭的两项主清单测试要求已由 Client SDK 和 SampleHost 章节覆盖，不对应新的独立未完成项。
- [×] 剩余 3 项继续保留：首次启动/配置/Agent/PATH/双击联合验证、双 CLI 进程并行/互斥/取消/锁释放矩阵，以及新终端 PATH/双击/无参数 REPL 实测完成标准。

## 修改文件

- `Docs/未来策划/独立CLI/备档/执行批次/阶段7/阶段7-批次30-多进程与同标识隔离.md`
- `Docs/未来策划/独立CLI/备档/执行步骤/07-CLI-ClientSDK与参考宿主.md`
- `Docs/未来策划/独立CLI/执行步骤/README.md`
- `Docs/未来策划/独立CLI/备档/执行计划/执行计划(阶段7-CLI-ClientSDK与参考宿主).md`
- `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/MultiInstanceProcessTests.cs`
- `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/RuntimeClientMultiInstanceIsolationStage7Tests.cs`
