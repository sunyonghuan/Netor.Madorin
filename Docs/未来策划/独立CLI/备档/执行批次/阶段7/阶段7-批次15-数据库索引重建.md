# 阶段 7 批次 15：数据库索引重建 : 100%

## 目标与冻结边界

- [√] `db rebuild` 只从 `messages/*.jsonl` 重建全新的 `state.db`、Session 元数据和消息索引。
- [√] CLI 复用全局 `--workspace` 与 `--data-dir`，增加 `--dry-run` 和 `--confirm`。
- [√] dry-run 只扫描并展示计划，不获取工作区写锁、不创建目录、不备份、不写入。
- [√] 实际执行在确认后获取工作区排他锁；锁冲突返回退出码 4 且不修改数据。
- [√] CLI 只调用 Server 高层维护服务，不直接初始化 SQLite 或构造 `ConversationStore`。

## 恢复点与诊断

- [√] 覆盖现有数据库前，将 `state.db` 及现存 WAL/SHM 保存到同卷唯一恢复点。
- [√] 恢复点失败时不开始重建；重建中途失败时保留恢复点并返回其路径。
- [√] Canonical JSONL 始终只读；尾行损坏只恢复有效前缀，中间损坏只报告且不改写原文件。
- [√] 恢复 Session 状态固定为 `Recovered`，Run、Grant、工具意图和 Invocation 快照明确报告为不可恢复。
- [√] 存在不可恢复 Session 时保留已恢复结果，返回 partial 状态和退出码 1。

## CLI 与输出

- [√] JSON 模式实际执行缺少 `--confirm` 时返回退出码 2，且不修改数据。
- [√] 人类模式缺少 `--confirm` 时展示计划并交互确认；拒绝返回 130。
- [√] JSON 成功或 partial 输出固定包含命令、状态、数据目录、扫描/恢复计数、恢复点和诊断数组。
- [√] 人类输出先展示计划，再显示恢复点、恢复数量和不可恢复诊断。

## 测试与验证

- [√] 红灯覆盖 dry-run 零写入/无锁、确认、拒绝、锁冲突、成功恢复和中间损坏。
- [√] 故障用例覆盖恢复点创建失败与重建中途失败后的恢复点保留。
- [√] 定向回归、Persistence、EndToEnd、全解决方案、严格构建、Native AOT、CodeMap 和差异检查通过。

## 修改文件

- [√] `Docs/未来策划/独立CLI/备档/执行批次/阶段7/阶段7-批次15-数据库索引重建.md`
- [√] `Docs/未来策划/独立CLI/命令规范/04-CLI命令规范.md`
- [√] `Docs/未来策划/独立CLI/备档/执行步骤/07-CLI-ClientSDK与参考宿主.md`
- [√] `Docs/未来策划/独立CLI/执行步骤/README.md`
- [√] `Docs/未来策划/独立CLI/备档/执行计划/执行计划(阶段7-CLI-ClientSDK与参考宿主).md`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Entities/SessionStatus.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Persistence.Files/ConversationStore.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Server/DatabaseMaintenanceModels.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Server/DatabaseMaintenanceService.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Cli/Commands/DatabaseCommands.cs`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.Persistence.Tests/ConversationStoreTests.cs`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/StandaloneDatabaseCommandsStage7Tests.cs`

## 验证记录

- 红灯基线：原有 21 项通过，新增 8 项失败；失败覆盖缺失选项、确认、锁、全量替换、partial 和恢复点错误模型。
- 定向回归：`StandaloneDatabaseCommandsStage7Tests` 29 / 29 通过。
- 项目回归：Persistence 171 / 171、EndToEnd 197 / 197 通过。
- 全解决方案：768 项通过；3 项真实 Provider 冒烟因缺少外部凭据按预期跳过。
- 严格构建：Debug/Release 均为 0 警告、0 错误。
- Windows `win-x64` Release Native AOT：0 条 trimming/AOT 警告；`madorin --help` 与 `madorin db rebuild --help` 实测通过。
- 协议版本 `1.1`、Schema `14`；本批未变更协议或 Schema。
- CodeMap workspace `session` overlay revision `95`：本批 7 个 C# 文件、142 个符号刷新。
- `git diff --check` 通过；仅报告工作树既有的 LF/CRLF 转换提示。
