# 阶段 7 返工：事件通道就绪竞态 : 100%

> 状态：已完成

## 问题

- EndToEnd 全量运行时，`ReadEventsAsync_WithoutExplicitAcknowledgement_ReplaysAfterReconnect` 稳定在 15 秒后取消。
- 同一用例独立连续 10/10 通过；排除批次 9 全部测试后，原 149 项全量仍稳定复现。
- 堆栈停在客户端 Named Pipe 帧头读取。服务端事件连接先重放 Outbox、后挂载到 Session；客户端可能已收到首个重放事件，而服务端断开入口仍看不到该通道，导致断开成为无操作。

## 修复边界

- [√] 在事件派发锁内先挂载通道，再开始 Outbox 重放。
- [√] 保证实时派发不能抢在重放前发送，维持 GSN 顺序。
- [√] 通过两个事件断线重连用例。
- [√] 通过 EndToEnd 全量、解决方案全量和严格构建。
- [√] 刷新 CodeMap 并记录验证证据。

## 约束

- 不增加协议消息类型，不修改协议版本、JSON Schema 或传输帧格式。
- 不改变 GSN 确认语义；未确认事件仍由现有 Outbox 重放。
- 不扩大到控制通道、Blob 通道或跨平台传输重构。

## 验证记录

- 修复前：EndToEnd 154 项连续两次均为 153 / 154；排除批次 9 后原 149 项仍为 148 / 149，同一重连用例超时。
- 隔离复核：失败用例独立连续 10 / 10 通过，确认是负载调度下的半就绪竞态，不是事件重放逻辑的单例失败。
- 定向：`EventDisconnect_ReconnectsWithoutRepeatingControlInitialization` 与 `ReadEventsAsync_WithoutExplicitAcknowledgement_ReplaysAfterReconnect` 合跑 2 / 2 通过。
- EndToEnd：修复后 154 / 154 通过。
- 解决方案：721 个通过；3 个真实 Provider 用例按外部凭据条件跳过。
- Debug/Release 严格构建：均为 0 警告、0 错误。
- CodeMap：workspace `session` 已刷新到 overlay revision `56`。
- `git diff --check`：通过；仅有工作树既有 LF/CRLF 提示。

## 修改文件

- `Docs/未来策划/独立CLI/备档/执行批次/阶段7/阶段7-返工-事件通道就绪竞态.md`
- `Docs/未来策划/独立CLI/备档/执行计划/执行计划(阶段7-CLI-ClientSDK与参考宿主).md`
- `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Server/RuntimeServer.cs`
