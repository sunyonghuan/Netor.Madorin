# 阶段 7 批次 24：安装、集成、安全与运维文档 : 100%

## 目标与交付

- [√] 新增安装与 CLI 使用指南，覆盖 PATH、Windows 双击、用户目录、首次配置、Agent、全局/项目记忆、单次运行、REPL、两阶段 Ctrl+C 和 Doctor。
- [√] 新增宿主集成指南，覆盖依赖边界、三种启动策略、实例绑定、显式 GSN 确认、宿主回调、Session/Selection、重连、恢复、Dispose 和版本回退。
- [√] 新增安全与数据运维指南，覆盖 Restricted、UnrestrictedShell 非沙箱风险、secret/日志/`.madorin` 边界、数据库维护、Blob 清理、排他锁、恢复点和故障处置顺序。
- [√] 更新总体文档索引，使三份用户与运维指南可从独立 CLI README 直接访问。

## 契约复核

- [√] 文档只使用正式 `madorin`/`madorin.exe` 和 `Madorin.AI.Runtime` 命名，不保留旧用户命令名。
- [√] 协议固定为 `1.1`，SQLite Schema 固定为 `14`。
- [√] 个人 `config.json` 仅用于直接启动；宿主只依赖 Client/Contracts 并通过 SDK/协议提供配置、凭据和 Selection。
- [√] Restricted 明确为 Runtime 权限策略而非 OS 沙箱；`UnrestrictedShell` 必须显式授权，使用当前用户权限，阶段 7 不将其描述为默认可用能力。
- [√] 维护命令参数与当前发布版帮助一致：`doctor --provider/--fix/--yes`、`db check --verbose`、`db repair --dry-run/--yes`、`db rebuild --dry-run/--confirm`、`db backup --output/--include-messages`、`db migrate --to/--dry-run`、`storage check --verify-hashes`、`storage gc --dry-run/--older-than/--confirm`。
- [√] `db check` 和 `storage check` 保持严格只读；所有写命令的锁、确认、备份或恢复点边界均与实现一致。

## 验证

- [√] 当前 Windows `win-x64` Release Native AOT 发布版的 Doctor、六个数据库子命令和两个存储子命令 `--help` 均返回 0，选项与文档一致。
- [√] 三份文档不包含旧用户命令名，Markdown 围栏、相对链接、协议和 Schema 标记通过静态检查。
- [√] 本批只修改 Markdown，不修改 C#；沿用批次 23 的全量 825 项、Debug/Release 严格构建和 Native AOT 结果，无需重复编译。

## 进度与剩余边界

- [√] 07 文档执行任务由 65 / 74 更新为 71 / 74，总清单由 70 / 96 更新为 77 / 96。
- [√] 阶段 7 执行计划由 36 / 54 更新为 39 / 54。
- [√] 执行步骤总览按 13 个阶段行重新求和为 750 / 912（82.2%），修正既有总计与分阶段明细不一致。
- [×] 完整命令参数集合、默认值、示例、互斥条件、退出码和 JSON Schema 快照仍待后续批次闭合。
- [×] PATH 发现、Windows 双击、完整 Client/多实例矩阵及最终 74 / 74 门禁仍须按主清单逐项提供实测证据。

## 修改文件

- `Docs/未来策划/独立CLI/使用指南/06-安装与CLI使用.md`
- `Docs/未来策划/独立CLI/使用指南/07-宿主集成指南.md`
- `Docs/未来策划/独立CLI/使用指南/08-安全与数据运维.md`
- `Docs/未来策划/独立CLI/README.md`
- `Docs/未来策划/独立CLI/备档/执行批次/阶段7/阶段7-批次24-安装集成安全与运维文档.md`
- `Docs/未来策划/独立CLI/备档/执行步骤/07-CLI-ClientSDK与参考宿主.md`
- `Docs/未来策划/独立CLI/执行步骤/README.md`
- `Docs/未来策划/独立CLI/备档/执行计划/执行计划(阶段7-CLI-ClientSDK与参考宿主).md`
