# 阶段 7 批次 13：数据库检查与修复 : 100%

## 目标与冻结边界

- [√] `db check` 为严格只读命令，不创建目录、数据库、锁文件或诊断文件，不执行迁移、WAL checkpoint 或恢复动作。
- [√] `db check` 不再公开 `--repair`、`--yes`，只保留全局工作区/数据目录和本命令 `--verbose`。
- [√] 新增独立 `db repair`，修复范围仅限已识别的 JSONL 损坏尾行和已有 Session 的消息索引不一致。
- [√] 中间行损坏、缺失 Session、缺失消息文件、SQLite 损坏和 Schema 不兼容只报告，不自动重建或迁移。
- [√] CLI 只调用 Server 高层维护服务，不直接扫描、截断或写入 Runtime 内部文件。

## 检查、锁与恢复点

- [√] 只读检查覆盖 SQLite quick/integrity check、foreign key、Schema 版本、WAL 模式、必需表和索引。
- [√] 只读检查覆盖 JSONL Header、连续序号、损坏尾行/中间行、Session 双向对应和消息索引一致性。
- [√] `db repair` 在展示计划并确认后获取工作区排他锁；锁冲突不得修改数据。
- [√] 实际修复前建立包含 SQLite 文件和待修复 JSONL 的恢复点；备份失败不得开始修复。
- [√] 用户拒绝确认、无可修复项和中途故障均返回稳定结果并保留原始诊断及恢复点。

## 测试与验证

- [√] 红灯测试命中旧 `db check --repair/--yes` 表面和缺失的独立 `db repair`。
- [√] 正常与损坏数据执行 `db check` 后，目录树、文件内容和最后写入时间保持不变。
- [√] Schema/WAL/SQLite、JSONL、Session 和消息索引不一致均产生可机器解析诊断。
- [√] `db repair` 覆盖拒绝确认、排他锁冲突、备份、尾行修复、索引修复和中间行损坏保持原文件。
- [√] 定向回归、Persistence、EndToEnd、全解决方案、严格构建、CodeMap 和差异检查全部通过。

## 修改文件

- [√] `Docs/未来策划/独立CLI/备档/执行批次/阶段7/阶段7-批次13-数据库检查与修复.md`
- [√] `Docs/未来策划/独立CLI/命令规范/04-CLI命令规范.md`
- [√] `Docs/未来策划/独立CLI/备档/执行步骤/07-CLI-ClientSDK与参考宿主.md`
- [√] `Docs/未来策划/独立CLI/执行步骤/README.md`
- [√] `Docs/未来策划/独立CLI/备档/执行计划/执行计划(阶段7-CLI-ClientSDK与参考宿主).md`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Persistence.Files/ConversationIntegrityInspector.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Persistence.Sqlite/SqliteSchema.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Server/DatabaseMaintenanceModels.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Server/DatabaseMaintenanceService.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Cli/Commands/DatabaseCommands.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Cli/CliApplication.cs`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/StandaloneDatabaseCommandsStage7Tests.cs`

## 验证记录

- 红灯：基础 9 项中 8 项命中旧的成功占位，1 项因旧命令表面解析失败；实现后基础 9 / 9 通过。
- 定向回归：`StandaloneDatabaseCommandsStage7Tests` 14 / 14，通过严格零写入、活动 WAL、无 SHM WAL、JSON `--yes`、dry-run、锁、备份失败和中途故障恢复点场景。
- 项目回归：Persistence 171 / 171；EndToEnd 182 / 182。
- 解决方案全量：753 个通过；3 个真实 Provider 用例按外部凭据条件跳过。
- 严格构建：Debug/Release `--no-restore -warnaserror -m:1` 均为 0 警告、0 错误。
- Native AOT：Windows `win-x64` Release 发布成功，0 条 trimming/AOT 警告。
- CodeMap：baseline `369c49099fd03afd05db23e540096b3bf18f3924`，workspace `session`，最终 revision 以执行计划记录为准。
- 差异检查：`git diff --check` 通过；仅有工作树既有的 LF/CRLF 提示。
