# 阶段 7 批次 2 返工 1：Qwen 提示识别

## 失败现象

首个 Qwen Code 会话只回复“已准备在 PowerShell 中工作”，未读取任务文档、未调用文件工具、未修改允许范围内文件。实际 `git diff` 未出现本批新增代码。

## 返工要求

1. 使用全新 Qwen Code 会话，不使用 `--resume`。
2. 提示词第一句直接要求“立即实现”，避免把工作目录说明误判为无任务上下文。
3. 以 `阶段7-批次2-Client高层API与宿主回调.md` 为唯一任务说明，严格限制允许修改路径。
4. 完成后必须列出实际修改文件；主代理以 `git diff` 为准审查。
