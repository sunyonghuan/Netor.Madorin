# 阶段 7 批次 27：记忆续调与特殊字符串往返 : 100%

## 目标与结论

- [√] 使用真实 Builtin Tool Catalog、Tool Gateway 和 Memory File Service，验证模型调用 `builtin.memory.read` 后获得结果并在同一 Provider 的下一轮续调中使用。
- [√] 通过真实 RuntimeServer、Named Pipe、认证握手和 RuntimeClient 高层 API，验证路径、URL、中文、引号、换行及首尾空白逐字符往返不变。
- [√] 复用既有 CLI 参数数组、UTF-8 输入文件和记忆工具 Schema/进程内执行证据，关闭 07 主清单中的两个测试要求。
- [√] 本批仅增加测试和必要的测试项目引用，不修改生产实现、协议版本或数据库 Schema。

## 实施证据

- [√] `ExpertToolLoopTests.RunAsync_MemoryRead_ReturnsSnapshotToSameProvider` 使用 `BuiltinToolRegistry`，断言 Provider 同时看到 `builtin.memory.read` 与 `builtin.memory.append`。
- [√] 测试经 `BuiltinMemoryToolExecutor` 读取 Invocation 固定的项目记忆快照，第二次 Provider 请求收到成功的 `ToolResultContentBlock`、原始记忆正文和非空哈希，Run 最终完成。
- [√] `RuntimeClientHighLevelStage7Tests.StartNewSessionRunAsync_SpecialCharacters_PreservesPromptAcrossTransport` 使用真实 Client/Server/Named Pipe 链路，Provider 捕获文本与调用方输入完全相等。
- [√] 特殊字符串覆盖 Windows 路径、空格、中文、双引号、URL 百分号编码与保留字符、换行、前导和尾随空白。

## 测试与门禁

- [√] 记忆续调定向测试 1 / 1 通过。
- [√] SDK 特殊字符串定向测试 1 / 1 通过。
- [√] `Madorin.AI.Runtime.Modes.Tests` 92 / 92 通过。
- [√] `Madorin.AI.Runtime.EndToEnd.Tests` 253 / 253 通过。
- [√] 全解决方案 829 项通过，3 个真实 Provider 用例因缺少外部凭据跳过，0 失败。
- [√] Debug/Release `--no-restore -warnaserror -m:1` 均为 0 警告、0 错误。
- [√] 协议保持 `1.1`，SQLite Schema 保持 `14`；本批未修改发布项目，批次 25 的 Native AOT 无警告证据继续有效。
- [√] CodeMap workspace `session` 已刷新至 overlay revision `158`。

## 进度与剩余边界

- [√] 07 测试要求由 7 / 14 更新为 9 / 14，总清单由 87 / 96 更新为 89 / 96（92.7%）。
- [√] 执行步骤总览由 760 / 912 更新为 762 / 912（83.6%）。
- [√] 阶段 7 执行计划保持 46 / 54（85.2%）；本批两个主清单测试要求不对应执行计划中的独立未完成项。
- [×] 剩余 7 项继续保留：Client 生命周期联合矩阵、完整多进程拓扑、同 ID 多实例隔离、首次启动与发布联合验证、双 CLI 进程矩阵、新终端/PATH/双击实测，以及 Client SDK 隐藏内部细节的最终标准。

## 修改文件

- `Docs/未来策划/独立CLI/备档/执行批次/阶段7/阶段7-批次27-记忆续调与特殊字符串往返.md`
- `Docs/未来策划/独立CLI/备档/执行步骤/07-CLI-ClientSDK与参考宿主.md`
- `Docs/未来策划/独立CLI/执行步骤/README.md`
- `Docs/未来策划/独立CLI/备档/执行计划/执行计划(阶段7-CLI-ClientSDK与参考宿主).md`
- `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.Modes.Tests/Madorin.AI.Runtime.Modes.Tests.csproj`
- `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.Modes.Tests/ExpertToolLoopTests.cs`
- `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/RuntimeClientHighLevelStage7Tests.cs`
