# 阶段 7 批次 2 返工 2：Qwen 工具预算

## 失败现象

第二个全新 Qwen Code 会话正确识别任务，但重复执行宽范围探索，在第 41 次工具调用前仍未编辑文件，因工具预算耗尽退出。

## 返工要求

1. 再次启动全新会话，不使用 `--resume`。
2. 将任务缩小为只修改 Client 生产代码，不创建测试、不运行测试。
3. 禁止启动子代理和宽范围搜索；只读取任务文档及五个允许修改的 Client 文件。
4. 直接实现既定 API、回调调度和显式事件确认，完成后立即结束。
5. 测试由主代理在代码审查后单独补充。
