# 阶段 7 批次 20：统一输出与退出码 : 100%

## 目标与冻结边界

- [√] 所有 `--json` 成功输出统一包含 `success=true`、`data`、`warnings` 和 `diagnosticId`；命令原有业务字段只放入 `data`。
- [√] 所有 `--json` 失败输出统一包含 `success=false`、`error.code/message/isRetryable/diagnosticId`、`warnings` 和顶层 `diagnosticId`；恢复点等失败上下文可保留在 `data`。
- [√] System.CommandLine 解析错误统一返回退出码 2；JSON 模式只向 stdout 写一个可独立解析的失败信封，人类模式只向 stderr 写错误。
- [√] JSON/JSONL stdout 不混入诊断、进度或 ANSI 控制码；`--no-color` 与非 TTY 输出保持无 ANSI。
- [√] `run --output-format jsonl` 的每一行都是完整对象，中间事件和终态可区分，终态可独立判断成功或失败。
- [√] 固定退出码只使用 0、1、2、3、4、5 和 130，不引入命令私有数值。
- [√] 已有 `run --output`、`session export --output` 和维护备份继续使用同目录临时文件与原子发布；失败时不覆盖旧结果。

## 测试与验证

- [√] 红灯覆盖版本、配置、Session/数据库/存储、在线控制和解析错误的统一 JSON Schema。
- [√] 覆盖 stdout/stderr 分离、无 ANSI、JSONL 逐行解析和独立终态判断。
- [√] 回归已有 Run/Session 导出原子输出测试，确认失败保留旧文件。
- [√] 定向回归、EndToEnd、全解决方案、严格 Debug/Release、Native AOT、CodeMap 和差异检查通过。

## 允许修改文件

- [√] `Docs/未来策划/独立CLI/备档/执行批次/阶段7/阶段7-批次20-统一输出与退出码.md`
- [√] `Docs/未来策划/独立CLI/命令规范/04-CLI命令规范.md`
- [√] `Docs/未来策划/独立CLI/备档/执行步骤/07-CLI-ClientSDK与参考宿主.md`
- [√] `Docs/未来策划/独立CLI/执行步骤/README.md`
- [√] `Docs/未来策划/独立CLI/备档/执行计划/执行计划(阶段7-CLI-ClientSDK与参考宿主).md`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Cli/CliApplication.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Cli/Commands/CliOutput.cs`
- [√] `Src/Madorin.Ai.Runtime/src/Madorin.AI.Runtime.Cli/Commands/DiagnosticCommands.cs`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/CliVersionTests.cs`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/Stage7CommandMatrixTests.cs`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/Stage7UnifiedOutputTests.cs`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/StandaloneControlCommandsStage7Tests.cs`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/StandaloneDatabaseCommandsStage7Tests.cs`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/StandaloneSessionCommandsStage7Tests.cs`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/StandaloneStorageCommandsStage7Tests.cs`

## 验证记录

- 红灯：`Stage7UnifiedOutputTests` 新增 5 项，统一信封实现前 `0 / 5` 通过；实现后 `5 / 5` 通过。
- 统一输出定向回归：版本、配置、Session、数据库、存储、在线控制与统一信封合计 `89 / 89` 通过。
- JSONL、stdout/stderr、ANSI 与原子输出：`Stage7UnifiedOutputTests`、`StandaloneRunInputOutputStage7Tests`、`StandaloneSessionExportStage7Tests` 合计 `16 / 16` 通过。
- EndToEnd 全量：`237 / 237` 通过。
- 全解决方案：共 `815` 项，`812` 项通过；`3` 个真实 Provider 用例仅因缺少外部凭据明确跳过。
- 严格构建：Debug/Release `--no-restore -warnaserror -m:1` 均为 `0` 警告、`0` 错误。
- Windows `win-x64` Release Native AOT：发布成功，`0` 条 trimming/AOT 警告；发布制品 `--help` 与 `version --json --no-color` 实测通过。
- 协议/Schema：协议 `1.1`、SQLite Schema `14`，本批未变更。
- CodeMap：baseline `369c49099fd03afd05db23e540096b3bf18f3924`，workspace `session` overlay revision `120`。
- 进度：执行计划 `22 / 54`；07 执行任务 `52 / 74`，测试要求 `1 / 14`，完成标准 `1 / 8`，总清单 `54 / 96`。
