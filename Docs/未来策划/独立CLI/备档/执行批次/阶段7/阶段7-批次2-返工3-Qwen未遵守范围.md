# 阶段 7 批次 2 返工 3：Qwen 未遵守范围

## 失败现象

第三个全新 Qwen Code 会话收到“禁止探索、禁止子代理、只改五个 Client 文件”的明确约束后，仍启动 Explore 子代理并读取 Server、Transport 等禁止范围，最终在第 21 次工具调用前未编辑文件并耗尽预算。

## 返工要求

1. 新会话只创建 `RuntimeHostCallbacks.cs` 一个文件，不读取仓库、不启动子代理。
2. 提示词提供完整目标代码结构，首个工具调用必须是 `write_file`。
3. 若最小写入仍失败，主代理接管余下实现，并在批次记录中保留三次失败证据。
