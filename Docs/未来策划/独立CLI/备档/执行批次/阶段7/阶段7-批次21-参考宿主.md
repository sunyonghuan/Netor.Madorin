# 阶段 7 批次 21：参考宿主 : 100%

## 目标与冻结边界

- [√] 保留现有 `multi-instance` 两 Runtime 隔离入口及测试行为，新增独立的阶段 7 参考宿主入口。
- [√] SampleHost 只引用 `Madorin.AI.Runtime.Client` 和 `Madorin.AI.Runtime.Contracts`，不引用 Provider、MAF、MEAI、SQLite 或 Server 实现项目。
- [√] 单个 SampleHost 通过三个独立 Client 绑定三个工作区和三个 Runtime，并并发演示 Expert、Meeting、Work 三模式。
- [√] 按 `runtimeInstanceId`、`runId`、GSN 和 Run 序号分流事件，处理成功后显式确认 GSN，并验证相同 `runId`/GSN 不跨实例串线。
- [√] 演示 Run 取消、工具目录发布、权限/审批/工具回调及 callId 结果查询。
- [√] 演示 Selection 更新、Session resume/rehydrate 和 Runtime 重启后的恢复。
- [√] 示例只使用测试生成 secret，并通过受控 stdin 传递，不在仓库、命令行、环境变量或 stdout 中暴露凭据。

## 测试与验证

- [√] 新增项目引用边界测试，检查 SampleHost 仅直接引用 Client/Contracts，并禁止 Provider、MAF、MEAI、SQLite、Server。
- [√] 新增真实三 Runtime/三 Client 端到端测试，使用确定性 Provider 覆盖三模式并发、事件分流与显式确认。
- [√] 覆盖取消、宿主工具/权限/审批回调、callId 查询、Selection、resume/rehydrate 和 Runtime 重启恢复。
- [√] 回归现有 `MultiInstanceProcessTests`，确认阶段 2 的两 Runtime 隔离入口未被破坏。
- [√] 定向测试、EndToEnd、全解决方案、严格 Debug/Release、Native AOT、CodeMap 和差异检查通过。

## 允许修改文件

- [√] `Docs/未来策划/独立CLI/备档/执行批次/阶段7/阶段7-批次21-参考宿主.md`
- [√] `Docs/未来策划/独立CLI/备档/执行步骤/07-CLI-ClientSDK与参考宿主.md`
- [√] `Docs/未来策划/独立CLI/执行步骤/README.md`
- [√] `Docs/未来策划/独立CLI/备档/执行计划/执行计划(阶段7-CLI-ClientSDK与参考宿主).md`
- [√] `Src/Madorin.Ai.Runtime/samples/Madorin.AI.Runtime.SampleHost/Program.cs`
- [√] `Src/Madorin.Ai.Runtime/samples/Madorin.AI.Runtime.SampleHost/ReferenceHost.cs`
- [√] `Src/Madorin.Ai.Runtime/samples/Madorin.AI.Runtime.SampleHost/ReferenceHostConfiguration.cs`
- [√] `Src/Madorin.Ai.Runtime/samples/Madorin.AI.Runtime.SampleHost/ReferenceHostCallbacks.cs`
- [√] `Src/Madorin.Ai.Runtime/samples/Madorin.AI.Runtime.SampleHost/README.md`
- [√] `Src/Madorin.Ai.Runtime/tests/Madorin.AI.Runtime.EndToEnd.Tests/ReferenceHostProcessTests.cs`

## 实施中发现的必要修复

- [√] `ExpertModeOrchestrator` 在宿主审批工具写入 Outbox 后重新同步下一 Run 序号，消除工具终态事件与 Run 状态事件复用序号的问题。
- [√] `RuntimeClient` 将高层控制请求的断管异常映射为可重试 `RuntimeClientConnectionException`；参考宿主显式重连后继续 resume/rehydrate。
- [√] 参考宿主初始并发 attach 遇到可重试初始化断连时进入 Client 重连窗口；完整流程稳定性复跑 10 / 10 通过。

## 验证记录

- `ReferenceHostProcessTests`：2 / 2 通过，覆盖依赖边界和三 Runtime 全链路；完整进程流程稳定性复跑 10 / 10 通过。
- `MultiInstanceProcessTests`：1 / 1 通过；`RuntimeClientLifecycleStage7Tests`：7 / 7 通过。
- 全解决方案：814 项通过，3 个真实 Provider 用例按凭据条件跳过，0 失败。
- 严格 Debug/Release：均为 0 警告、0 错误。
- CLI 与 SampleHost Windows `win-x64` Release Native AOT：发布成功，均无 trimming/AOT 警告；两个发布入口已实测。
- 协议 `1.1`、SQLite Schema `14`；CodeMap workspace `session` overlay revision `134`。
- 阶段 7 执行任务更新为 59 / 74，总清单更新为 63 / 96；执行计划更新为 28 / 54。
