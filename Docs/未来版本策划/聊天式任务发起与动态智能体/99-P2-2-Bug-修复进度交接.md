# P2-2 Bug 修复 + P2-3 提示词模板 + P2-4 审批与持久化 进度交接（2026-05-17 更新）

> 临时交接文档（前缀 99 = 待合并），用于跨对话续接 P2-2 Bug 修复 + P2-3 + P2-4 工作。
> 验证通过 + commit 后请删除本文件 / 合并到 03-实施阶段.md。

## 0. 新对话续接指引（最重要）

**第一句话告诉新对话**：
```
请读 Docs/未来版本策划/聊天式任务发起与动态智能体/99-P2-2-Bug-修复进度交接.md
继续 P2-4 实测 + commit 工作。
```

---

## 0.6 当前对话快照（2026-05-17 第三轮）

本轮在 P2-2 Bug 修复 + P2-3 提示词模板基础上，**完整实施 P2-4**（Plan 审批 + 保存常用 Agent），plan 文件在 `C:/Users/Administrator/.claude/plans/harmonic-hugging-fountain.md`。

### A. 后端审批闸（已完成）

- ✅ [Events.cs](../../../Src/Netor.Cortana.Entitys/Events.cs)：新增 `OnDynamicAgentCreationRequested` / `OnDynamicAgentCreationResolved` 两个事件键 + `DynamicAgentCreationRequestedArgs` / `DynamicAgentCreationResolvedArgs` 两个 record（继承 WorkflowEventArgs）+ 对应 `*Event` record
- ✅ [DynamicAgentCreationGate.cs](../../../Src/Netor.Cortana.AI/Workflow/DynamicAgents/DynamicAgentCreationGate.cs)：Singleton，`ConcurrentDictionary<RequestId,Tcs>` + `auto-approve 表` + `RequestId→TaskId 反向索引`；含 `IsTaskAutoApproved` / `WaitForDecisionAsync(taskId, requestId, ct)` / `ResolveDecision` / `EnableAutoApproveForTask` / `ClearTask`；`DynamicAgentCreationDecision` 枚举（Approved / ApprovedAll / Rejected）
- ✅ [CreateSubAgentTool.cs](../../../Src/Netor.Cortana.AI/Workflow/DynamicAgents/CreateSubAgentTool.cs)：`Create` 签名加 3 个新依赖（`gate / publisher / requireApproval`），在 4.5 步骤插入 HITL 审批：发布 Requested 事件 → await Gate → Rejected 返回友好字符串、ApprovedAll 启用 auto-approve、Cancelled 走任务取消路径
- ✅ [AIAgentFactory.cs](../../../Src/Netor.Cortana.AI/AIAgentFactory.cs)：`BuildSubAgent` isManager+taskId 块从 DI 取 Gate / IPublisher / SystemSettingsService；`requireApproval` 从 SystemSettings 读 `workflow.dynamicAgent.requireApproval`（默认 true）；透传给 `CreateSubAgentTool.Create`
- ✅ [WorkflowExecutor.cs](../../../Src/Netor.Cortana.AI/Workflow/WorkflowExecutor.cs)：构造函数加 `DynamicAgentCreationGate` 依赖；finally 块 `dynamicAgentRegistry.ClearTask` 之后追加 `dynamicAgentCreationGate.ClearTask`
- ✅ [AIServiceExtensions.cs](../../../Src/Netor.Cortana.AI/AIServiceExtensions.cs)：注册 `DynamicAgentCreationGate` 为 Singleton
- ✅ AgentService.GetByName 已存在（`AgentService.cs:53`），无需新加

### B. UI 审批卡片（已完成）

- ✅ [DynamicAgentCreationApprovalVm.cs](../../../Src/Netor.Cortana.UI/ViewModels/Workspace/DynamicAgentCreationApprovalVm.cs)（新增）：与 `WorkflowTaskApprovalVm` 同模式；字段 ProposedName / ProposedResponsibility / ProposedInstructions / ProposedRequiredToolsText / CountText / IsVisible 等；命令 `ApproveAsync` / `ApproveAllAsync` / `RejectAsync` 全部走 `_gate.ResolveDecision(_requestId, decision)`
- ✅ [TaskDetailVm.cs](../../../Src/Netor.Cortana.UI/ViewModels/Workspace/TaskDetailVm.cs)：加属性 `DynamicAgentCreationApproval`；构造时 `new DynamicAgentCreationApprovalVm()`；订阅 `OnDynamicAgentCreationRequested`（TaskId 匹配则 Load）+ `OnDynamicAgentCreationResolved`（RequestId 匹配则 Clear，避免幽灵卡片）
- ✅ [WorkflowDetailView.axaml](../../../Src/Netor.Cortana.UI/Views/Workspace/WorkflowDetailView.axaml)：在 HITL 审批卡片之后追加新 `Border.approval-card`，DataContext 绑 `Detail.DynamicAgentCreationApproval`，含名称/职责/工具/提示词预览（ScrollViewer MaxHeight=160）+ 三按钮 `✓ 批准创建` / `✓✓ 本任务全部批准` / `✕ 拒绝创建`
- ✅ [WorkflowDetailView.axaml.cs](../../../Src/Netor.Cortana.UI/Views/Workspace/WorkflowDetailView.axaml.cs)：加 3 个 click handler（`OnDynamicAgentApproveClick` / `OnDynamicAgentApproveAllClick` / `OnDynamicAgentRejectClick`）

### C. SaveAgentDialog（已完成）

- ✅ [SaveAgentItemVm.cs](../../../Src/Netor.Cortana.UI/ViewModels/Workspace/SaveAgentItemVm.cs)（新增）：单行 ViewModel，含 IsSelected / NewName / NameError / HasError + OriginalName/Responsibility/Instructions/RequiredTools/ToolsHint
- ✅ [SaveAgentDialog.axaml](../../../Src/Netor.Cortana.UI/Views/Workspace/SaveAgentDialog.axaml) + [.axaml.cs](../../../Src/Netor.Cortana.UI/Views/Workspace/SaveAgentDialog.axaml.cs)（新增）：Window 700x560；ItemsControl + DataTemplate（CheckBox + 改名 TextBox + 行内错误提示 + 工具 hint）；`OnSaveClick` 一次性预检（命名空校验 + 对话框内重复 + AgentService.GetByName 命中）→ 有错误就停留，全部通过才逐条 `agentService.Add` 后 Close
- ✅ [WorkflowDetailView.axaml.cs](../../../Src/Netor.Cortana.UI/Views/Workspace/WorkflowDetailView.axaml.cs) `OnWorkflowTaskCompleted` 订阅扩展：在订阅入口同步 snapshot `registry.GetByTask(taskId)`（防 finally ClearTask 先于 ShowDialog 执行）→ Dispatcher.UIThread.Post 内 `await new SaveAgentDialog(snapshot, taskId, providerId, modelId).ShowDialog(owner)`

### 验证

- ✅ Debug build：0 错误，2 个 baseline warnings（CS0414 `_isDragOver` × 2，pre-existing）
- ⏸ AOT publish 跳过（环境工具链问题未解决）
- ❌ 用户烟雾测试 9-13 全部待实测：
  - 场景 9：单次 ✓ 批准创建 → Manager 收到"已创建 / 可用 dynamic_agent_xxx"
  - 场景 10：✓✓ 本任务全部批准 → 后续 create_subagent 不再弹卡片
  - 场景 11：✕ 拒绝 → Manager 收到"用户拒绝创建 'name'..."消息后自行重新规划
  - 场景 12：保存对话框改名重复 → 行内红字"已存在同名智能体"，对话框不关闭
  - 场景 13：保存对话框改名唯一 + 勾选 → 智能体设置中出现新 Agent，对话框关闭

### 当前未 commit 改动（累计）

| 阶段 | 新增 | 修改 |
|---|---|---|
| P2-2 Bug 修复 | DynamicAgents/（4 文件）+ Providers/DynamicAgentToolsProvider.cs | 10 个 modified |
| P2-3 提示词模板 | Resources/Prompts/Magentic.DynamicCreation.md + Providers/MagenticDynamicCreationInstructionsProvider.cs | AIAgentFactory.cs + Netor.Cortana.AI.csproj |
| P2-4 审批+持久化 | Workflow/DynamicAgents/DynamicAgentCreationGate.cs + ViewModels/Workspace/DynamicAgentCreationApprovalVm.cs + ViewModels/Workspace/SaveAgentItemVm.cs + Views/Workspace/SaveAgentDialog.axaml + .axaml.cs | Events.cs + AIAgentFactory.cs + WorkflowExecutor.cs + AIServiceExtensions.cs + CreateSubAgentTool.cs + TaskDetailVm.cs + WorkflowDetailView.axaml + .axaml.cs |
| 体验扩展（P2-2 同期） | — | SharedStyles.axaml + WorkflowTaskListVm.cs + WorkspaceTabVm.cs |

**建议 commit 拆分**（待 AOT publish 环境恢复后）：
- commit A: `fix(workflow): P2-2 阻塞 Bug 修复合集 + 输入框/列表体验扩展`（13 文件）
- commit B: `feat(workflow): P2-3 Magentic Manager 动态创建提示词模板`（4 文件）
- commit C: `feat(workflow): P2-4 动态子智能体创建审批 + 保存常用 Agent`（13 文件）

### 待办

1. **AOT publish 环境恢复**：vswhere.exe / link.exe 工具链问题。建议从 VS Developer PowerShell / `Launch-VsDevShell.ps1` 启动后再 publish
2. **用户烟雾实测**：场景 9-13（动态创建审批 + SaveAgentDialog）
3. **5 个真实任务 prompt 工程迭代**（P2-3 §3.2.2，未做）
4. **三个 commit 拆分 + push 双远程**

### 关键设计速记（避免下次跑偏）

- Plan 审批走**并行新事件**而非复用 SDK plan signoff——SDK RequestInfoEvent 钩子拿不到 function-call 内的 await
- Decision 枚举仅 Approved / ApprovedAll / Rejected（**不支持 Revise**），Manager 拒绝后基于错误字符串自行重新规划
- 审批**不加 timeout**，靠任务级 ct 兜底；用户点"停止任务"传播 ct.Cancel → Gate 抛 OperationCanceledException → 工具内 catch 返回友好字符串
- SaveAgentDialog 的 records snapshot 必须在 OnWorkflowTaskCompleted 订阅入口同步拷贝（finally ClearTask 在事件发布后立即执行）
- AgentEntity.Name 无 UNIQUE 约束，重名靠 SaveAgentDialog 预检 + UI 红字提示，不依赖数据库异常
- AOT 兼容：嵌入资源走元数据，trim/AOT 不影响 GetManifestResourceStream

---

---

## 0.5 当前对话快照（2026-05-17 第二轮）

本轮在已完成的 P2-2 Bug 修复 6 个 Step（Step 1/2/3/E/F + 3 个体验扩展文件）基础上，**跳过本轮 commit，直接推进 P2-3**：

- ✅ 创建 [Src/Netor.Cortana.AI/Resources/Prompts/Magentic.DynamicCreation.md](../../../Src/Netor.Cortana.AI/Resources/Prompts/Magentic.DynamicCreation.md)（教 Manager 使用 create_subagent / dynamic_agent_xxx 的指令模板，含 7 节 + 3 个示例 + `{{MaxSubAgents}}` 占位）
- ✅ 创建 [Src/Netor.Cortana.AI/Providers/MagenticDynamicCreationInstructionsProvider.cs](../../../Src/Netor.Cortana.AI/Providers/MagenticDynamicCreationInstructionsProvider.cs)（AIContextProvider 路径，Lazy 缓存模板，每次 invocation 替换 `{{MaxSubAgents}}` 后注入 `AIContext.Instructions`）
- ✅ 修改 [Src/Netor.Cortana.AI/AIAgentFactory.cs](../../../Src/Netor.Cortana.AI/AIAgentFactory.cs) `BuildSubAgent` 的 `isManager && !string.IsNullOrEmpty(taskId)` 块，在 DynamicAgentToolsProvider + create_subagent 工具之后追加注入新 Provider
- ✅ 修改 [Src/Netor.Cortana.AI/Netor.Cortana.AI.csproj](../../../Src/Netor.Cortana.AI/Netor.Cortana.AI.csproj) 加 EmbeddedResource（显式 `LogicalName=Netor.Cortana.AI.Resources.Prompts.Magentic.DynamicCreation.md`）
- ✅ Debug build：0 错误，3 个 baseline warnings（CS8604 + 两个 CS0414 `_isDragOver`，均为 pre-existing）
- ✅ 嵌入资源运行时验证：`[Reflection.Assembly]::LoadFrom('Netor.Cortana.AI.dll').GetManifestResourceNames()` 返回 `["Netor.Cortana.AI.Resources.Prompts.Magentic.DynamicCreation.md"]` 与 Provider 的 ResourceName 严格匹配；嵌入资源走元数据，trim/AOT 不影响加载
- ⏸ AOT publish 未做：当前环境 ILC 阶段失败（vswhere.exe / link.exe 工具链问题，非代码问题），用户决策跳过本轮验证

### 待办

1. **未做 prompt 工程迭代**（03-实施阶段.md §3.2.2 任务）。第一版模板是基于设计意图与示例 SDK 行为编写的"完整可用第一版"，但 5 个真实任务（代码审查 / 研究报告 / 市场分析 / 文档生成 / 测试用例）的实际跑测没做——这一步必须用户实测后才知道 Manager 会不会"过度创建 / 漏建 / 命名乱"。
2. **未 commit**。当前工作区累计 13 个 modified + 2 个 untracked 目录（DynamicAgents/ + Providers/DynamicAgentToolsProvider.cs + Providers/MagenticDynamicCreationInstructionsProvider.cs + Resources/Prompts/Magentic.DynamicCreation.md），覆盖 P2-2 Bug 修复合集 + P2-3 全部代码改动 + 3 个体验扩展。建议按"P2-2 Bug 修复合集（10 文件）" + "P2-3 提示词模板（4 文件）"两个独立 commit 拆分，但需先解决 AOT publish 环境问题。
3. **3 个体验扩展归属**（已在原 §2 之外，本轮未列入表格）：
   - [Src/Netor.Cortana.UI/Styles/SharedStyles.axaml](../../../Src/Netor.Cortana.UI/Styles/SharedStyles.axaml) +34：把 `titlebar-btn` 系列样式从 MainWindow 内联**提取到全局**（解决发送按钮 hover Bug，原 §6 P2 第 4 项的根因修复 —— SettingsWindow / WorkspaceExplorer / ChatHistoryPanel 等 UserControl 不再依赖 Window 级样式）
   - [Src/Netor.Cortana.UI/ViewModels/Workspace/WorkflowTaskListVm.cs](../../../Src/Netor.Cortana.UI/ViewModels/Workspace/WorkflowTaskListVm.cs) +40：加 `SelectTaskById(string)` + `_pendingSelectedTaskId` 缓冲（任务尚未插入列表时暂存选中请求）
   - [Src/Netor.Cortana.UI/ViewModels/Workspace/WorkspaceTabVm.cs](../../../Src/Netor.Cortana.UI/ViewModels/Workspace/WorkspaceTabVm.cs) +11：加 `ShowTaskAsync(taskId)`：发起任务后立即切到该任务详情；已被 WorkflowDetailView.axaml.cs:246 + GroupChatDetailView.axaml.cs:238 调用

---

## 1. 项目背景

- **项目**：Netor.Madorin（Avalonia + .NET 10 + AOT publish 桌面 App）
- **路径**：`E:\Netor.me\Madorin`（git 仓库根）
- **当前阶段**：P2-2 聊天式任务发起 + 动态子智能体（详见 `Docs/未来版本策划/聊天式任务发起与动态智能体/01-P2方案设计.md`）
- **当前任务**：修复一系列阻塞 Bug（用户实测过程中暴露的）

---

## 2. 已完成的代码改动（全部未 commit）

| Step | 文件 | 改动内容 | 状态 |
|---|---|---|---|
| **Step 1**：MagenticWorkflowFactory Manager self-talk | `Src/Netor.Cortana.AI/Workflow/Builders/MagenticWorkflowFactory.cs` | 校验从 `< 2` 改 `< 1`；participants.Count == 1 时让 Manager 同时充当 team[0]（self-talk fallback）；log 加 isSelfTalk 标记 | ✅ 代码完成、Debug+AOT 通过 |
| **Step 2**：WorkflowInputVm 错误提示导向化 | `Src/Netor.Cortana.UI/ViewModels/Workspace/WorkflowInputVm.cs` (line 448-454) | "请选择主智能体" → "未找到可用的智能体，请先到设置中创建智能体。"（Provider/Model 同理） | ✅ 代码完成 |
| **Step 3a**：WorkflowTaskRequest 加 override 字段 | `Src/Netor.Cortana.AI/Workflow/WorkflowTaskRequest.cs` (line 51-65) | 加 `OverrideProviderId` + `OverrideModelId` 两个 init 属性 | ✅ 代码完成 |
| **Step 3b**：WorkflowInputVm 传递 override | `Src/Netor.Cortana.UI/ViewModels/Workspace/WorkflowInputVm.cs` (line 486-487 附近) | 构造 WorkflowTaskRequest 时显式传 `OverrideProviderId = _selectedProvider!.Id` + `OverrideModelId = _selectedModel!.Id` | ✅ 代码完成 |
| **Step 3c**：AIAgentFactory.BuildWorkflowParticipants 支持 override | `Src/Netor.Cortana.AI/AIAgentFactory.cs` (line 478-540 附近) | 加 `overrideProviderId/overrideModelId` 两个可选参数；Manager（participants[0]）优先用 override，fallback 才走 Agent.DefaultXxxId | ✅ 代码完成 |
| **Step 3d**：LoadAndBuildParticipants 透传 override | `Src/Netor.Cortana.AI/Workflow/WorkflowExecutor.cs` (line 795-798 附近) | 把 `request.OverrideProviderId / request.OverrideModelId` 传给 `factory.BuildWorkflowParticipants` | ✅ 代码完成 |
| **Step E**：finalReport 提取优先级修复 | `Src/Netor.Cortana.AI/Workflow/WorkflowExecutor.cs` (line 677-690 附近) | (a) `AgentResponseEvent` 加 `when !string.IsNullOrWhiteSpace(...)` 避免空文本覆盖；(b) `WorkflowOutputEvent` 去掉 `when finalReport is null` 守卫，让 SDK 真正的 FinalAnswer 能覆盖；(c) 提取后用非空判定决定是否覆盖 | ✅ 代码完成 |
| **Step F**：AOT 发布模式输入框显式同步 | `Src/Netor.Cortana.UI/Views/Workspace/WorkflowDetailView.axaml.cs` + `GroupChatDetailView.axaml.cs` | `TextChanged` / Enter / 点击发送前显式把 `InputBox.Text` 同步到 `_inputVm.InitialInput`，避免 NativeAOT/CompiledBinding 下 `StartAsync` 读到空任务描述 | ✅ 代码完成，Debug 通过，AOT publish 当前环境失败（见 §5） |
| **Step G**：发送按钮 hover 状态修复 | `Src/Netor.Cortana.UI/Styles/SharedStyles.axaml` | 把 `titlebar-btn` / `titlebar-btn-warn` 的基础、hover、pressed、disabled 样式迁移到全局共享样式，解决 Workspace UserControl 里的发送/停止按钮吃不到 MainWindow 局部样式 | ✅ 代码完成，Debug 通过 |
| **Step H**：发送后自动展示当前任务详情 | `Src/Netor.Cortana.UI/ViewModels/Workspace/WorkspaceTabVm.cs` + `WorkflowTaskListVm.cs` + 两个 DetailView | `StartAsync` 成功返回 taskId 后调用 `ShowTaskAsync(taskId)`；列表支持按 taskId 选中，若 task.started 事件尚未插入列表则暂存选中请求；详情区立即加载当前任务状态与后续步骤 | ✅ 代码完成，Debug 通过 |

---

## 3. 工作区改动统计（git diff --stat HEAD）

```
 Src/Netor.Cortana.AI/AIAgentFactory.cs             | 250 ++++++++++++++++++++-
 Src/Netor.Cortana.AI/AIServiceExtensions.cs        |   4 +
 .../Workflow/Builders/MagenticWorkflowFactory.cs   |  24 +-
 Src/Netor.Cortana.AI/Workflow/WorkflowExecutor.cs  | 193 +++++++++++-----
 .../Workflow/WorkflowTaskRequest.cs                |  14 ++
 Src/Netor.Cortana.UI/Styles/SharedStyles.axaml     |  34 +++
 .../ViewModels/Workspace/WorkflowInputVm.cs        |  13 +-
 .../ViewModels/Workspace/WorkflowTaskListVm.cs     |  40 +++-
 .../ViewModels/Workspace/WorkspaceTabVm.cs         |  11 +
 .../Views/Workspace/GroupChatDetailView.axaml      |  24 +-
 .../Views/Workspace/GroupChatDetailView.axaml.cs   | 179 ++++++++++++++-
 .../Views/Workspace/WorkflowDetailView.axaml       |  24 +-
 .../Views/Workspace/WorkflowDetailView.axaml.cs    | 185 ++++++++++++++-
 13 files changed, 899 insertions(+), 96 deletions(-)
```

**新增文件（未追踪）**：
- `Src/Netor.Cortana.AI/Providers/DynamicAgentToolsProvider.cs`
- `Src/Netor.Cortana.AI/Workflow/DynamicAgents/`（整个目录）
- `.claude/`（本地配置，已在 .gitignore 中）

**注意**：这些"新增"文件其实是早先 P2-2 工作的一部分（DynamicAgents 整套），不是本轮 Bug 修复加的，但因为之前没 commit 所以一起出现在工作区。

---

## 4. 用户已确认的决策（避免新对话重复问）

| 决策点 | 用户选择 | 实施位置 |
|---|---|---|
| 工作流模式 Member 的概念 | **不存在 Member**（设计意图：Manager 通过 create_subagent 自主创建动态子智能体） | MagenticWorkflowFactory.Build + AIAgentFactory.BuildWorkflowParticipants |
| Manager 来源 | **输入框下方 AgentSelector 当前选中值**（不是 IsDefault） | WorkflowInputVm.StartAsync（已对接） |
| SDK 至少 2 个 participant 约束的解决 | **甲方案：Manager self-talk** — Manager 同时充当 team[0]，UI 无感知 | MagenticWorkflowFactory.Build |
| Override Provider/Model 作用范围 | **甲方案：仅 Manager + 动态子智能体**（GroupChat Members 仍用各自 Agent.DefaultProviderId） | AIAgentFactory.BuildWorkflowParticipants foreach 中按 `isManager` 判断 |
| 错误提示文案风格 | **导向"请先到设置创建"**，不怪用户没勾选 | WorkflowInputVm.StartAsync ValidationError 文案 |

---

## 5. 关键测试结果

| 测试 | 结果 | 备注 |
|---|---|---|
| Debug build (P2-2-Bug-E 完成后) | ✅ 0 错误，3 个 baseline warnings | warnings 是 P2-1 遗留的 `_isDragOver` CS0414 + AiChatHostedService.cs:476 CS8604，与本轮无关 |
| AOT publish (P2-2-Bug-E 完成后) | ✅ 成功，产物 73M（与 baseline 持平） | 仅 baseline IL3050 OllamaDelegatingChatClient trim warning |
| Debug build (Step F 完成后) | ✅ 0 错误，2 个 baseline warnings | `GroupChatDetailView._isDragOver` + `WorkflowDetailView._isDragOver` CS0414 |
| AOT publish (Step F 完成后) | ⚠️ 当前环境失败 | `dotnet publish Src/Netor.Cortana.UI/Netor.Cortana.UI.csproj -c Release --nologo --verbosity minimal` 在 NativeAOT 阶段报 `Cross-OS native compilation is not supported.`；Release 编译到 `Madorin.dll` 已完成，失败发生在 ILC native publish 阶段 |
| Debug build (Step G/H 完成后) | ✅ 0 错误，2 个 baseline warnings | `dotnet build Src/Netor.Cortana.UI/Netor.Cortana.UI.csproj -c Debug --nologo --verbosity minimal`；warnings 仍为两个 `_isDragOver` CS0414 |
| 用户实测 Override Provider/Model 修复 | ✅ 任务能启动、不报"Provider 失效"错 | 用户日志显示 deepseek-v4-pro 正常用了 |
| 用户实测 finalReport 修复 | ❌ **未验证**（用户已退出 Madorin，要求落盘开新对话） | **新对话续接的核心待办** |
| 用户实测 Step F 输入框同步修复 | ❌ **未验证** | 需在可正常 AOT publish 的 Windows 本机环境重新发布后验证：工作流/群聊输入文字后点击发送不再报"请填写任务描述" |
| 用户实测 Step G/H UI 修复 | ❌ **未验证** | 需在 AOT publish 产物中验证：发送按钮 hover 有背景变化；点击发送后主详情区立即显示当前任务运行状态 |

---

## 6. 待办事项（按优先级）

### 🔴 P0 - 阻塞续接（必须先做）

1. **用户验证 finalReport 修复** — 启动 AOT publish 让用户实测 Magentic 任务能否正确显示最终回复
   - 启动命令：`cd "E:/Netor.me/Madorin/Src/Netor.Cortana.UI/bin/Release/net10.0/win-x64/publish" && ./Cortana.exe`
   - 验证场景：进入工作流 tab → Magentic 模式 → 让 Manager 创建动态子智能体并执行 → 看任务详情页**最终回复栏**是否有内容（不再是 `[magentic 已完成，但未产出最终回复]`）

2. **用户验证 Step F 输入框同步修复** — 重新 AOT publish 后验证工作流和群聊发送
   - 验证场景 1：工作流 tab → 输入任意任务描述 → 点击发送 → 日志里的 `Input.Len` 应大于 0，不再出现 `请填写任务描述`
   - 验证场景 2：群聊 tab → 选择至少 2 个智能体 → 输入讨论话题 → 点击发送 → 日志里的 `Input.Len` 应大于 0，不再出现 `请填写讨论话题`
   - 根因判断：主聊天直接读 `InputBox.Text`，工作流/群聊原先只依赖 `Text="{Binding InitialInput}"`；NativeAOT 发布模式下绑定写回不稳定时 `StartAsync` 会读到空字符串。

3. **用户验证 Step G/H UI 修复**
   - 验证场景 1：工作流/群聊输入框右侧发送按钮鼠标经过时出现 `#252526` hover 背景，停止按钮 hover 为红色警示背景。
   - 验证场景 2：工作流 tab 输入任务并发送后，主详情区立即切到新任务，显示运行状态和后续步骤，不只是左侧记录列表新增。
   - 验证场景 3：群聊 tab 输入话题并发送后，同样立即切到新群聊任务详情。

### 🟡 P1 - 验证通过后执行

2. **本地 commit + 双远程 push**（用户偏好：每个 Bug fix 都是单独 commit；本轮多个 Step 可合并为一个大 commit）
   - 推荐 commit msg 结构（多个 Bug 一次性修复）：
     ```
     fix(workflow): P2-2 阻塞 Bug 修复合集 (4 个改动)
     
     Step 1: MagenticWorkflowFactory Manager self-talk
       放宽 ≥2 校验为 ≥1；participants.Count == 1 时 Manager 同时充当 team[0]，
       绕过 SDK MagenticOrchestrator.cs:265 team.First() 约束。
       根因：P2-2 设计意图是 Manager 通过 create_subagent 自主创建子智能体，
       不预设 Member，但 SDK 运行时仍需 team ≥ 1。
     
     Step 2: WorkflowInputVm 错误提示导向化
       "请选择主智能体" → "未找到可用的智能体，请先到设置中创建智能体。"
       用户决策：选择器初始化时会自动填默认值，错误指向系统数据不完整。
     
     Step 3: OverrideProviderId/ModelId 完整链路
       WorkflowTaskRequest 加 2 个 override 字段，
       UI → ViewModel → WorkflowExecutor → AIAgentFactory 透传到 Manager + 动态子智能体，
       覆盖 Manager Agent 数据库里的 DefaultProviderId（可能已失效）。
       决策 D-甲：仅作用于 Manager，不影响 GroupChat Members。
     
     Step E: finalReport 提取优先级修复
       (a) AgentResponseEvent 加非空过滤，避免空文本覆盖；
       (b) WorkflowOutputEvent 去掉 finalReport is null 守卫，让 SDK 真正的 FinalAnswer
           （MagenticOrchestrator.PrepareFinalAnswerAsync 通过 YieldOutputAsync 输出）覆盖。
       根因：SDK Magentic FinalAnswer 走 WorkflowOutputEvent，不是 AgentResponseEvent。
     
     Debug build: 0 错误
     AOT publish: 73M（baseline 持平）
     用户实测: Step 1+2+3 ✅；Step E 待验证
     
     详见 Docs/未来版本策划/聊天式任务发起与动态智能体/99-P2-2-Bug-修复进度交接.md
     ```
   - push 双远程：`git push github master && git push netor master`

### 🟢 P2 - 用户提出但未实施（新对话可推进）

3. **群聊模式 UI 重构**（用户原话："group 下的 manager 也应该是输入框下面选择的智能体"）
   - 需要的改动：
     - `GroupChatDetailView.axaml` 底部加 3 个选择器（Manager/Provider/Model，复刻 WorkflowDetailView 结构）
     - `GroupChatInputVm.cs` 加 SelectedManager/SelectedProvider/SelectedModel 字段 + 持久化
     - `GroupChatInputVm.StartAsync` ManagerAgentId 用新字段；MemberAgentIds = SelectedAgents 但过滤掉 ManagerAgentId（去重）
     - `GroupChatDetailView.axaml.cs` 加 Fill / OnXxxClick handlers
   - 工作量估算：~1.5h

4. ~~发送按钮 hover 状态丢失~~ ✅ 已作为 Step G 修复

### 🔵 P3 - 长期改进（可不做）

5. P1 baseline warnings 清理：`_isDragOver` 字段未使用（WorkflowDetailView.axaml.cs:82 + GroupChatDetailView.axaml.cs:75）

---

## 7. 用户工作偏好（重要）

| 偏好 | 说明 |
|---|---|
| **每个 Bug fix 单独 commit** | 但本轮 P2-2-Bug 系列是同一个用户故事的多个 Step，可合并为 1 个 commit |
| **commit msg 风格** | 中文描述 + 详细说明根因和方案，附 Debug build / AOT publish 验证结果 |
| **double remote push** | `git push github master && git push netor master` |
| **AOT publish 而不是 dotnet run** | 用户实测都是用 publish 产物（`Src/Netor.Cortana.UI/bin/Release/net10.0/win-x64/publish/Cortana.exe`） |
| **Cortana.exe 后台启动** | 用 `run_in_background: true` + `tail` 日志确认服务启动完成 |
| **必须先 Read 才能 Edit** | Edit 工具强制要求；可用 `cat` 看内容但不能代替 Read |
| **Debug build 验证 0 错误** | 用 `dotnet build Src/Netor.Cortana.UI/Netor.Cortana.UI.csproj -c Debug --nologo --verbosity minimal` |

---

## 8. 关键技术参考

### Magentic 模式 SDK 调用链（FinalAnswer 走 WorkflowOutputEvent）

```
WorkflowExecutor.RunWorkflowAsync
  ↓
SdkWorkflow workflow = MagenticWorkflowFactory.Build(participants, ...)
  ↓
InProcessExecution.RunStreamingAsync(workflow, input, checkpointManager, sessionId: taskId, ct)
  ↓
await foreach (var evt in run.WatchStreamAsync(ct))
  ├─ ExecutorCompletedEvent → PersistAndPublishStep (写库 + 发 step.completed)
  ├─ AgentResponseEvent → 候选 finalReport（仅文本非空时）
  ├─ WorkflowOutputEvent → ✅ SDK 真正的 FinalAnswer（最高优先级覆盖）
  └─ RequestInfoEvent → HITL HandleHitlRequestAsync
```

### SDK 源码关键位置

- `e:\OpenSourse\agent-framework-main.v1.5.0\dotnet\src\Microsoft.Agents.AI.Workflows\MagenticWorkflowBuilder.cs` （team 可空，但 line 117-124 foreach 跳过）
- `e:\OpenSourse\agent-framework-main.v1.5.0\dotnet\src\Microsoft.Agents.AI.Workflows\Specialized\Magentic\MagenticOrchestrator.cs:265` （`team.First()` 强制约束，所以 self-talk 必须传 [manager]）
- `MagenticOrchestrator.cs:297-300` PrepareFinalAnswerAsync → YieldOutputAsync(messages) → WorkflowOutputEvent
- `MagenticManager.cs:108-114` PrepareFinalAnswerAsync 返回 `ChatMessage(ChatRole.Assistant, finalAnswer.Text)`

### 我们的代码关键位置

- `Src/Netor.Cortana.AI/Workflow/Builders/MagenticWorkflowFactory.cs` — Manager self-talk 逻辑
- `Src/Netor.Cortana.AI/Workflow/WorkflowExecutor.cs:665-693` — 事件循环（finalReport 提取）
- `Src/Netor.Cortana.AI/Workflow/WorkflowExecutor.cs:806` — ExtractFinalReportFromOutput（支持 AgentResponse / List<ChatMessage> / ChatMessage 三种）
- `Src/Netor.Cortana.AI/AIAgentFactory.cs:478-540` — BuildWorkflowParticipants override 逻辑
- `Src/Netor.Cortana.AI/AIAgentFactory.cs:623-639` — ResolveSubAgentProviderAndModel（保留旧 fallback 逻辑）

---

## 9. 新对话开场建议

新对话开启时，推荐的对话开场：

> 请读 `Docs/未来版本策划/聊天式任务发起与动态智能体/99-P2-2-Bug-修复进度交接.md`，
> 然后启动 AOT publish 让我实测 finalReport 修复（验证 Magentic 任务能正确显示最终回复）。

或者跳过实测直接 commit（如果用户已自行验证）：

> 请读 `Docs/未来版本策划/聊天式任务发起与动态智能体/99-P2-2-Bug-修复进度交接.md`，
> 我已自行验证 finalReport 修复，请按文档 §6.P1 的建议 commit + double remote push。

---

## 10. 风险提示

- ⚠️ **工作区有 10 个 modified + 2 个 untracked**，但都属于同一个用户故事（P2-2 Bug 修复 + 早先 P2-2 主功能）。**新对话不要意外回退这些改动**。
- ⚠️ **DynamicAgentToolsProvider.cs 和 DynamicAgents/ 目录是早先 P2-2 主功能的产物**（未追踪），不要以为是本轮 Bug 修复加的。如果不熟悉，**先在新对话中 read 这些文件了解架构**。
- ⚠️ **`.claude/` 目录**已被 git 视为未追踪，但是 Coding CLI 自己的工作目录（包含 plans / scheduled_tasks 等），**不要主动 commit / 删除**。
- ⚠️ 用户偏好 commit 前必须做 **Debug build (0 错误) + AOT publish (产物大小验证)** 双验证，**不能跳过**。
