# 会议模式阶段 8 附件路径规约收敛 : 100%

## Step 1 阶段定义与对齐复核 : 100%
- [√] 建立阶段 8 执行计划文件
- [√] 对照 README v8 / 03-数据模型与持久化复核附件路径硬约束
- [√] 确认当前缺口：`MeetingAttachmentEntity.StoredPath` 应存相对路径，但 UI 创建会议时直接写入附件绝对路径

## Step 2 附件导入路径修正 : 100%
- [√] `MeetingInputVm` 创建会议附件时复制到 `WorkspaceResourcesDirectory/meetings/{meetingId}/attachments/`
- [√] `MeetingAttachments.StoredPath` 仅保存相对资源目录路径
- [√] 保留原始文件名、MIME 与大小信息

## Step 3 自动化验收补强 : 100%
- [√] 增补 `MeetingAgentBuilder.BuildOpeningMessage` 附件绝对路径暴露测试
- [√] 增补附件超过 10 个时截断并提示 `list_meeting_attachments` 的测试

## Step 4 验证与收尾 : 100%
- [√] 执行会议模式独立测试工程
- [√] 执行解决方案普通编译
- [√] 更新阶段 8 修改文件与验证结果

## 当前对齐结论
- 数据层实体注释与文档一致：`StoredPath` 是相对 workspace resources 目录。
- AI 层 `MeetingAgentBuilder.BuildOpeningMessage` 与 `MeetingControlTools.list_meeting_attachments` 已按 `Path.Combine(WorkspaceResourcesDirectory, StoredPath)` 对 LLM 暴露绝对路径。
- UI 层会议创建当前仍把 `AttachmentInfo.Path` 直接写入 `StoredPath`，需要本阶段修正。

## 修改文件
- Docs/已完成功能规划/会议模式方案策划/执行计划(会议模式阶段8).md
- Src/Netor.Cortana.AI/AIServiceExtensions.cs
- Src/Netor.Cortana.AI/MeetingMode/MeetingAttachmentImporter.cs
- Src/Netor.Cortana.AI/MeetingMode/MeetingAgentBuilder.cs
- Src/Netor.Cortana.UI/ViewModels/Meeting/MeetingInputVm.cs
- Tests/Netor.Cortana.MeetingMode.Tests/MeetingAttachmentPathTests.cs

## 验证
- `dotnet test Tests/Netor.Cortana.MeetingMode.Tests/Netor.Cortana.MeetingMode.Tests.csproj --no-restore`：通过，10/10。
- `dotnet build Netor.Cortana.slnx --no-restore`：通过，0 警告，0 错误。
