# 会议模式阶段 17 参会者上下文与工具边界契约 : 100%

## Step 1 对齐审计 : 100%
- [√] 复核 README 中参会者 `MeetingContextHeader` 与无 `ask_user` 工具约束
- [√] 复核 02 / 05 中 `meeting.participant_header`、原 Agent 指令拼接和参会者工具边界要求
- [√] 复核现有会议模式测试中参会者上下文头覆盖缺口

## Step 2 可测入口整理 : 100%
- [√] 设计不依赖真实模型网络的参会者构建契约测试面
- [√] 暴露参会者提示词拼接的内部可测方法
- [√] 暴露参会者会议控制工具白名单的内部可测方法

## Step 3 契约测试 : 100%
- [√] 补充参会者提示词必须以会议模式 header 开头的测试
- [√] 补充参会者提示词必须保留原 Agent 指令的测试
- [√] 补充参会者工具白名单只包含 `list_meeting_attachments` 且不包含 `ask_user` 的测试

## Step 4 验证与收尾 : 100%
- [√] 执行会议模式独立测试工程
- [√] 执行解决方案普通编译
- [√] 更新阶段 17 修改文件与验证结果

## 当前对齐结论
- README 硬约束要求参会者 `SystemPrompt` 前注入 `MeetingContextHeader`，覆盖普通 AgentEntity 里“用户提问”语境。
- README / 02 / 05 均要求参会者没有 `ask_user` 工具；需要老板决策时由参会者发言说明，主持人代为询问。
- `MeetingAgentBuilder.BuildParticipantAgentAsync` 当前实现已读取 `meeting.participant_header` 并拼接原 Agent 指令，只给参会者追加 `list_meeting_attachments`，但现有测试尚未直接锁定该契约。

## 阶段内测试进展
- `dotnet test Tests/Netor.Cortana.MeetingMode.Tests/Netor.Cortana.MeetingMode.Tests.csproj --no-restore`：通过，37/37。
- `dotnet build Netor.Cortana.slnx --no-restore`：通过，0 错误，59 个既有警告。

## 修改文件
- `Docs/已完成功能规划/会议模式方案策划/执行计划(会议模式阶段17).md`
- `Src/Netor.Cortana.AI/MeetingMode/MeetingAgentBuilder.cs`
- `Src/Netor.Cortana.AI/Properties/AssemblyInfo.cs`
- `Tests/Netor.Cortana.MeetingMode.Tests/MeetingParticipantAgentContractTests.cs`
