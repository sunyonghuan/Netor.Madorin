# 09. 最终收口审计

## 目标

在全部功能模块完成后，对硬性约束做一次代码级审计，避免实现层仍残留旧依赖或 Bootstrap 表格、表单样式。

## 审计范围

1. `Netor.Cortana.Platform.Admin.csproj` 不再引用 `Netor.Operates.LayuiHelpers`。
2. Admin 视图中不再出现 Bootstrap 表单、表格、弹窗和 grid 关键类。
3. 静态详情表使用 `layui-table` 与项目内 `admin-table`、`admin-empty-cell` 等样式。
4. 危险操作继续使用 LayUI `layer.confirm` 或项目公共确认提交能力。
5. 构建使用临时输出目录验证，避免运行中站点锁定 `bin` 文件。

## 本轮处理

1. 已移除 Admin 项目的 `Netor.Operates.LayuiHelpers` 包引用。
2. 已清理 `Orders/Details.cshtml` 中残留的 Bootstrap 表格与 utility class。
3. 已复查 Admin 视图目录，未发现 `form-control`、`form-select`、`table-responsive`、`class="table`、`row g-`、`col-md`、`btn-group`、`input-group`、`modal-`、`data-bs-`、`bootstrap` 等残留。
4. 已补充 HTTP session 验证：`/Auth/Login` 登录成功，`/` 与 `/Orders/TableData?page=1&limit=1` 返回 200。
5. 当前数据库订单数为 0，订单详情抽屉未能通过真实订单 ID 做 HTTP 页面验证；已通过代码审计确认详情页使用 `mode=drawer`、底部交易流水抽屉和 LayUI 表格结构。
6. Browser MCP 本轮重试仍返回 `Transport closed`，未完成截图检查。
7. 已通过构建验证：

```powershell
dotnet build Src\Netor.Cortana.Platform\Netor.Cortana.Platform.Admin\Netor.Cortana.Platform.Admin.csproj -o .artifacts\admin-build-check
```

结果：0 警告，0 错误。
