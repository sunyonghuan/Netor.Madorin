# 执行进度

> 更新时间：2026-06-05

## 当前结论

前一版方案缺少外壳、菜单、业务入口和视觉验收的完整约束。本轮已决定新建重写版方案目录，后续按本执行计划推进，不再只围绕表格和抽屉做局部修补。

## 已完成

### Step 1：重新分析与方案重写

- 已复查 `Netor.Cortana.Platform.Admin` 控制器、视图、实体和当前后台外壳。
- 已确认前一版方案缺口：未把菜单功能面、工作台快捷入口、真实业务路由、状态数量和视觉验收纳入一等目标。
- 已新增拆分版方案文档：
  - `00-方案总览.md`
  - `01-当前项目能力映射.md`
  - `02-信息架构与菜单方案.md`
  - `03-交互与页面规范.md`
  - `04-模块执行矩阵.md`
  - `05-视觉规范与验收标准.md`
- 已新增执行计划和本执行进度文档。

### Step 2：后台外壳与菜单状态化

- 已新增 `AdminShellMetricsService`，用于查询资源待审核、创作者待审核、待结算、待支付订单、有效订阅、推荐资源、已发布资源等外壳指标。
- 已在 `Program.cs` 注册 `AdminShellMetricsService`。
- `_NavPartial.cshtml` 已接入真实数量徽标。
- `_WorkspacePartial.cshtml` 已接入真实数量和待结算金额。
- `site.css` 已新增菜单数量徽标、工作台数量徽标和金额提示样式。

### Step 3：业务场景快捷入口分层

- `_WorkspacePartial.cshtml` 已按当前控制器分层输出快捷入口。
- 资源、审核、分类页面优先展示待审核、已发布、新增资源、新增分类。
- 订单、订阅页面优先展示待支付、支付流水、有效订阅、下载记录。
- 创作者、结算页面优先展示创作者待审、待结算和待结算金额。
- 用户页面优先展示有余额用户、创作者账户、交易流水。
- 设置页面优先展示存储配置、风控配置、新增设置。

### Step 4：验证

- 已通过构建验证：`dotnet build Src\Netor.Cortana.Platform\Netor.Cortana.Platform.Admin\Netor.Cortana.Platform.Admin.csproj -o .artifacts\admin-build-check`，结果 0 警告、0 错误。
- 已通过 HTTP session 验证：
  - `/` 包含状态徽标。
  - `/Assets` 包含资源区快捷入口。
  - `/Orders` 包含交易区快捷入口。
  - `/Settlements` 包含待结算金额。
  - `/Settings` 包含设置区快捷入口。
  - `/Assets/Create?mode=drawer` 返回抽屉页面。
  - `/Orders/TableData?payStatus=1&page=1&limit=1` 返回 200 且 LayUI `code=0`。
- 此前 Browser MCP 返回 `Transport closed`，截图验证曾阻塞；本轮 MCP 已恢复并完成补充验证。

### Step 5：提交

- 本轮方案与执行代码纳入提交。

### Step 6：菜单场景高亮与响应式复核

- 已调整 `_NavPartial.cshtml` 的菜单展开规则。
- 已根据 URL query 区分待办场景和业务管理场景：
  - `/Assets?reviewStatus=1` 展开待办队列。
  - `/Assets` 展开资源市场。
  - `/Creators?status=1` 展开待办队列。
  - `/Creators` 展开创作者经营。
  - `/Orders?payStatus=1` 展开订单与权益。
  - `/Settings?keyPrefix=Risk` 展开系统运维。
- 已恢复创作者经营里的“有待审资源”和收益结算里的“有备注结算”业务入口，待审核创作者和待结算收益继续归属待办队列。
- 已补强 `site.css` 中工作台按钮的最小宽度、换行、窄屏分列和金额提示布局。
- 已通过构建验证，结果 0 警告、0 错误。
- 已通过 HTTP session 验证上述 6 个场景页面均返回 200，且 `layui-nav-itemed` 展开数量均为 1。
- 已验证 `/Assets/TableData?reviewStatus=1&page=1&limit=1` 返回 200 且 LayUI `code=0`。
- 此前 Browser MCP 返回 `Transport closed`，移动端截图复核曾阻塞；本轮已完成 390px 响应式验证和截图。

### Step 7：筛选入口闭环与重置行为

- 已增强 `site.js` 中 URL query 到 LayUI 筛选表单的回填逻辑。
- `adminFilter.collect` 已增加兜底：如果表单还未标记完成 query 回填，会先执行回填再序列化表单。
- `adminFilter.reset` 已增强：重置筛选时会清理当前表单非隐藏字段对应的 URL query。
- 重置逻辑会保留隐藏字段和抽屉上下文，例如 `mode`、`id`、`subscriptionId`、`accountId`，避免底部抽屉丢失关联对象。
- 已通过构建验证，结果 0 警告、0 错误。
- 已通过 HTTP session 验证：
  - `/Assets?reviewStatus=1`
  - `/Orders?payStatus=1`
  - `/Settings?keyPrefix=Risk`
  - `/Subscriptions/Downloads?mode=drawer&subscriptionId=test`
  - `/js/site.js` 包含 `clearFilterQuery` 和 `adminQueryHydrated`
  - `/Assets/TableData?reviewStatus=1&page=1&limit=1` 返回 LayUI `code=0`
  - `/Orders/TableData?payStatus=1&page=1&limit=1` 返回 LayUI `code=0`
- 此前 Browser MCP 返回 `Transport closed`，重置按钮点击级验证曾阻塞；本轮已完成顶层页面和抽屉上下文重置验证。

### Step 8：重置后的导航状态一致性

- 已推演 Step 7 后续逻辑问题：顶层列表页重置筛选后虽然 URL query 和 table 会清理，但左侧菜单高亮仍来自初次 Razor 渲染，可能停留在旧筛选入口。
- 已调整 `site.js`：`clearFilterQuery` 返回 URL 是否变化。
- 已新增 `isTopLevelWindow` 判断。
- 顶层页面重置筛选且 URL query 被清理时，会刷新到清理后的基础路由，使菜单高亮、工作台、筛选表单和 table 初始状态重新一致。
- 抽屉 iframe 内重置筛选不会强制刷新，只重置表单和 table，避免丢失 `mode`、`id`、`subscriptionId`、`accountId` 等上下文。
- 已通过构建验证，结果 0 警告、0 错误。
- 已通过 HTTP session 验证：
  - `/Orders?payStatus=1` 返回 200。
  - `/Orders` 返回 200。
  - `/Subscriptions/Downloads?mode=drawer&subscriptionId=test` 保留隐藏 `subscriptionId`。
  - `/js/site.js` 包含 `window.location.reload()` 和 `isTopLevelWindow`。
- 此前 Browser MCP 返回 `Transport closed`，点击级验证曾阻塞；本轮已完成重置后的 URL 和菜单状态验证。

### Step 9：公共 LayUI table 参数语义复核

- 已复核 `adminTable.render` 的默认值处理逻辑。
- 已发现公共参数语义问题：`toolbar: options.toolbar || true` 会导致调用方传入 `toolbar: false` 时仍被强制改为 `true`。
- 已修正为 `toolbar: options.toolbar ?? true`，保留显式 `false`。
- 已同步将 `defaultToolbar` 改为 nullish 默认值，保留调用方显式传入的 `false` 或空配置。
- 已通过构建验证，结果 0 警告、0 错误。
- 已通过 HTTP session 验证：
  - `/` 返回 200 且包含 `admin-workspace`。
  - `/Accounts` 返回 200。
  - `/js/site.js` 返回 200，包含 `toolbar: options.toolbar ?? true` 和 `defaultToolbar: options.defaultToolbar ??`。
  - `/Accounts/TableData?page=1&limit=1` 返回 LayUI `code=0`。
- 此前 Browser MCP 返回 `Transport closed`，浏览器级 table 工具栏验证曾阻塞；本轮已完成 `adminTable.render` 参数透传验证。

### Step 10：回归 LayUI 标准视觉与抽屉严格化

- 已根据反馈确认前序问题：导航被过度自定义成白色侧栏，偏离 LayUI 标准后台视觉。
- 已将方案文档修正为：导航和顶部栏必须保持 LayUI 标准深色体系，差异应体现在菜单信息架构和业务入口，而不是改掉 LayUI 颜色。
- 已恢复导航宽度为 LayUI 默认 200px，折叠宽度为 60px。
- 已将 `_Layout.cshtml` 的 Logo 区恢复为标准文字 Logo。
- 已移除 `_NavPartial.cshtml` 中侧栏简介、分区标题和自定义导航 badge。
- 菜单数量改用 LayUI `layui-badge` / `layui-bg-gray`。
- `site.css` 已增加后置覆盖，恢复 LayUI 标准深色 header、`layui-bg-black` 侧栏、`layui-nav-tree` 链接、子菜单和 `layui-this` 高亮。
- `site.css` 已将 LayUI table 工具栏、表头、分页颜色回到 LayUI 默认风格。
- 已修正账户详情中的编辑资料、更改状态、钱包充值、重置密码为 `data-admin-drawer="right"`。
- 已修正账户编辑、状态、充值、改密页中的返回详情/取消入口为右侧抽屉入口。
- `site.js` 已支持抽屉 iframe 内的 drawer 链接委托父页面 `adminDrawer` 打开，避免在 iframe 内部嵌套 layer。
- 已通过构建验证，结果 0 警告、0 错误。
- 已通过 HTTP session 验证：
  - `/Accounts` 返回 200，包含 LayUI nav-tree 和账户 table。
  - `/css/site.css` 返回 200，包含 LayUI 深色侧栏和默认表头样式。
  - `/js/site.js` 返回 200，包含父页面 drawer 委托逻辑。
  - `/Accounts/TableData?page=1&limit=1` 返回 LayUI `code=0`。
  - `/Accounts/Details?id={真实用户ID}&mode=drawer` 返回 200，包含右侧抽屉链接和 `mode=drawer`。
- 此前 Browser MCP 返回 `Transport closed`，截图和点击级验证曾阻塞；本轮已完成 LayUI 导航和抽屉委托点击验证。

### Browser MCP 补充验证

- MCP 已恢复，已补完前序挂起的点击级和截图级验证。
- 移动端响应式验证：
  - 已在 390px 视口验证 `/Subscriptions`。
  - 摘要条和按钮换行正常。
  - `.layui-table-view` 宽度保持在主体容器内，表格列在 LayUI 表格内部滚动，没有撑破页面主体。
  - 截图验证完成；截图为临时验证产物，未纳入版本变更。
- 筛选重置验证：
  - `/Orders?payStatus=1` 能回填支付状态。
  - 点击重置后 URL 回到 `/Orders`，支付状态清空，表格继续渲染。
  - `/Subscriptions/Downloads?mode=drawer&subscriptionId=...&keyword=test` 点击重置后清理 `keyword`，保留 `mode=drawer` 和 `subscriptionId`。
- LayUI table 参数验证：
  - `toolbar:false` 被保留。
  - `defaultToolbar:false` 被保留。
  - `defaultToolbar:[]` 被保留。
  - 默认调用不再传入 `height`。
- LayUI 标准导航验证：
  - 顶部栏为 LayUI 深色体系。
  - 侧栏保持 `layui-bg-black`。
  - 当前仅 1 个菜单组展开。
- 抽屉严格化验证：
  - `/Accounts` 第一行“查看”打开右侧抽屉。
  - 账户详情 iframe 内点击“编辑资料”后，父页面打开第二个右侧抽屉，没有在 iframe 内嵌套 layer。
- 浏览器控制台未出现 warning/error。

### Step 11：表格高度自动化

- 已按插队要求修正 LayUI table 高度策略：表格默认不再使用固定 `full-*` 高度，而是根据数据内容自动撑开。
- 已调整 `site.js` 的 `adminTable.render`：默认不传 `height`；只有调用方显式传入非空 `height` 时才透传给 LayUI。
- 已移除以下页面的固定表格高度：
  - `Views/Subscriptions/Downloads.cshtml`
  - `Views/Accounts/Transactions.cshtml`
  - `Views/Orders/Transactions.cshtml`
  - `Views/Creators/Assets.cshtml`
  - `Views/Creators/Settlements.cshtml`
- 已保留抽屉自身的 `height: "76%"` / `data-height` 等配置；这些控制的是 layer 抽屉高度，不是表格高度。
- 已通过构建验证：`dotnet build Src\Netor.Cortana.Platform\Netor.Cortana.Platform.Admin\Netor.Cortana.Platform.Admin.csproj`，结果 0 警告、0 错误。
- Browser MCP 已恢复，本轮已验证：
  - `/Subscriptions`：3 行数据，表格容器无 inline `height`，高度约 263px。
  - `/Orders/Transactions`：空态表格无 inline `height`，高度约 232px。
  - `/Subscriptions/Downloads?mode=drawer&subscriptionId=...`：抽屉明细表 1 行数据，表格容器无 inline `height`，高度约 170px。

### Step 12：操作列宽度紧凑化

- 已按界面反馈复核所有列表页操作列配置。
- 已发现可见页面操作列空白主要来自 LayUI 单元格默认左右 padding，实测约 31px。
- 已给包含 `.admin-row-actions.layui-btn-group` 的 LayUI 表格单元格设置 8px 左右 padding。
- 已收窄各列表操作列：
  - 账户：300px -> 285px。
  - 资源：220px -> 205px。
  - 分类：190px -> 176px。
  - 订阅：150px -> 136px。
  - 设置：220px -> 206px。
  - 订单：140px -> 116px。
  - 审核：190px -> 176px。
  - 创作者：260px -> 220px。
  - 结算：220px -> 190px。
- 已通过构建验证：`dotnet build Src\Netor.Cortana.Platform\Netor.Cortana.Platform.Admin\Netor.Cortana.Platform.Admin.csproj`，结果 0 警告、0 错误。
- Browser MCP 已复测：
  - `/Assets` 操作列约 206px，按钮组约 189px，不换行。
  - `/Categories` 操作列约 177px，按钮组约 160px，不换行。
  - `/Subscriptions` 操作列约 137px，按钮组约 120px，不换行。
  - `/Settings` 操作列约 207px，按钮组约 190px，不换行。
  - `/Accounts` 操作列约 286px，按钮组约 269px，不换行。

### Step 13：新增资源 LayUI 拖拽上传

- 已将 `Views/Assets/Create.cshtml` 的资源包上传区改为 LayUI `layui-upload-drag` 拖拽上传控件。
- 已使用 `layui.upload.render` 绑定上传区：
  - `accept: "file"`。
  - `exts: "zip"`。
  - `size: 204800`，对应 200 MB。
  - `auto: false`，不提前 AJAX 上传，继续随当前资源创建表单一起提交。
- 已新增隐藏真实输入 `PackageFile`，并在 LayUI `choose` 回调中把点击或拖拽选中的文件同步到真实表单字段，避免拖拽场景下后端收不到文件。
- 已通过构建验证：`dotnet build Src\Netor.Cortana.Platform\Netor.Cortana.Platform.Admin\Netor.Cortana.Platform.Admin.csproj`，结果 0 警告、0 错误。
- Browser MCP 已验证：
  - `/Assets/Create?mode=drawer` 渲染 `layui-upload-drag asset-layui-upload`。
  - 点击选择 `asset-upload-test.zip` 后，真实 `input#PackageFile` 文件数为 1。
  - 拖拽 `asset-upload-test.zip` 后，真实 `input#PackageFile` 文件数仍为 1。
  - 文件名展示区域更新为 `asset-upload-test.zip · 0.00 MB`。

### Step 14：运营总览图表高度与用户编辑 Ajax 提交

- 已修复 `Views/Home/Index.cshtml` 中 canvas resize 高度持续增长的问题：
  - 不再把已按 DPR 放大的 `height` 属性当作下一轮逻辑高度。
  - 逻辑高度保存到 `data-chart-height`，显示高度固定到 `style.height`。
  - 绘图缓冲区继续按 DPR 设置，保证清晰度。
  - 使用 `context.setTransform(...)` 避免连续 resize 后变换累积。
- 已在 `wwwroot/js/site.js` 新增 LayUI 标准 Ajax 表单提交通道：
  - 通过 `form.on("submit(adminAjaxSubmit)")` 接管带 `lay-filter="adminAjaxSubmit"` 的提交按钮。
  - 提交时使用 `fetch + FormData`，请求头带 `X-Requested-With: XMLHttpRequest` 和 `Accept: application/json`。
  - 成功后显示 LayUI toast，刷新指定表格，并在 iframe 自身上下文关闭当前右侧抽屉。
- 已将 `Views/Accounts/Edit.cshtml` 的右侧抽屉模式改为 Ajax 提交：
  - 表单增加 `data-admin-ajax-form="true"`、`data-admin-table-id="accountTable"`。
  - 保存按钮增加 `lay-filter="adminAjaxSubmit"`。
- 已调整 `AccountsController.Edit`：
  - Ajax 校验失败返回 JSON 400，避免抽屉整页白屏。
  - Ajax 保存成功返回 JSON 200：`success=true`、`message=用户基本信息已保存。`、`tableId=accountTable`。
  - `RealName` 改为可空字段，保存时兜底为空字符串，符合“真实姓名可不填”的表单语义。
  - 旧版账户抽屉 `DrawerSuccess` 改为直接通过 `parent.layer.getFrameIndex(window.name)` 关闭当前 iframe layer，避免非 Ajax 操作留下白页。
- 已通过构建验证：`dotnet build Src\Netor.Cortana.Platform\Netor.Cortana.Platform.Admin\Netor.Cortana.Platform.Admin.csproj`，结果 0 警告、0 错误。
- Browser MCP 已验证：
  - `/` 在 1440px、390px、1200px、800px、1440px 连续切换后，`orderTrendChart`、`assetStatusChart`、`hotAssetChart` 显示高度均保持 260px。
  - `/Accounts` 第一行编辑抽屉提交后，POST `/Accounts/Edit?...&mode=drawer` 返回 JSON 200。
  - toast 显示“用户基本信息已保存。”。
  - 编辑抽屉 iframe 数量从 1 变为 0，父页面仍停留在 `/Accounts`，没有白屏。
  - 浏览器控制台无 warning/error。

### Step 15：新增/修改抽屉 Ajax 提交统一化

- 已复核右侧抽屉新增/修改表单，确认用户编辑之外仍有分类、设置、资源新增等页面没有统一 Ajax 提交。
- 已扩展公共 `wwwroot/js/site.js`：
  - `adminAjaxSubmit` 支持 LayUI `form.on("submit(adminAjaxSubmit)")`。
  - Ajax 提交继续使用 `FormData`，兼容普通表单和文件上传表单。
  - 带 `data-admin-confirm-submit` 的表单保留 LayUI 确认弹层，确认后再提交。
  - 成功后在 iframe 自身上下文调用 `window.adminDrawer.close(...)`，确保能拿到当前 layer index。
- 已改造分类表单：
  - `Views/Categories/_CategoryForm.cshtml` 增加 `data-admin-ajax-form`、`data-admin-table-id="categoryTable"`。
  - 保存按钮增加 `lay-submit lay-filter="adminAjaxSubmit"`。
  - `CategoriesController.Create/Edit` 支持 Ajax JSON 成功和失败返回。
  - 旧版 `DrawerSuccess` 改为直接关闭当前 iframe layer。
- 已改造系统设置表单：
  - `Views/Settings/Edit.cshtml` 增加 `data-admin-ajax-form`、`data-admin-table-id="settingTable"`。
  - 保存按钮增加 `lay-submit lay-filter="adminAjaxSubmit"`。
  - `SettingsController.Create/Edit` 支持 Ajax JSON 成功和失败返回。
  - 旧版 `DrawerSuccess` 改为直接关闭当前 iframe layer。
- 已改造资源新增表单：
  - `Views/Assets/Create.cshtml` 增加 `data-admin-ajax-form`、`data-admin-table-id="assetTable"`。
  - 保存按钮增加 `lay-submit lay-filter="adminAjaxSubmit"`。
  - `AssetsController.Create` 支持 Ajax JSON 成功和失败返回。
  - 未上传 ZIP 时返回中文 JSON 错误“请上传资源包”。
  - 修正 `AssetCreateViewModel.Description`、`Tags` 为可空字段，避免可选输入被 MVC 隐式 Required 拦截。
- 已改造用户右侧操作表单：
  - `Views/Accounts/Status.cshtml`、`Recharge.cshtml`、`Password.cshtml` 接入 `adminAjaxSubmit`。
  - `AccountsController.Status/Recharge/Password` 支持 Ajax JSON 成功和失败返回。
- 已通过构建验证：`dotnet build Src\Netor.Cortana.Platform\Netor.Cortana.Platform.Admin\Netor.Cortana.Platform.Admin.csproj`，结果 0 警告、0 错误。
- Browser MCP 已验证：
  - 用户编辑：POST 返回 JSON 200，toast 显示“用户基本信息已保存。”，抽屉关闭。
  - 分类新增：创建临时分类返回 JSON 200，toast 显示“分类已创建。”，抽屉关闭，表格刷新可见；临时分类已删除。
  - 分类编辑：编辑临时分类返回 JSON 200，toast 显示“分类已保存。”，抽屉关闭，表格刷新可见。
  - 系统设置新增：创建临时设置返回 JSON 200，toast 显示“系统设置已创建。”，抽屉关闭，表格刷新可见；临时设置已删除。
  - 系统设置编辑：编辑临时设置返回 JSON 200，toast 显示“系统设置已保存。”，抽屉关闭，表格刷新可见。
  - 资源新增：在未选择 ZIP 的情况下提交，返回 JSON 400，toast 显示“请上传资源包”，抽屉保持，页面不是白屏。

## 进行中

暂无。

## 待执行

暂无。
