# 执行进度

> 更新时间：2026-06-05

## 已完成

### 阶段 01：资源与约束确认

- 已确认本地 LayUI 资源可用。
- 已明确不使用 `Netor.Operates.LayuiHelpers`。
- 已明确所有界面、表单、控件采用 Razor 编写，并使用 LayUI 标准样式。

### 阶段 02：后台壳与导航

- `_Layout.cshtml` 已加入刷新、全屏、用户菜单、退出确认。
- `_NavPartial.cshtml` 已改为分组导航。
- `_ViewStart.cshtml` 已支持 `mode=drawer` 自动切换抽屉布局。
- 已新增 `_DrawerLayout.cshtml`。

### 阶段 03：公共表格与抽屉能力

- `site.js` 已加入 `adminTable`、`adminDrawer`、`adminFilter`、确认提交等公共能力。
- `site.css` 已加入筛选区、LayUI table、右侧抽屉、底部抽屉、空状态样式。
- 已新增 `LayuiTableResult<T>`。

### 阶段 04：用户账户示范页

- `Accounts/Index.cshtml` 已改为 LayUI table 数据驱动。
- `AccountsController` 已新增 `TableData`、`Transactions`、`TransactionsForAccount`。
- `Accounts/Edit`、`Recharge`、`Password`、`Status`、`Details` 已支持右侧抽屉模式。
- 已新增 `Accounts/Transactions.cshtml` 作为底部交易记录抽屉页。
- 已通过临时输出目录构建验证。
- 已通过 HTTP session 验证 `/Accounts`、`/Accounts/TableData`、`/Accounts/Transactions`、`/Accounts/TransactionsForAccount`。

### 阶段 05：资源与审核

- `Assets/Index.cshtml` 已改为 LayUI table 数据驱动。
- `AssetsController` 已新增 `TableData`。
- `Assets/Create`、`Details` 已支持右侧抽屉模式。
- 资源列表已支持关键词、类型、状态、分类、推荐、拥有者、开发者、价格类型、审核状态、下载、价格、包文件、存储方式筛选。
- 资源批量操作、推荐、发布、下架已改为 table 选中项 + 确认提交。
- `Reviews/Index.cshtml` 已改为 LayUI table 数据驱动。
- `ReviewsController` 已新增 `TableData`。
- 审核列表已支持关键词、审核状态、资源类型、资源状态、提交人、审核人、审核时间筛选。
- 审核通过、驳回、撤回已改为 LayUI table 行操作 + 确认/输入弹层，继续调用现有 `AssetReviewService`。
- 已通过临时输出目录构建验证。
- 已通过 HTTP session 验证 `/Assets`、`/Assets/TableData`、`/Assets/Create?mode=drawer`、`/Assets/Details?...&mode=drawer`、`/Reviews`、`/Reviews/TableData`。

### 阶段 06：订单、交易、订阅、下载

- `Orders/Index.cshtml` 已改为 LayUI table 数据驱动。
- `OrdersController` 已新增 `TableData`，支持订单号、标题、内容、用户、资源、方案、订单状态、支付状态、支付方式、金额、数量、支付时间、是否有流水筛选。
- `Orders/Details.cshtml` 已支持右侧抽屉查看，并可继续从父页面打开底部交易流水抽屉。
- `Orders/Transactions.cshtml` 已改为 LayUI table 数据驱动。
- `OrdersController` 已新增 `TransactionsTableData`，支持流水号、订单号、三方单号、用户、交易类型、支付状态、支付方式、金额、实付金额、支付时间筛选。
- 订单批量状态、交易批量支付状态已改为 table 选中项 + LayUI 确认提交。
- `Subscriptions/Index.cshtml` 已改为 LayUI table 数据驱动。
- `SubscriptionsController` 已新增 `TableData`，支持订阅ID、订单ID、用户、资源、方案、状态、是否有订单、下载次数、开始时间、到期时间、取消时间筛选。
- `Subscriptions/Downloads.cshtml` 已改为 LayUI table 数据驱动，并支持作为底部抽屉查看单个订阅的下载记录。
- `SubscriptionsController` 已新增 `DownloadsTableData`，支持下载ID、订阅ID、用户、资源、版本、IP、客户端、是否有关联订阅筛选。
- 订阅取消、恢复、批量操作已改为 table 行操作/选中项 + LayUI 确认提交。
- 已通过临时输出目录构建验证。
- 已通过 HTTP session 验证 `/Orders`、`/Orders/TableData`、`/Orders/Transactions`、`/Orders/TransactionsTableData`、`/Subscriptions`、`/Subscriptions/TableData`、`/Subscriptions/Downloads`、`/Subscriptions/DownloadsTableData`。

### 阶段 07：分类、创作者、结算、设置

- `Categories/Index.cshtml` 已改为 LayUI table 数据驱动。
- `CategoriesController` 已新增 `TableData`、`Details`，新增、编辑、查看已支持右侧抽屉。
- 分类筛选已覆盖关键词、Slug、可见状态、排序范围、资源数量、已发布资源数量。
- 分类批量显示、隐藏、删除空分类以及行显示/隐藏、删除已改为 LayUI table 选中项/行操作 + 确认提交。
- `Creators/Index.cshtml` 已改为 LayUI table 数据驱动。
- `CreatorsController` 已新增 `TableData`、`Details`、`Assets`、`AssetsTableData`、`Settlements`、`SettlementsTableData`。
- 创作者筛选已覆盖关键词、显示名、用户、状态、通过时间、资产数量、待结算金额、已结算金额、是否有待审核资源。
- 创作者详情已支持右侧抽屉；名下资源、结算记录已支持底部抽屉。
- 创作者通过、暂停已改为 LayUI table 行操作 + 确认提交。
- `Settlements/Index.cshtml` 已改为 LayUI table 数据驱动。
- `SettlementsController` 已新增 `TableData`、`Details`，查看已支持右侧抽屉。
- 结算筛选已覆盖关键词、创作者、资源、订单号、状态、币种、总额、平台费用、净额、结算时间、是否有备注。
- 结算处理、冻结、驳回已改为 LayUI table 行操作 + 备注输入弹层。
- `Settings/Index.cshtml` 已改为 LayUI table 数据驱动。
- `SettingsController` 已新增 `TableData`、`Details`，新增、编辑、查看已支持右侧抽屉。
- 设置筛选已覆盖关键词、Key 前缀、分组、类型、保护、空值、显示名、配置名。
- 设置当前页批量保存已改为 LayUI table 可编辑值列 + 当前页提交；检测包存储、重置、删除已改为确认提交。
- 已通过临时输出目录构建验证。
- 已通过 HTTP session 验证 `/Categories`、`/Categories/TableData`、`/Categories/Create?mode=drawer`、`/Categories/Details?...&mode=drawer`、`/Categories/Edit?...&mode=drawer`、`/Creators`、`/Creators/TableData`、`/Settlements`、`/Settlements/TableData`、`/Settings`、`/Settings/TableData`、`/Settings/Create?mode=drawer`、`/Settings/Details?...&mode=drawer`、`/Settings/Edit?...&mode=drawer`。

### 阶段 08：控制台首页与整体验收

- `Home/Index.cshtml` 已按运营工作台重做，不再使用静态建议卡作为主体。
- `HomeController` 已改为从当前数据库查询首页真实指标。
- 首页指标已覆盖资源总数、待审核资源、用户总数、有效订阅、今日订单、今日收入、待结算金额。
- 首页已展示热门资源排行、最新订单、审核队列，并使用 LayUI 标准表格样式。
- 快捷入口已指向资源、审核、用户、订单、订阅、结算、设置等真实后台模块。
- 已完成全模块统一构建验证。
- 已通过 HTTP session 验证首页及主要模块页面/数据端点：`/`、`/Accounts`、`/Accounts/TableData`、`/Assets`、`/Assets/TableData`、`/Reviews`、`/Reviews/TableData`、`/Orders`、`/Orders/TableData`、`/Orders/Transactions`、`/Orders/TransactionsTableData`、`/Subscriptions`、`/Subscriptions/TableData`、`/Subscriptions/Downloads`、`/Subscriptions/DownloadsTableData`、`/Categories`、`/Categories/TableData`、`/Creators`、`/Creators/TableData`、`/Settlements`、`/Settlements/TableData`、`/Settings`、`/Settings/TableData`。
- 浏览器 MCP 在最终检查时连接中断，未完成控制台截图检查；已采用 HTTP 验证覆盖页面与 JSON 响应。

### 阶段 09：最终收口审计

- 已移除 Admin 项目中的 `Netor.Operates.LayuiHelpers` 包引用。
- 已清理 `Orders/Details.cshtml` 中残留的 Bootstrap 表格和 utility class，改为 `layui-table`、`admin-table`、`admin-empty-cell`、`detail-label`、`admin-section-title`。
- 已复查 Admin 视图目录，未发现 Bootstrap 表单、表格、弹窗和 grid 关键类残留。
- 已补充 HTTP session 验证：`/Auth/Login` 登录成功，`/` 与 `/Orders/TableData?page=1&limit=1` 返回 200；当前数据库订单数为 0，订单详情抽屉未能通过真实订单 ID 做 HTTP 页面验证。
- Browser MCP 本轮重试仍返回 `Transport closed`，未完成截图检查；已保留 HTTP 验证和代码审计结果。
- 已通过临时输出目录构建验证：`dotnet build Src\Netor.Cortana.Platform\Netor.Cortana.Platform.Admin\Netor.Cortana.Platform.Admin.csproj -o .artifacts\admin-build-check`。
- 构建结果：0 警告，0 错误。

### 阶段 10：逻辑推演与筛选闭环

- 已推演 ID、抽屉表单、批量操作、筛选字段、金额空值、审核状态等后台使用链路。
- 已确认 ID 以字符串传递，不存在 JavaScript 长整型精度丢失风险。
- 已确认 `mode=drawer` 表单、`_ViewStart`、`DrawerSuccess` 能形成抽屉保存后关闭并刷新父表格的闭环。
- 已补齐用户列表筛选入口：编号范围、订单上限、订阅上限、下载上限、登录次数上限、注册 IP。
- 已补齐资源列表筛选入口：下载上限、版本数量最小值。
- 已补齐创作者列表筛选入口：已结算金额上限。
- 已修正钱包余额、创作者待结算和已结算金额筛选的空集合逻辑，空钱包/空结算按 0 参与比较。
- 已修正资源审核状态筛选逻辑，改为按最新审核记录匹配，和表格展示状态保持一致。
- 已通过临时输出目录构建验证，结果：0 警告，0 错误。
- 已通过 HTTP session 验证 `/Accounts/TableData`、`/Assets/TableData`、`/Creators/TableData` 带新筛选参数返回 200 且 LayUI `code=0`。

### 阶段 11：视觉与菜单功能面重构

- 已根据反馈复查后台外壳，确认前序实现偏重表格、抽屉、筛选和数据接口，菜单信息架构与旧版通用 LayUI 后台区分度不足。
- `_Layout.cshtml` 已重做 Logo 区和顶部当前工作区展示。
- 已新增 `_WorkspacePartial.cshtml`，提供待审核、待支付、待结算、新增资源、新增分类、存储配置等统一快捷入口。
- `_NavPartial.cshtml` 已按当前项目功能重构为待办队列、用户与钱包、资源市场、订单与权益、创作者经营、系统运维六类业务导航。
- 菜单已补充真实功能入口：资源待审核、资源最新审核、创作者待审核、待结算收益、有余额用户、创作者账户、异常/停用账户、已发布资源、推荐资源、待支付订单、待确认流水、有效订阅、有待审资源、有备注结算、包存储配置、风控配置等。
- 侧栏新增资源、新增分类、新增设置继续通过右侧抽屉打开。
- `site.js` 已新增 URL query 参数自动灌入 LayUI 筛选表单能力，菜单筛选入口可驱动 table 初始查询条件。
- `site.css` 已将旧深色通用侧栏改为白色运营导航，并加入工作台快捷区、Logo 标识、业务导航分组、折叠态和移动端适配。
- 已更新总览、后台壳与导航、验收标准、执行总览文档，明确外壳、菜单和真实功能入口属于验收范围。
- 已通过临时输出目录构建验证，结果：0 警告，0 错误。
- 已通过 HTTP session 验证 `/`、`/Reviews?status=1`、`/Orders?payStatus=1`、`/Settlements?status=1`、`/Assets/Create?mode=drawer`、`/Reviews/TableData?status=1&page=1&limit=1`。
- Browser MCP 仍返回 `Transport closed`，未能完成截图检查；本轮采用 HTTP 验证和构建验证覆盖。

## 进行中

暂无。

## 待执行

暂无。
