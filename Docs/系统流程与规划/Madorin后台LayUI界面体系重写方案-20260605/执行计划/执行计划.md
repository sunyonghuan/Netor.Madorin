# Madorin 后台 LayUI 界面体系重写执行计划 : 100%

## Step 1 重新分析与方案重写 : 100%
- [√] 复查 Admin 控制器、视图、实体和现有外壳缺口。
- [√] 重写拆分版方案总览、能力映射、信息架构、交互规范、模块矩阵、视觉规范。
- [√] 建立新的执行进度文档。

## Step 2 后台外壳与菜单状态化 : 100%
- [√] 为后台外壳新增实时状态服务，提供待审核、待支付、待结算、创作者待审等数量。
- [√] 菜单待办入口显示真实数量提示。
- [√] 工作台快捷入口显示真实数量或金额。
- [√] 保持所有入口落到真实页面、筛选或右侧抽屉。

## Step 3 业务场景快捷入口分层 : 100%
- [√] 按当前页面或业务区调整工作台快捷入口，而不是所有页面完全相同。
- [√] 审核页优先显示审核相关入口，交易页优先显示订单流水入口。
- [√] 用户、资源、创作者、系统设置各自有对应快捷操作。

## Step 4 页面级体验复核 : 100%
- [√] 复核列表页筛选是否支持 URL query 回填。
- [√] 复核右侧抽屉入口和底部抽屉入口。
- [√] 复核移动端侧栏、按钮换行、表格容器不溢出。

## Step 5 验证与提交 : 100%
- [√] 执行 Admin 项目构建。
- [√] 使用 HTTP session 验证首页、菜单筛选入口、抽屉入口和 JSON 数据。
- [√] 更新执行进度。
- [√] 提交本轮方案与执行代码。

## Step 6 菜单场景高亮与响应式复核 : 100%
- [√] 根据 URL query 区分待办场景和业务管理场景，避免多个菜单组同时展开。
- [√] 当前菜单项高亮精确到筛选入口，例如待审核、已发布、待支付、有效订阅。
- [√] 补强工作台按钮在窄屏下的换行和最小宽度。
- [√] 构建与 HTTP 验证。
- [√] Browser MCP 移动端截图复核完成。
- [√] 更新进度并提交。

## Step 7 筛选入口闭环与重置行为 : 100%
- [√] 强化 URL query 到 LayUI 筛选表单的回填逻辑。
- [√] `adminFilter.collect` 在表单未完成 query 回填时自动兜底。
- [√] 重置筛选时清理当前表单的非隐藏 query 字段。
- [√] 保留抽屉上下文字段，例如 `mode`、`id`、`subscriptionId`、`accountId`。
- [√] 构建与 HTTP 验证。
- [√] Browser MCP 重置按钮点击级验证完成。
- [√] 更新进度并提交。

## Step 8 重置后的导航状态一致性 : 100%
- [√] 推演顶层页面重置筛选后的 URL、菜单高亮、表格状态不一致问题。
- [√] 顶层页面清理 query 后刷新到基础路由，让 Razor 菜单和工作台重新渲染。
- [√] 抽屉 iframe 内重置筛选不强制刷新，避免丢失关联对象上下文。
- [√] 构建与 HTTP 验证。
- [√] Browser MCP 点击级验证完成。
- [√] 更新进度并提交。

## Step 9 公共 LayUI table 参数语义复核 : 100%
- [√] 复核 `adminTable.render` 的默认值处理。
- [√] 修正 `toolbar: false` 被默认值覆盖的问题。
- [√] 修正 `defaultToolbar: false` 或空配置无法被保留的问题。
- [√] 构建与 HTTP 验证。
- [√] Browser MCP table 工具栏参数验证完成。
- [√] 更新进度并提交。

## Step 10 回归 LayUI 标准视觉与抽屉严格化 : 100%
- [√] 恢复顶部栏、左侧导航为 LayUI 标准深色体系。
- [√] 移除导航区白色侧栏、侧栏简介、分区标题等非 LayUI 标准视觉。
- [√] 菜单状态数字改用 LayUI `layui-badge`。
- [√] 表格工具栏、表头、分页色彩回归 LayUI 默认。
- [√] 审计新增、修改、查看入口，修正账户详情相关普通跳转为右侧抽屉入口。
- [√] 抽屉 iframe 内再次打开抽屉时委托父页面 `adminDrawer`。
- [√] 构建与 HTTP 验证。
- [√] Browser MCP 截图和点击级验证完成。
- [√] 更新进度并提交。

## Step 11 表格高度自动化 : 100%
- [√] 复核公共 `adminTable.render` 默认参数，确认固定高度来自默认 `height: "full-320"`。
- [√] 移除公共 LayUI table 默认固定高度，改为默认不传 `height`，仅保留调用方显式传入高度时透传。
- [√] 移除交易流水、账户流水、订阅下载、创作者资源、创作者结算等页面级 `full-*` 表格高度。
- [√] 保留右侧/底部抽屉自身高度配置，不混同为表格高度。
- [√] 构建验证通过，0 warning / 0 error。
- [√] Browser MCP 验证 `/Subscriptions`、`/Orders/Transactions`、`/Subscriptions/Downloads?mode=drawer...` 表格均无 inline 固定高度，并按数据内容自动撑开。

## Step 12 操作列宽度紧凑化 : 100%
- [√] Browser MCP 实测账户、资源、分类、订阅、设置等有数据页面的操作列宽度和按钮组宽度。
- [√] 将操作列单元格内边距从 LayUI 默认约 15px 收紧为 8px。
- [√] 收窄账户、资源、分类、订阅、设置、订单、审核、创作者、结算列表的操作列宽度。
- [√] 对无数据页面按最长按钮组做保守预留，避免未来有数据后按钮换行。
- [√] 构建验证通过，0 warning / 0 error。
- [√] Browser MCP 复测可见页面：操作列空白从约 31px 降至约 17px，按钮组不换行。

## Step 13 新增资源 LayUI 拖拽上传 : 100%
- [√] 将新增资源页原生文件输入改为 LayUI `layui-upload-drag` 拖拽上传控件。
- [√] 使用 `layui.upload.render` 绑定点击选择和拖拽选择，限制 `.zip`，大小限制 200 MB。
- [√] 保留后端 `AssetCreateViewModel.PackageFile` 提交流程，通过隐藏真实 `PackageFile` 输入同步 LayUI 选择结果。
- [√] Browser MCP 验证点击选择 zip 后真实 `PackageFile` 包含文件。
- [√] Browser MCP 验证拖拽 zip 后真实 `PackageFile` 同样包含文件。
- [√] 构建验证通过，0 warning / 0 error。

## Step 14 运营总览图表高度与用户编辑 Ajax 提交 : 100%
- [√] 修复运营总览 canvas resize 时反复读取已放大的 `height` 属性导致图表高度持续增长的问题。
- [√] 将 canvas 逻辑高度固定到 `data-chart-height` 和 `style.height`，绘图缓冲区按 DPR 单独设置。
- [√] 使用 `context.setTransform(...)` 替代连续 `scale(...)`，避免重绘时变换累积。
- [√] 为抽屉表单新增通用 LayUI Ajax 提交处理 `adminAjaxSubmit`。
- [√] 用户编辑页在右侧抽屉模式下改为 Ajax 提交，成功后显示 LayUI toast、刷新 `accountTable` 并关闭抽屉。
- [√] 修正用户编辑模型中 `RealName` 的可选字段语义，避免空真实姓名被 MVC 非 nullable 隐式必填拦截。
- [√] 修正账户抽屉旧版 `DrawerSuccess` 直接关闭当前 iframe layer，避免非 Ajax 提交后白屏。
- [√] 构建验证通过，0 warning / 0 error。
- [√] Browser MCP 验证运营总览在 390px、800px、1200px、1440px 间切换时三张图高度均稳定为 260px。
- [√] Browser MCP 验证用户编辑提交返回 JSON 200，toast 显示“用户基本信息已保存。”，抽屉关闭，父页仍停留在用户列表。

## Step 15 新增/修改抽屉 Ajax 提交统一化 : 100%
- [√] 分类新增、分类编辑接入 `adminAjaxSubmit`，成功后 toast、刷新 `categoryTable`、关闭右侧抽屉。
- [√] 系统设置新增、系统设置编辑接入 `adminAjaxSubmit`，成功后 toast、刷新 `settingTable`、关闭右侧抽屉。
- [√] 资源新增上传接入 `adminAjaxSubmit`，使用 `FormData` 保持文件上传能力；校验失败返回 JSON 并保留抽屉，不再白屏。
- [√] 用户状态、钱包充值、重置密码接入 `adminAjaxSubmit`，保留 LayUI 确认弹层，确认后 Ajax 提交。
- [√] 分类、设置、资源控制器补齐 Ajax 成功/失败 JSON 返回。
- [√] 分类、设置、资源、账户控制器旧版 `DrawerSuccess` 修正为直接关闭当前 iframe layer，避免非 Ajax 提交留下白页。
- [√] 修正资源新增模型可选字段 `Description`、`Tags` 的 nullable 语义，避免 MVC 隐式 Required 报英文错误。
- [√] 构建验证通过，0 warning / 0 error。
- [√] Browser MCP 验证分类新增和编辑成功；临时分类已删除。
- [√] Browser MCP 验证系统设置新增和编辑成功；临时设置已删除。
- [√] Browser MCP 验证资源新增未上传 ZIP 时返回 JSON 400、toast 显示“请上传资源包”、抽屉保持且页面不白屏。

## Browser MCP 补充验证结果
- [√] 移动端 390px 验证 `/Subscriptions`：摘要条和按钮换行正常，`.layui-table-view` 未撑破主体容器，截图验证完成；截图为临时验证产物，未纳入版本变更。
- [√] 顶层重置验证 `/Orders?payStatus=1`：支付状态回填为 `1`，点击重置后 URL 回到 `/Orders`，筛选值清空，表格继续渲染。
- [√] 抽屉上下文重置验证 `/Subscriptions/Downloads?mode=drawer&subscriptionId=...&keyword=test`：点击重置后仅清理 `keyword`，保留 `mode=drawer` 和 `subscriptionId`。
- [√] 公共 `adminTable.render` 参数验证：`toolbar:false`、`defaultToolbar:false`、`defaultToolbar:[]` 均被保留，默认调用不再传入 `height`。
- [√] LayUI 标准导航验证：顶部栏背景为 `rgb(35, 38, 46)`，侧栏为 `layui-bg-black` 深色体系，当前仅 1 个菜单组展开。
- [√] 右侧抽屉点击验证：`/Accounts` 第一行“查看”打开 880px 右侧抽屉。
- [√] iframe 内抽屉委托验证：账户详情 iframe 内点击“编辑资料”后，父页面打开第二个 760px 右侧抽屉，没有在 iframe 内嵌套 layer。
- [√] 浏览器控制台无 warning/error。
- [√] 操作列宽度复测：`/Assets` 206px、`/Categories` 177px、`/Subscriptions` 137px、`/Settings` 207px、`/Accounts` 286px，按钮组均不换行。
- [√] 新增资源页上传控件复测：`/Assets/Create?mode=drawer` 渲染 `layui-upload-drag`，点击选择和拖拽选择 zip 后真实 `PackageFile` 字段均有文件。
- [√] 运营总览图表 resize 复测：三张 canvas 在多组视口宽度切换后 `clientHeight`、`data-chart-height`、`style.height` 均保持 260px。
- [√] 用户编辑 Ajax 复测：`/Accounts/Edit?...&mode=drawer` 提交返回 `{ success: true, tableId: "accountTable" }`，父页 toast 正常，编辑抽屉关闭。
- [√] 新增/修改抽屉 Ajax 复测：分类新增/编辑、设置新增/编辑均返回 JSON 200 并关闭抽屉；资源新增缺少 ZIP 返回 JSON 400，保留抽屉并显示 toast。
