# 03 - 宿主转发器实现

> 子文档 03 / 06，从属于 [README.md](README.md) v1.5
>
> 本文档把 README §3.2 与 §3.6 的宿主侧改动展开到代码级颗粒度，作为后续 PR 的脚手架。所有代码片段都按现仓库 AOT 强约束写成。
>
> **v1.1 修订（2026-06-14）**：与 README v1.3 同步，按 F1-F7 修正：复用 `PluginBusEventMessage` 不新建 POCO；F2 op 优先 eventType 兜底；F3 排除 host source；F4 `PluginManifestRegistry` 改 DI 单例；F6 后台投递 + per-subscriber 1 秒超时；F7 `voice.tts.greeting_ready` 显式列 case。
>
> **v1.2 修订（2026-06-14）**：与 README v1.4 同步，按源码核验结果修订 7 条 F-XXX（详见原条目）。
>
> **v1.3 修订（2026-06-14）**：与 README v1.5 同步，落地 5 个 OQ + 5 个 Top 高危 + 中危补丁（完整清单见 [README.md §九 v1.5 修订记录](README.md)）：
>
> 1. **OQ1 PluginManifestRegistry 跨程序集**：从 `Src/Netor.Cortana.Networks/WebSockets/` 改放 `Src/Netor.Cortana.Plugin/`（贴近 PluginLoader 写入方）；DI 注册移到 `PluginServiceExtensions.AddCortanaPlugin`；BroadcastDispatcher 跨程序集消费（Networks → Plugin 单向依赖维持）
> 2. **OQ2 删除直发式 grace 期**：本文档全部 grace 期措辞删除，HandleMessageAsync 入口仅拦截 `type == "event"`，不为 `type=voice.xxx` 加分支
> 3. **OQ3 chat.\* op 重命名 A2**：§6.5 分支顺序所有 `chat.message.send` / `chat.generation.stop` 字面量更新为 `conversation.chat.message.send` / `conversation.chat.generation.stop`
> 4. **OQ4 memory/model 子命名空间**：§2.1 职责 1 受保护 op 黑名单包含完整 RPC op 清单（`memory.context.supply.*` / `model.capability.*`），plugin event 必须用 `memory.event.*` / `model.event.*`
> 5. **OQ5 NormalizeVoiceOp**：§三 VoiceEventBridge 入口加 `NormalizeVoiceOp(op)` 把 `voice.xxx` 归一到 `voice.xxx.v1`，兼容旧版仓库三件套发包；新增测试 V12
> 6. **R-CHAT-MISFIRE 受保护 op 黑名单**：§2.1 新增职责 1 `IsProtectedOp` 守卫（详见 §2.3 内部方法）；测试矩阵加 T17 chat 帧误标 type=event 防御
> 7. **R-PAYLOAD-AMP 单帧 1MB 上限**：§2.2 HandleAsync 入口加 `frame.GetRawText().Length` 校验；测试加 T18 大 payload 拒绝
> 8. **R-DEAD-SUBSCRIBER 死链清理**：§2.3 SendWithTimeoutAsync OperationCanceledException 分支主动 `_subscriptions.Remove(cid)` + `_pluginBusConnections.RemoveAndCloseAsync(cid)`；测试加 T19 死链清理验证
> 9. **R-AOT-CMD 命令矩阵**：§八 §8.4 验证命令重写——库走 `dotnet build` + `<IsAotCompatible>true</IsAotCompatible>`，可执行项目（UI / NativeHost / 三件套）独立 publish 矩阵
> 10. **R-FILES 文件清单补全**：§一 修改清单补 [PluginManifest.cs](../../../Src/Netor.Cortana.Plugin/Core/PluginManifest.cs)（加 PublishedOps / SubscribedOps + 两个 record）/ NetworkServiceExtensions / PluginServiceExtensions / [WebSocketPluginBusServerMeetingIntegrationTests:49](../../../Tests/Netor.Cortana.Networks.Tests/WebSocketPluginBusServerMeetingIntegrationTests.cs#L49) ctor 调用四处
>
> **v1.2 修订（2026-06-14）**：与 README v1.4 同步，按源码核验结果修订 7 条：
>
> 1. **F-LIFETIME（JsonElement 生命周期）**：`HandleMessageAsync` 中 `using var doc = JsonDocument.Parse(json)` 在方法返回后立即 Dispose，`root` / `frame` 即指向已释放对象。`HandleAsync` 同步阶段必须把 frame 转成 owned 数据（forwarded JSON 字符串 + 目标 clientId 列表 snapshot），后台 `Task.Run` 内**绝不**再读 `JsonElement`。
> 2. **F-PRIORITY（op 优先级是有意变更）**：旧 `PluginBusVoiceEventDispatcher.ReadEventType` 真实优先级 `eventType > op > type`，v1.4.0 **有意变更**为 `op > eventType > type`，把 op 提到首位。所有"延续现有规则"措辞改为"1.4.0 有意变更"，并明确兼容/迁移：op 与 eventType 同时存在且不一致时以 op 为权威，老插件无 op 时 fallback 到 eventType（不破坏 1.3.0 行为）。
> 3. **F-DIRECT（老 voice 直发式入口闭环）**：voice 三件套同 PR 升级到 `type=event` + op，宿主**不**为旧的 `type=voice.xxx` 直发式加兜底分支；grace 期承诺仅面向外部第三方插件，本仓库三件套发布前都已升级。
> 4. **F-SCHEMA（publishedOps/subscribedOps schema 矛盾）**：`plugin.json` schema 统一为对象数组 `[{op, description/reason}]`；`PluginManifest` 模型新增 `PublishedOpDeclaration` / `SubscribedOpDeclaration` record；`PluginManifestRegistry.Register` 签名保留 `IEnumerable<string>`（仅取 op 集合），由 `PluginLoader` 内从对象数组抽 op 后传入；Generator 输出对象数组。
> 5. **F-LOADER（PluginManifestRegistry 接入点挂底层）**：注册挂在 `LoadProcessPluginAsync` (line 403, 成功分支 line 416-420) / `LoadNativePluginAsync` (line 432, 成功分支 line 445-449) 的 `if (host.Plugins.Count > 0)` 紧挨 `NotifyPluginsChanged()` 之处；注销挂在 `UnloadPluginByPath` (line 730-753) `changed=true` 后 / `NotifyPluginsChanged()` 前；`UnloadAllPlugins` (line 233) 加 `manifests.Clear()`。顶层方法（`ScanAndLoadAsync` / `LoadPluginByPathAsync` / `Reload*` / 文件 watcher / `UnloadPlugin`）**不需要**单独挂，自动通过底层方法覆盖。
> 6. **F-FIELDCOUNT（README §2.1 字段计数订正）**：README 标题"仅新增 1 个字段 hopCount"与字段表 + 代码示例不符（实际 2 个字段），改为"新增 2 个可选字段（hopCount + sourcePluginId）"。
> 7. **F-SOURCE（source / sourcePluginId 语义收紧）**：v1 路由仅信任 clientId 排除自回环（不信任自报字段）；弱声明校验从 frame 读 `sourcePluginId`（缺省 fallback 到 `source`），与 `plugin.json` 比对，仅 LogWarning，不作为安全依据；`sourcePluginId` 是新增字段，推荐插件填，向下兼容 `source`（host→plugin 现有 `source = "host"` 模式不变）；F3 host 排除仅靠 `source == "host"` 判别（host 自身的 `BroadcastPluginBusAsync` 已确保不会进 `HandleMessageAsync` 上行；客户端冒充 `source = "host"` 上行被立即拒）；v2 才做可信绑定。

---

## 一、文件清单

### 新建（5 个）

| 文件 | 用途 |
| --- | --- |
| `Src/Netor.Cortana.Networks/WebSockets/PluginBusBroadcastDispatcher.cs` | plugin→plugin 方向 `type: event` 帧转发器（§二） |
| `Src/Netor.Cortana.Networks/WebSockets/Voice/VoiceEventBridge.cs` | voice.\* 事件桥接到宿主 IPublisher（§三） |
| `Src/Netor.Cortana.Plugin/PluginManifestRegistry.cs` | DI 单例（v1.5 OQ1 修订：从 Networks 改放 Plugin 程序集贴近 PluginLoader 写入方；BroadcastDispatcher 跨程序集消费走现有间接路径 Networks → AI → Plugin，编译期可见 Plugin 公开类型，新增直接 ProjectReference 是依赖加固而非编译必需），由 PluginLoader 注入插件 publishedOps / subscribedOps（§四） |
| `Tests/Netor.Cortana.Networks.Tests/PluginBusBroadcastDispatcherTests.cs` | 七条主路径覆盖（§七） |
| `Tests/Netor.Cortana.Networks.Tests/VoiceEventBridgeTests.cs` | voice 桥接单测（§七） |

### 删除（2 个）

| 文件 | 删除原因 |
| --- | --- |
| [`Src/Netor.Cortana.Networks/WebSockets/Voice/PluginBusVoiceEventDispatcher.cs`](../../../Src/Netor.Cortana.Networks/WebSockets/Voice/PluginBusVoiceEventDispatcher.cs) | 顶层拦截器角色被 BroadcastDispatcher 取代；switch 逻辑搬到 VoiceEventBridge |
| [`Tests/Netor.Cortana.Networks.Tests/PluginBusVoiceEventDispatcherTests.cs`](../../../Tests/Netor.Cortana.Networks.Tests/PluginBusVoiceEventDispatcherTests.cs) | 测试用例并入新两份；`TryHandle_TtsGreetingReady_ReturnsTrue` 迁移为 V10 |

### 修改（8 个）

| 文件 | 改动 |
| --- | --- |
| [PluginBusSubscriptionRegistry.cs](../../../Src/Netor.Cortana.Networks/WebSockets/PluginBusSubscriptionRegistry.cs) | 新增 op-level 订阅 API（§五） |
| [WebSocketPluginBusServerService.cs](../../../Src/Netor.Cortana.Networks/WebSockets/Servers/WebSocketPluginBusServerService.cs) | ctor 注入新组件 + HandleMessageAsync 替换分支（§六） |
| [CortanaWsEndpoints.cs:22](../../../Src/Netor.Cortana.Entitys/CortanaWsEndpoints.cs#L22) | `PluginBusVersion` 升至 `"1.4.0"`；**不新增** `EventFrameType` 常量（沿用字面量 `"event"`） |
| [Serialization/WebSocketJsonContext.cs](../../../Src/Netor.Cortana.Networks/WebSockets/Serialization/WebSocketJsonContext.cs#L93) | **复用** `PluginBusEventMessage`（已注册），新增 2 个可选属性：`HopCount` / `SourcePluginId`；**不新建** `PluginBusEventFrame` |
| [Core/PluginManifest.cs](../../../Src/Netor.Cortana.Plugin/Core/PluginManifest.cs) | 新增 `PublishedOps` / `SubscribedOps` 两个 `IReadOnlyList<>` 字段 + 两个 record `PublishedOpDeclaration` / `SubscribedOpDeclaration`（含 `op` + `description/reason`，对应 plugin.json 对象数组 schema）；`Normalize()` 兜底 `?? []`；同步在 [Core/PluginManifestJsonContext.cs](../../../Src/Netor.Cortana.Plugin/Core/PluginManifestJsonContext.cs) 追加 2 条 `[JsonSerializable]` 注册 |
| [PluginServiceExtensions.cs](../../../Src/Netor.Cortana.Plugin/PluginServiceExtensions.cs) | `AddCortanaPlugin` 新增 `services.AddSingleton<PluginManifestRegistry>()`（v1.5 OQ1，DI 单例就近 PluginLoader 写入方） |
| `Src/Netor.Cortana.Networks/NetworkServiceExtensions.cs`（按现有模块 DI 入口路径） | 新增 DI 注册：`PluginBusBroadcastDispatcher` / `VoiceEventBridge`（PR 实施时确认与现有 `Add*` 方法一致；如不存在则直接复用 `WebSocketPluginBusServerService` 注册位） |
| [WebSocketPluginBusServerMeetingIntegrationTests.cs:49](../../../Tests/Netor.Cortana.Networks.Tests/WebSocketPluginBusServerMeetingIntegrationTests.cs#L49) | `WebSocketPluginBusServerService` ctor 增 `PluginManifestRegistry` 参数后，本测试 ctor 调用同步加构造参数（其他 Networks/Tests 内 ctor 调用同 PR 一并改动） |

---

## 二、PluginBusBroadcastDispatcher

### 2.1 职责

1. **受保护 op 防御（v1.5 R-CHAT-MISFIRE）**：在所有解析前先做黑名单守卫；命中 `IsProtectedOp(op)` 立即 LogWarning 并丢弃，不进入后续路由。受保护 op 列表 = 现有 RPC / 控制 op 全集：`conversation.chat.message.send` / `conversation.chat.generation.stop` / `system.notice` / `conversation.history.replay` / `workflow.history.replay` / `meeting.history.replay` / `replay` / `model.capability.request` / `model.capability.response` / `memory.context.supply.request` / `memory.context.supply.response` / `memory.context.supply.error`。原因：客户端 SDK bug 把 chat 帧误标 `type=event` 时，新分支会先于 chat 分支命中并按 op namespace 路由到 conversation topic，可能把控制帧错投给广播订阅者；显式拒收避免对话链路无故断流（详见 §2.3 内部方法 `IsProtectedOp`）。
2. **来源辨识（F3 + F-SOURCE）**：读 `source` 字段，**仅当 `source != "host"`** 时进入广播路径（host→plugin 走现有 [BroadcastPluginBusAsync](../../../Src/Netor.Cortana.Networks/WebSockets/Servers/WebSocketPluginBusServerService.cs#L242)，不经本 dispatcher）。
   - **v1 安全模型**：路由层仅信任 clientId 排除自回环（连接级身份），**不**信任 frame 内自报的 `source` / `sourcePluginId` 作为安全依据
   - **F3 host 排除**：仅靠 `source == "host"` 判别（host 自身的 `BroadcastPluginBusAsync` 已确保 host 出帧不会进 `HandleMessageAsync` 上行；客户端冒充 `source = "host"` 上行被立即拒）
   - **弱声明校验**（见职责 5）从 frame 读 `sourcePluginId`（缺省 fallback 到 `source`），与 `plugin.json` 比对仅 LogWarning，不作为安全依据
   - v2 才做可信绑定（clientId ↔ pluginId 强绑定）
3. **单帧字节上限（v1.5 R-PAYLOAD-AMP）**：HandleAsync 入口检查 `frame.GetRawText().Length`，超过 `MaxFrameBytes`（默认 1MB，可配）即丢弃 + LogWarning。**重要**：[WebSocketPluginBusServerService:381](../../../Src/Netor.Cortana.Networks/WebSockets/Servers/WebSocketPluginBusServerService.cs#L381) 的 16K 是 `ReceiveAsync` 读缓冲，不是消息上限；引入 plugin→plugin 广播后 1 个大事件 × N 订阅者会被复制到 N 份 forwardedJson，瞬时 OOM，必须在 dispatcher 入口二次防御。
4. **op 解析（F2 + F-PRIORITY）**：按 `op > eventType > type` 优先级解析。**1.4.0 有意变更优先级**——旧版 [PluginBusVoiceEventDispatcher.ReadEventType](../../../Src/Netor.Cortana.Networks/WebSockets/Voice/PluginBusVoiceEventDispatcher.cs#L88-L93) 真实优先级是 `eventType > op > type`，新版改为 `op > eventType > type`，把 op 提到第一位。
   - **兼容/迁移**：`op` 与 `eventType` 同时存在且不一致时，以 `op` 为权威；老插件无 `op` 时 fallback 到 `eventType`，不破坏 1.3.0 行为
   - 三者都缺即丢弃
5. **hopCount 兜底**：判 `hopCount >= 8` 立即丢弃 + LogWarning
6. **弱声明校验**：发布方 `plugin.json.publishedOps` 是否声明该 op（不一致仅 LogWarning，与 5B-D 一致）
7. **路由**：从 op 解析 namespace 段为 topic → `_subscriptions.GetSubscribers(topic)` → 按订阅方 `subscribedOps` 二次过滤 → 排除 sourceClientId
8. **转发（F6 后台投递 + F-LIFETIME + R-DEAD-SUBSCRIBER）**：业务侧不等订阅方响应，但宿主等待网络投递完成（每个订阅者独立 1 秒超时）；整个转发循环放在后台 `Task.Run`，`HandleAsync` 立刻返回，不阻塞 ReceiveLoop；hopCount 字段重写为原值 + 1。**死链主动清理**（v1.5 R-DEAD-SUBSCRIBER）：SendWithTimeoutAsync 的 OperationCanceledException 分支主动调 `_subscriptions.Remove(cid)` + `_pluginBusConnections.RemoveAndCloseAsync(cid)`，避免 [WebSocketConnectionManager.SendAsync:94](../../../Src/Netor.Cortana.Networks/WebSockets/Connections/WebSocketConnectionManager.cs#L94) 的 `when (!cancellationToken.IsCancellationRequested)` 守卫被 1 秒 linked CTS 击穿、自动 RemoveAndCloseAsync 不触发导致死链残留
   - **F-LIFETIME 警告**：上游 `HandleMessageAsync` 用 `using var doc = JsonDocument.Parse(json)`，方法返回后 `doc` 立即 Dispose，`frame` / `root` 立即指向已释放对象。`HandleAsync` 同步阶段**必须**把 frame 转成 owned 数据（forwarded JSON 字符串 + 目标 clientId 列表 snapshot 到数组），后台 `Task.Run` 内**只**调用 `SendWithTimeoutAsync`，**绝不**再读 `JsonElement` 或访问 `_subscriptions` 字典
9. **voice 桥接**：当 `op.namespace == "voice"`，**额外**调用 `VoiceEventBridge.Bridge(op, frame)` 桥接到宿主 `IPublisher`，UI 层零感知。**桥接发生在 `HandleAsync` 同步阶段**（不进后台 `Task.Run`），避免 `JsonElement` 跨越生命周期边界。`VoiceEventBridge` 内部对 op 做 `NormalizeVoiceOp` 归一化（v1.5 OQ5），兼容旧版仓库三件套发包（无 `.v1`）的 envelope 帧

### 2.2 类骨架

```csharp
using System.Text.Json;
using Microsoft.Extensions.Logging;
using Netor.Cortana.Networks.WebSockets.Connections;
using Netor.Cortana.Plugin;

namespace Netor.Cortana.Networks;

/// <summary>
/// 处理 PluginBus 上 plugin->plugin 方向 type=event 的异步广播事件帧。
/// 由 WebSocketPluginBusServerService.HandleMessageAsync 在识别到事件帧后调用。
/// host->plugin 方向的 event 帧由 BroadcastPluginBusAsync 直接处理，不经本类。
/// 详见 docs/已完成功能规划/插件事件广播/README.md §2.4。
/// </summary>
internal sealed class PluginBusBroadcastDispatcher
{
    private const int MaxHopCount = 8;
    private const int PerSubscriberTimeoutMs = 1000;
    // v1.5 R-PAYLOAD-AMP：单帧 raw JSON 字节上限。1MB 是默认值，最终阈值由配置/常量控制
    private const int MaxFrameBytes = 1 * 1024 * 1024;

    private readonly PluginBusSubscriptionRegistry _subscriptions;
    private readonly PluginManifestRegistry _manifests;
    private readonly VoiceEventBridge _voiceBridge;
    // v1.5 R-DEAD-SUBSCRIBER：注入连接管理器以便死链主动清理
    private readonly WebSocketConnectionManager _pluginBusConnections;
    private readonly Func<string, string, CancellationToken, Task> _sendAsync;
    private readonly ILogger _logger;

    public PluginBusBroadcastDispatcher(
        PluginBusSubscriptionRegistry subscriptions,
        PluginManifestRegistry manifests,
        VoiceEventBridge voiceBridge,
        WebSocketConnectionManager pluginBusConnections,
        Func<string, string, CancellationToken, Task> sendAsync,
        ILogger logger)
    {
        _subscriptions = subscriptions;
        _manifests = manifests;
        _voiceBridge = voiceBridge;
        _pluginBusConnections = pluginBusConnections;
        _sendAsync = sendAsync;
        _logger = logger;
    }

    /// <summary>
    /// 处理一个 type=event 帧。同步返回（不阻塞 ReceiveLoop），实际转发在后台 Task.Run 中执行。
    ///
    /// F-LIFETIME 关键约束：上游 HandleMessageAsync 用 using var doc = JsonDocument.Parse(json)，
    /// 本方法返回后 doc 立即 Dispose、frame 立即指向已释放对象。
    /// 因此本方法的同步阶段必须：
    ///   (a) 把 frame 重写为 owned 字符串（forwarded JSON）
    ///   (b) 把订阅者 clientId 列表 snapshot 到数组
    ///   (c) voice 桥接也必须在同步阶段完成（VoiceEventBridge 内部读 JsonElement）
    /// 后台 Task.Run 内只用 owned 字符串和 clientId 数组，绝不再读 JsonElement 或 _subscriptions 字典。
    /// </summary>
    public Task HandleAsync(string sourceClientId, JsonElement frame, CancellationToken ct)
    {
        // v1.5 R-PAYLOAD-AMP：单帧字节上限二次防御（ReadTextMessageAsync 16K 是读缓冲不是消息上限）
        var rawLength = frame.GetRawText().Length;
        if (rawLength > MaxFrameBytes)
        {
            _logger.LogWarning(
                "Event dropped: frame too large. Bytes={Bytes} Limit={Limit} ClientId={ClientId}",
                rawLength, MaxFrameBytes, sourceClientId);
            return Task.CompletedTask;
        }

        // F3：host source 排除（v1 仅信任 clientId 排除自回环，但 source==host 的客户端冒充立即拒）
        var source = ReadString(frame, "source");
        if (string.Equals(source, "host", StringComparison.Ordinal))
        {
            _logger.LogWarning(
                "Client uploaded host-source event, ignored. ClientId={ClientId}", sourceClientId);
            return Task.CompletedTask;
        }

        // F2 / F-PRIORITY：op > eventType > type 优先级（1.4.0 有意变更，旧版为 eventType > op > type）
        if (!TryParse(frame, out var op, out var hops, out var sourcePluginId))
        {
            _logger.LogWarning("Event frame missing op/eventType. ClientId={ClientId}", sourceClientId);
            return Task.CompletedTask;
        }

        // v1.5 R-CHAT-MISFIRE：受保护 op 黑名单守卫，命中即拒收，不进入路由
        if (IsProtectedOp(op))
        {
            _logger.LogWarning(
                "Event dropped: protected op cannot be broadcast. Op={Op} ClientId={ClientId}",
                op, sourceClientId);
            return Task.CompletedTask;
        }

        if (hops >= MaxHopCount)
        {
            _logger.LogWarning(
                "Event dropped: hop limit exceeded. Op={Op} Hops={Hops} ClientId={ClientId}",
                op, hops, sourceClientId);
            return Task.CompletedTask;
        }

        // F-SOURCE：弱声明校验从 frame 读 sourcePluginId（缺省 fallback 到 source），仅 LogWarning
        ValidatePublisherDeclaration(sourcePluginId, op);

        var topic = ExtractNamespace(op);

        // F-LIFETIME 同步阶段 (a)：重写 hopCount 得到 owned JSON 字符串，脱离 JsonElement 生命周期
        var forwardedJson = RewriteHopCount(frame, hops + 1);

        // F-LIFETIME 同步阶段 (b)：snapshot 订阅者 clientId 列表为数组，脱离 _subscriptions 字典
        var targetClientIds = _subscriptions.GetSubscribers(topic)
            .Where(cid => !string.Equals(cid, sourceClientId, StringComparison.Ordinal))
            .Where(cid => _subscriptions.MatchesSubscribedOp(cid, op))
            .ToArray();

        // F-LIFETIME 同步阶段 (c)：voice 桥接必须在 frame 仍然有效时调用
        // VoiceEventBridge 内部读 JsonElement.payload，进后台前必须执行完
        if (string.Equals(topic, "voice", StringComparison.Ordinal))
        {
            _voiceBridge.Bridge(op, frame);
        }

        // F6：后台投递，HandleAsync 立刻返回；后台只用 owned 数据
        _ = Task.Run(
            () => ForwardToSubscribersAsync(targetClientIds, op, forwardedJson, ct),
            ct);

        return Task.CompletedTask;
    }

    // ── 内部方法见 §2.3 ──
}
```

### 2.3 内部方法

```csharp
private static bool TryParse(
    JsonElement frame,
    out string op,
    out int hops,
    out string sourcePluginId)
{
    // F2 / F-PRIORITY：op > eventType > type 优先级
    // 1.4.0 有意变更，旧版 PluginBusVoiceEventDispatcher.ReadEventType 真实优先级是 eventType > op > type，
    // 新版把 op 提到首位，op 与 eventType 同时存在且不一致时以 op 为权威；
    // 老插件无 op 时 fallback 到 eventType，不破坏 1.3.0 行为。
    //
    // 兼容两种格式：
    // - 信封式（v1.4 后所有插件）: type=event + op=voice.xxx.v1 → 取 op
    // - 老信封式（1.3.0 老插件未升级 op）: type=event + eventType=voice.xxx → 取 eventType
    // 直发式（type=voice.xxx）已在 HandleMessageAsync 入口被排除（§6.2 F-DIRECT），不会到达本方法
    op = ReadString(frame, "op")
        ?? ReadString(frame, "eventType")
        ?? ReadString(frame, "type")
        ?? string.Empty;

    // F-SOURCE：sourcePluginId 是 1.4.0 新增字段，推荐插件填；
    // 缺省 fallback 到 source，向下兼容老插件；仅供弱声明校验使用，不作为安全依据
    sourcePluginId = ReadString(frame, "sourcePluginId")
        ?? ReadString(frame, "source")
        ?? string.Empty;

    hops = frame.TryGetProperty("hopCount", out var h) && h.ValueKind == JsonValueKind.Number
        ? h.GetInt32()
        : 0;

    // type 字段如果是控制字（"event" / "subscribe" / "pong" 等），不能被当 op 误用
    if (string.Equals(op, "event", StringComparison.Ordinal)
        || string.Equals(op, "subscribe", StringComparison.Ordinal)
        || string.Equals(op, "pong", StringComparison.Ordinal)
        || string.Equals(op, "send", StringComparison.Ordinal)
        || string.Equals(op, "stop", StringComparison.Ordinal))
    {
        op = string.Empty;
    }

    return !string.IsNullOrWhiteSpace(op);
}

private static string ExtractNamespace(string op)
{
    var dot = op.IndexOf('.', StringComparison.Ordinal);
    return dot > 0 ? op[..dot] : op;
}

private void ValidatePublisherDeclaration(string sourcePluginId, string op)
{
    // F-SOURCE：v1 弱声明，仅 LogWarning，不强拒（与 5B-D 一致）；不作为安全依据
    if (string.IsNullOrWhiteSpace(sourcePluginId)) return;
    if (_manifests.HasPublishedOp(sourcePluginId, op)) return;

    _logger.LogWarning(
        "Event publisher not declared in plugin.json. PluginId={PluginId} Op={Op}",
        sourcePluginId, op);
}

private string RewriteHopCount(JsonElement frame, int newHops)
{
    // 复制原帧、重写 hopCount，避免修改入参
    // F-LIFETIME：返回 owned UTF-8 字符串，脱离 frame 的 JsonDocument 生命周期
    using var stream = new MemoryStream();
    using (var writer = new Utf8JsonWriter(stream))
    {
        writer.WriteStartObject();
        var hopWritten = false;
        foreach (var property in frame.EnumerateObject())
        {
            if (string.Equals(property.Name, "hopCount", StringComparison.Ordinal))
            {
                writer.WriteNumber("hopCount", newHops);
                hopWritten = true;
                continue;
            }
            property.WriteTo(writer);
        }
        if (!hopWritten)
        {
            writer.WriteNumber("hopCount", newHops);
        }
        writer.WriteEndObject();
    }
    return Encoding.UTF8.GetString(stream.ToArray());
}

private async Task ForwardToSubscribersAsync(
    IReadOnlyCollection<string> targetClientIds,
    string op,
    string forwardedJson,
    CancellationToken ct)
{
    // F-LIFETIME：本方法只接收 owned 数据（clientId 数组 + JSON 字符串），
    // 不再访问 _subscriptions 字典或任何 JsonElement，可安全地在后台 Task.Run 内执行
    // F6：per-subscriber 1 秒超时，慢订阅者不阻塞其他人；外层 Task.Run 已让 HandleAsync 立刻返回
    var tasks = targetClientIds
        .Select(cid => SendWithTimeoutAsync(cid, op, forwardedJson, ct));
    await Task.WhenAll(tasks).ConfigureAwait(false);
}

private async Task SendWithTimeoutAsync(string cid, string op, string payload, CancellationToken ct)
{
    using var cts = CancellationTokenSource.CreateLinkedTokenSource(ct);
    cts.CancelAfter(PerSubscriberTimeoutMs);
    try
    {
        await _sendAsync(cid, payload, cts.Token).ConfigureAwait(false);
    }
    catch (OperationCanceledException) when (cts.IsCancellationRequested && !ct.IsCancellationRequested)
    {
        // v1.5 R-DEAD-SUBSCRIBER：1 秒 linked CTS cancel 会击穿
        // WebSocketConnectionManager.SendAsync:94 的 `when (!cancellationToken.IsCancellationRequested)` 守卫，
        // 自动 RemoveAndCloseAsync 不会触发，必须主动清理；否则死链残留导致每次广播都重新尝试 1 秒超时
        _logger.LogWarning("Forward timeout, dead-link cleanup. Target={Cid} Op={Op}", cid, op);
        _subscriptions.Remove(cid);
        await _pluginBusConnections.RemoveAndCloseAsync(cid).ConfigureAwait(false);
    }
    catch (Exception ex) when (ex is not OperationCanceledException)
    {
        _logger.LogWarning(ex, "Forward failed. Target={Cid} Op={Op}", cid, op);
    }
}

/// <summary>
/// v1.5 R-CHAT-MISFIRE 受保护 op 黑名单守卫。
/// 命中即拒收，不进入广播路由——避免客户端 SDK bug 把 RPC/控制帧误标 type=event 时
/// 走广播路径错投给订阅者，进而打断对话/记忆/模型链路。
/// 列表 = 现有 RPC / 控制 op 全集（OQ3 重命名后 chat.* 都在 conversation 子命名空间下）。
/// </summary>
private static bool IsProtectedOp(string op)
{
    return op switch
    {
        "conversation.chat.message.send" => true,
        "conversation.chat.generation.stop" => true,
        "system.notice" => true,
        "conversation.history.replay" => true,
        "workflow.history.replay" => true,
        "meeting.history.replay" => true,
        "replay" => true,
        "model.capability.request" => true,
        "model.capability.response" => true,
        "memory.context.supply.request" => true,
        "memory.context.supply.response" => true,
        "memory.context.supply.error" => true,
        _ => false,
    };
}

private static string? ReadString(JsonElement element, string propertyName)
{
    return element.TryGetProperty(propertyName, out var p) && p.ValueKind == JsonValueKind.String
        ? p.GetString()
        : null;
}
```

### 2.4 AOT 注意

- 使用 `Utf8JsonWriter` 手动重写 hopCount，避免 `JsonSerializer.Serialize<T>` 反射重载
- `JsonElement` 透传 payload，宿主**不**反序列化为业务类型
- 全部 logger 调用使用模板字符串，未来可迁移到 `[LoggerMessage]` 源生成器
- `CancellationTokenSource.CreateLinkedTokenSource` 已 AOT 验证可用

---

## 三、VoiceEventBridge

### 3.1 职责

把现有 [PluginBusVoiceEventDispatcher.cs:35-85](../../../Src/Netor.Cortana.Networks/WebSockets/Voice/PluginBusVoiceEventDispatcher.cs#L35-L85) 的 switch 逻辑搬过来，**只做"按 op 桥接到 IPublisher"**，不再做帧识别（识别由 BroadcastDispatcher 统一做）。

### 3.2 类骨架

```csharp
using System.Text.Json;
using Microsoft.Extensions.Logging;
using Netor.Cortana.Entitys;
using Netor.EventHub;

namespace Netor.Cortana.Networks;

/// <summary>
/// voice.* 事件桥接器：把 PluginBusBroadcastDispatcher 转发的 voice 事件
/// 桥接到宿主 IPublisher 的 Events.OnXxx，UI 层（字幕/桌宠/状态）零感知。
/// 取代原 PluginBusVoiceEventDispatcher 的拦截器角色。
/// </summary>
internal sealed class VoiceEventBridge
{
    private readonly IPublisher _publisher;
    private readonly ILogger _logger;

    public VoiceEventBridge(IPublisher publisher, ILogger logger)
    {
        _publisher = publisher;
        _logger = logger;
    }

    public void Bridge(string op, JsonElement frame)
    {
        // F-LIFETIME 注意：本方法由 PluginBusBroadcastDispatcher.HandleAsync 在同步阶段调用，
        // frame 此时仍然有效（来自 HandleMessageAsync 的 using var doc）。
        // 本方法是同步方法、不进 Task.Run、不持有 frame 引用，方法返回后调用方释放 doc 也安全。
        // 未来若桥接需要异步化（例如桥接到外部消息总线），必须先把 payload Clone 或转 owned。

        // v1.5 OQ5：归一化 op 到 .v1 后缀，兼容旧版仓库三件套（无 .v1）发包；
        // 否则旧 voice.tts.subtitle 帧落 default 分支，UI 字幕静默失声。
        op = NormalizeVoiceOp(op);

        var payload = frame.TryGetProperty("payload", out var p) && p.ValueKind == JsonValueKind.Object
            ? p
            : frame;
        var text = ReadString(payload, "text") ?? string.Empty;

        switch (op)
        {
            case "voice.kws.detected.v1":
                _publisher.Publish(Events.OnWakeWordDetected, new VoiceSignalArgs());
                break;

            case "voice.stt.partial.v1":
                _publisher.Publish(Events.OnSttPartial, new VoiceTextArgs(text));
                break;

            case "voice.stt.final.v1":
                _publisher.Publish(Events.OnSttFinal, new VoiceTextArgs(text));
                break;

            case "voice.stt.stopped.v1":
                _publisher.Publish(Events.OnSttStopped, new VoiceSignalArgs());
                break;

            case "voice.tts.started.v1":
                _publisher.Publish(Events.OnTtsStarted, new VoiceSignalArgs());
                break;

            case "voice.tts.subtitle.v1":
                _publisher.Publish(Events.OnTtsSubtitle, new VoiceTextArgs(text));
                break;

            case "voice.tts.completed.v1":
                _publisher.Publish(Events.OnTtsCompleted, new VoiceSignalArgs());
                break;

            // F7：显式列 case，与旧 dispatcher TryHandle_TtsGreetingReady_ReturnsTrue 测试期望对齐：
            // 该 op 仅业务上通知 TTS 问候语模型已就绪，宿主侧无 UI 联动需求，吞掉不发 IPublisher。
            // 不要走 default 分支以免被误判为未识别 op。
            case "voice.tts.greeting_ready.v1":
                _logger.LogDebug("Voice greeting ready (no UI bridge by design). Op={Op}", op);
                break;

            // F7：voice.tts.error.v1 显式列 case，对齐旧 dispatcher 的 .error 后缀分支
            case "voice.tts.error.v1":
                _logger.LogWarning("Voice TTS plugin reported error. Op={Op} Text={Text}", op, text);
                break;

            default:
                if (op.EndsWith(".error.v1", StringComparison.Ordinal)
                    || op.EndsWith(".error", StringComparison.Ordinal))
                {
                    _logger.LogWarning("Voice plugin reported error. Op={Op} Text={Text}", op, text);
                    return;
                }
                _logger.LogDebug("Unmapped voice op (forward only). Op={Op}", op);
                break;
        }
    }

    private static string? ReadString(JsonElement element, string propertyName)
    {
        return element.TryGetProperty(propertyName, out var p) && p.ValueKind == JsonValueKind.String
            ? p.GetString()
            : null;
    }

    /// <summary>
    /// v1.5 OQ5：归一化 voice op 到 .vN 后缀。
    /// 旧版仓库三件套发包不带 .v1 后缀（如 voice.tts.subtitle），
    /// 宿主升级后 case 表全是 voice.xxx.v1 形式，未归一化会落 default 分支导致 UI 字幕静默失声。
    /// 规则：op 不含 .v 段则追加 .v1；已带 .vN 后缀则原样返回。
    /// </summary>
    internal static string NormalizeVoiceOp(string op)
    {
        if (string.IsNullOrEmpty(op))
        {
            return op;
        }
        // 检查是否已带 .vN 后缀（最后一段以 v 开头跟数字）
        var lastDot = op.LastIndexOf('.');
        if (lastDot >= 0 && lastDot + 1 < op.Length)
        {
            var tail = op.AsSpan(lastDot + 1);
            if (tail.Length >= 2 && tail[0] == 'v' && char.IsDigit(tail[1]))
            {
                return op;
            }
        }
        return op + ".v1";
    }
}
```

### 3.3 op 命名变化对照（基于真实 voice 三件套发包代码）

下表对应仓库内 voice 三件套实际发布的 op 清单，详见 [§3.5 README v1.3](README.md) 与各 client 源码：

| 来源 | 当前 op | 升级后 op | IPublisher 桥接 | 备注 |
| --- | --- | --- | --- | --- |
| [KwsPluginBusClient.PublishDetectedAsync:30](../../../Plugins/Src/Cortana.Plugins.Voice.Kws.Sherpa/PluginBus/KwsPluginBusClient.cs#L30) | `voice.kws.detected` | `voice.kws.detected.v1` | `Events.OnWakeWordDetected` | KWS 唯一 op |
| [SttPluginBusClient.PublishPartialAsync:30](../../../Plugins/Src/Cortana.Plugins.Voice.Stt.Sherpa/PluginBus/SttPluginBusClient.cs#L30) | `voice.stt.partial` | `voice.stt.partial.v1` | `Events.OnSttPartial` | 携带 text |
| [SttPluginBusClient.PublishFinalAsync:33](../../../Plugins/Src/Cortana.Plugins.Voice.Stt.Sherpa/PluginBus/SttPluginBusClient.cs#L33) | `voice.stt.final` | `voice.stt.final.v1` | `Events.OnSttFinal` | 携带 text |
| [SttPluginBusClient.PublishStoppedAsync:36](../../../Plugins/Src/Cortana.Plugins.Voice.Stt.Sherpa/PluginBus/SttPluginBusClient.cs#L36) | `voice.stt.stopped` | `voice.stt.stopped.v1` | `Events.OnSttStopped` | — |
| [TtsPluginBusClient.PublishStartedAsync:30](../../../Plugins/Src/Cortana.Plugins.Voice.Tts.Melo/PluginBus/TtsPluginBusClient.cs#L30) | `voice.tts.started` | `voice.tts.started.v1` | `Events.OnTtsStarted` | — |
| [TtsPluginBusClient.PublishSubtitleAsync:33](../../../Plugins/Src/Cortana.Plugins.Voice.Tts.Melo/PluginBus/TtsPluginBusClient.cs#L33) | `voice.tts.subtitle` | `voice.tts.subtitle.v1` | `Events.OnTtsSubtitle` | 携带 text |
| [TtsPluginBusClient.PublishCompletedAsync:36](../../../Plugins/Src/Cortana.Plugins.Voice.Tts.Melo/PluginBus/TtsPluginBusClient.cs#L36) | `voice.tts.completed` | `voice.tts.completed.v1` | `Events.OnTtsCompleted` | — |
| [TtsPluginBusClient.PublishGreetingReadyAsync:39](../../../Plugins/Src/Cortana.Plugins.Voice.Tts.Melo/PluginBus/TtsPluginBusClient.cs#L39) | `voice.tts.greeting_ready` | `voice.tts.greeting_ready.v1` | **不桥接（设计如此）** | F7：显式列 case + LogDebug；对齐旧 dispatcher [TryHandle_TtsGreetingReady_ReturnsTrue](../../../Tests/Netor.Cortana.Networks.Tests/PluginBusVoiceEventDispatcherTests.cs#L108-L123) 行为 |
| [TtsPluginBusClient.PublishErrorAsync:42](../../../Plugins/Src/Cortana.Plugins.Voice.Tts.Melo/PluginBus/TtsPluginBusClient.cs#L42) | `voice.tts.error` | `voice.tts.error.v1` | LogWarning（不发 IPublisher 事件） | 对齐旧 dispatcher `.error` 后缀分支 |

加 `.v1` 后缀符合 README §2.1 op 命名规范，未来若 payload 演进可平滑发 `.v2`。

---

## 四、PluginManifestRegistry

### 4.1 职责

由 [PluginLoader](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs) 在 load / reload / unload 时把 `plugin.json` 的 `publishedOps` / `subscribedOps` 推到本注册器，供 `BroadcastDispatcher` 校验发布方声明。

注意：**v1 弱声明**，不一致只 LogWarning，不阻断转发；该注册器存在的目的是给运维侧"声明 vs 实际"对账提供数据。

### 4.2 类骨架（DI 单例）

```csharp
using System.Collections.Concurrent;
using Microsoft.Extensions.Logging;

namespace Netor.Cortana.Plugin;

/// <summary>
/// 维护所有已加载插件的事件契约声明（来自 plugin.json publishedOps / subscribedOps）。
/// 由 PluginBusBroadcastDispatcher 在弱声明校验时查询。
///
/// DI 单例：应在容器中 AddSingleton；由 PluginLoader 在 load/reload/unload 时调用 Register/Unregister。
///
/// v1.5 OQ1：放在 Netor.Cortana.Plugin 程序集贴近 PluginLoader 写入方；
/// BroadcastDispatcher 跨程序集消费走现有间接路径 Networks → AI → Plugin（编译期可见 Plugin 公开类型，
/// 无需在 Networks.csproj 新增直接 ProjectReference；如要加固依赖链，可显式追加，但不是编译必需）。
/// </summary>
public sealed class PluginManifestRegistry
{
    private readonly ConcurrentDictionary<string, HashSet<string>> _publishedOps =
        new(StringComparer.Ordinal);

    private readonly ConcurrentDictionary<string, HashSet<string>> _subscribedOps =
        new(StringComparer.Ordinal);

    private readonly ILogger<PluginManifestRegistry> _logger;

    public PluginManifestRegistry(ILogger<PluginManifestRegistry> logger) => _logger = logger;

    /// <summary>
    /// 由 PluginLoader 在每次插件 load / reload 后调用。重复调用会覆盖旧记录。
    /// </summary>
    public void Register(string pluginId, IEnumerable<string> publishedOps, IEnumerable<string> subscribedOps)
    {
        ArgumentException.ThrowIfNullOrWhiteSpace(pluginId);
        _publishedOps[pluginId] = new HashSet<string>(publishedOps ?? Array.Empty<string>(), StringComparer.Ordinal);
        _subscribedOps[pluginId] = new HashSet<string>(subscribedOps ?? Array.Empty<string>(), StringComparer.Ordinal);
    }

    public void Unregister(string pluginId)
    {
        if (string.IsNullOrWhiteSpace(pluginId)) return;
        _publishedOps.TryRemove(pluginId, out _);
        _subscribedOps.TryRemove(pluginId, out _);
    }

    public bool HasPublishedOp(string pluginId, string op)
    {
        return _publishedOps.TryGetValue(pluginId, out var ops) && ops.Contains(op);
    }

    public bool HasSubscribedOp(string pluginId, string op)
    {
        return _subscribedOps.TryGetValue(pluginId, out var ops) && ops.Contains(op);
    }
}
```

### 4.3 接入点（基于真实 PluginLoader 方法 / F-LOADER）

源码核验过 [Src/Netor.Cortana.Plugin/PluginLoader.cs](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs) 的实际加载结构后确认：**注册/注销必须挂底层（汇聚点）方法，不要单独挂顶层方法**——所有顶层入口都通过底层方法间接覆盖。

**底层挂接点（必须改）**：

| PluginLoader 方法 | 行号 | 改动 |
| --- | --- | --- |
| [`LoadProcessPluginAsync`](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs#L403) | line 416-420 | 在 `if (host.Plugins.Count > 0)` 成功分支内、紧挨 `NotifyPluginsChanged()` 之前调 `_manifestRegistry.Register(pluginId, pubOps, subOps)` |
| [`LoadNativePluginAsync`](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs#L432) | line 445-449 | 同上，紧挨 `NotifyPluginsChanged()` 之前调 `_manifestRegistry.Register(...)` |
| [`UnloadPluginByPath`](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs#L730) | line 730-753 | `changed=true` 分支末尾、`NotifyPluginsChanged()` 之前调 `_manifestRegistry.Unregister(pluginId)` |
| [`UnloadAllPlugins`](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs#L233) | — | 末尾追加 `_manifestRegistry.Clear()` 整体清空 |

**顶层入口（不需要单独挂，自动覆盖）**：

| PluginLoader 方法 | 行号 | 覆盖路径 |
| --- | --- | --- |
| [`ScanAndLoadAsync`](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs#L168) | line 168 | 走 `TryLoadPluginDirectoryAsync` (line 347) → `LoadProcessPluginAsync` / `LoadNativePluginAsync`，自动注册 |
| [`LoadPluginByPathAsync`](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs#L297) | line 297 | 同上 |
| [`ReloadPluginAsync`](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs#L276) | line 276 | 内部先 `UnloadPluginByPath` → `TryLoadPluginDirectoryAsync`，自动注销+注册（`Register` 重复调用会覆盖旧记录） |
| [`ReloadPluginByIdAsync`](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs#L325) | line 325 | 同上 |
| [`UnloadPlugin`](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs#L264) | line 264 | 走 `UnloadPluginByPath`，自动注销 |
| `OnPluginDirectoryCreated` | line 575 | 文件 watcher，走 `TryLoadPluginDirectoryAsync` |
| `OnPluginDirectoryDeleted` | line 587 | 文件 watcher，走 `UnloadPluginByPath` |
| `OnPluginDirectoryRenamed` | line 597 | 文件 watcher，走 `UnloadPluginByPath` + `TryLoadPluginDirectoryAsync` |

需要在 `PluginManifestRegistry` 上加一个轻量 `Clear()` 方法配合 `UnloadAllPlugins`：

```csharp
public void Clear()
{
    _publishedOps.Clear();
    _subscribedOps.Clear();
}
```

DI 注册（应在 `Program.cs` 或 `ServiceCollectionExtensions` 等容器配置入口）：

```csharp
services.AddSingleton<PluginManifestRegistry>();
// PluginLoader 已存在的注册行追加 manifest 依赖（PluginLoader ctor 接受注入）
// WebSocketPluginBusServerService ctor 也接受注入（详见 §六）
```

`PluginLoader` 自身已经有 [`Events.OnPluginsChanged`](../../../Src/Netor.Cortana.Plugin/PluginLoader.cs#L628) 发布点，可作为 manifest 变更的辅助通知，但 BroadcastDispatcher 不需要订阅它（直接查询注册器即可）。

### 4.4 schema 一致性（F-SCHEMA）

源码核验确认了三处对 `publishedOps` / `subscribedOps` 形态的描述彼此矛盾。本版本统一收口为：

| 层 | 形态 |
| --- | --- |
| `plugin.json` schema | **对象数组** `[{op, description?, reason?}]`，便于运维侧"声明 vs 实际"对账 |
| `PluginManifest` C# 模型 | record 数组，新增两个声明类型：`PublishedOpDeclaration(string Op, string? Description)` / `SubscribedOpDeclaration(string Op, string? Reason)` |
| `PluginManifestRegistry.Register` 签名 | 保留 `IEnumerable<string>`（仅取 op 集合），由 `PluginLoader` 内从对象数组 `manifest.PublishedOps.Select(p => p.Op)` 抽 op 后传入 |
| Generator 输出 | 对象数组（与 schema 对齐） |

PluginManifest 模型增量：

```csharp
public sealed record PublishedOpDeclaration(
    [property: JsonPropertyName("op")] string Op,
    [property: JsonPropertyName("description")] string? Description = null);

public sealed record SubscribedOpDeclaration(
    [property: JsonPropertyName("op")] string Op,
    [property: JsonPropertyName("reason")] string? Reason = null);

// PluginManifest 上叠加：
[JsonPropertyName("publishedOps")]  public PublishedOpDeclaration[]? PublishedOps { get; init; }
[JsonPropertyName("subscribedOps")] public SubscribedOpDeclaration[]? SubscribedOps { get; init; }
```

PluginLoader 调用样例（在 §4.3 的两个底层挂接点上）：

```csharp
// LoadProcessPluginAsync line 416-420 / LoadNativePluginAsync line 445-449
if (host.Plugins.Count > 0)
{
    var pubOps = manifest.PublishedOps?.Select(p => p.Op).ToArray()
                 ?? Array.Empty<string>();
    var subOps = manifest.SubscribedOps?.Select(s => s.Op).ToArray()
                 ?? Array.Empty<string>();
    _manifestRegistry.Register(pluginId, pubOps, subOps);
    NotifyPluginsChanged();
}
```

### 4.5 AOT 注意

- `ConcurrentDictionary<string, HashSet<string>>` 已在 `PluginBusSubscriptionRegistry` 中验证可 AOT
- `plugin.json` 的 `publishedOps` / `subscribedOps` 由 Generator 生成时**已是对象数组**（详见 04-SDK与Generator.md，§4.4 schema 一致性）；PluginLoader 现有 plugin.json 解析路径已 AOT 验证；本方案追加 `PublishedOpDeclaration` / `SubscribedOpDeclaration` 两个 record 到 `JsonSerializerContext`，类型简单（仅 string + string?），无新增反射风险

---

## 五、PluginBusSubscriptionRegistry 扩展

### 5.1 新增字段

在 [现有类](../../../Src/Netor.Cortana.Networks/WebSockets/PluginBusSubscriptionRegistry.cs) 上叠加 op 级订阅维度，与 `_subscriptions`（topic）/ `_capabilities` 平级:

```csharp
// 现有
private readonly ConcurrentDictionary<string, HashSet<string>> _subscriptions = new(StringComparer.Ordinal);
private readonly ConcurrentDictionary<string, HashSet<string>> _capabilities = new(StringComparer.Ordinal);

// 新增
private readonly ConcurrentDictionary<string, HashSet<string>> _subscribedOps = new(StringComparer.Ordinal);
```

### 5.2 新增 API

```csharp
/// <summary>
/// 记录客户端在 subscribe 帧中声明的 subscribedOps 集合。
/// 缺省（空集合）= 订阅 topic 下所有 op，无细粒度过滤。
/// </summary>
public void SetSubscribedOps(string clientId, IEnumerable<string> ops)
{
    ArgumentException.ThrowIfNullOrWhiteSpace(clientId);
    ArgumentNullException.ThrowIfNull(ops);

    var normalized = ops
        .Select(static op => op.Trim())
        .Where(static op => !string.IsNullOrWhiteSpace(op))
        .Distinct(StringComparer.Ordinal)
        .ToArray();

    _subscribedOps[clientId] = new HashSet<string>(normalized, StringComparer.Ordinal);
}

/// <summary>
/// 判断客户端是否应当收到该 op（缺省订阅 = 全收）。
/// </summary>
public bool MatchesSubscribedOp(string clientId, string op)
{
    if (string.IsNullOrWhiteSpace(clientId) || string.IsNullOrWhiteSpace(op))
        return false;
    if (!_subscribedOps.TryGetValue(clientId, out var ops) || ops.Count == 0)
        return true;   // 缺省 = 订阅 topic 下全部
    return ops.Contains(op);
}

public IReadOnlyCollection<string> GetSubscribedOps(string clientId)
{
    if (string.IsNullOrWhiteSpace(clientId)) return Array.Empty<string>();
    return _subscribedOps.TryGetValue(clientId, out var ops)
        ? ops.ToArray()
        : Array.Empty<string>();
}
```

### 5.3 现有方法叠加清理

`Remove(clientId)` 与 `Clear()` 必须同步清理 `_subscribedOps`:

```csharp
public void Remove(string clientId)
{
    if (string.IsNullOrWhiteSpace(clientId)) return;
    _subscriptions.TryRemove(clientId, out _);
    _capabilities.TryRemove(clientId, out _);
    _subscribedOps.TryRemove(clientId, out _);   // 新增
}

public void Clear()
{
    _subscriptions.Clear();
    _capabilities.Clear();
    _subscribedOps.Clear();   // 新增
}
```

---

## 六、WebSocketPluginBusServerService 接入

### 6.1 ctor 改动（DI 注入）

`WebSocketPluginBusServerService` 的 ctor 改为接受 `PluginManifestRegistry` 作为 DI 注入参数（与 §四 4.3 一致），不要在 ctor 内部 `new`：

```csharp
// 改前 —— ctor 内创建 _voiceEventDispatcher
_voiceEventDispatcher = new PluginBusVoiceEventDispatcher(publisher, logger);

// 改后 —— ctor 参数追加 PluginManifestRegistry 注入
public WebSocketPluginBusServerService(
    ILogger<WebSocketPluginBusServerService> logger,
    SystemSettingsService settingsService,
    CortanaDbContext db,
    AgentService agentService,
    IPluginModelCapabilityService modelCapabilityService,
    IPublisher publisher,
    PluginManifestRegistry manifestRegistry)   // ★ 新增 DI 参数
{
    // ... 现有字段赋值保持不变 ...

    _manifestRegistry = manifestRegistry;
    _voiceEventBridge = new VoiceEventBridge(publisher, logger);
    _broadcastDispatcher = new PluginBusBroadcastDispatcher(
        _subscriptions,
        _manifestRegistry,
        _voiceEventBridge,
        _pluginBusConnections,   // v1.5 R-DEAD-SUBSCRIBER：现有字段（WebSocketPluginBusServerService 已注入），死链主动清理用
        SendAsync,
        logger);
}
```

DI 容器配置（`Program.cs` 或等价入口）：

```csharp
services.AddSingleton<PluginManifestRegistry>();
services.AddSingleton<WebSocketPluginBusServerService>();
// PluginLoader 已存在的注册行追加 PluginManifestRegistry 依赖（详见 §四 4.3）
```

`VoiceEventBridge` 是 dispatcher 的内部协作者，不需要进 DI 容器（直接 new 即可）。

### 6.2 HandleMessageAsync 分支替换

```csharp
// 改前 —— 删除整段（实际行号 PR 实施时按 grep 定位 _voiceEventDispatcher.TryHandle）
if (_voiceEventDispatcher.TryHandle(id, root))
{
    return;
}

// 改后 —— 在 pong 分支后、conversation.chat.message.send 之前插入
// F-DIRECT + v1.5 OQ2：仅拦截 type == "event"，不为旧 type=voice.xxx 直发式加兜底分支
// 理由：voice 三件套（KWS / STT / TTS）同 PR 升级到 type=event + op，
// 仓库内三件套发布前都已升级；外部第三方语音插件不存在，1.4.0 起直发式不再支持，
// 宿主侧不为它额外加分支（避免老格式路径常驻）。
if (string.Equals(type, "event", StringComparison.Ordinal))
{
    await _broadcastDispatcher.HandleAsync(id, root, ct).ConfigureAwait(false);
    return;
}
```

### 6.3 subscribe 分支扩展

[HandleMessageAsync:462-502](../../../Src/Netor.Cortana.Networks/WebSockets/Servers/WebSocketPluginBusServerService.cs#L462-L502) 现有 capabilities 解析后追加：

```csharp
// 解析 subscribedOps（v1.4.0 新增字段，缺省 = 订阅 topic 下全部 op）
var subscribedOps = root.TryGetProperty("subscribedOps", out var opsElement)
    && opsElement.ValueKind == JsonValueKind.Array
        ? opsElement.EnumerateArray()
            .Select(static o => o.GetString())
            .Where(static o => !string.IsNullOrWhiteSpace(o))
            .Cast<string>()
            .Distinct(StringComparer.Ordinal)
            .ToArray()
        : Array.Empty<string>();
_subscriptions.SetSubscribedOps(id, subscribedOps);
```

### 6.4 字段声明

类字段区域:

```csharp
// 删除
private readonly PluginBusVoiceEventDispatcher _voiceEventDispatcher;

// 新增
private readonly PluginManifestRegistry _manifestRegistry;
private readonly VoiceEventBridge _voiceEventBridge;
private readonly PluginBusBroadcastDispatcher _broadcastDispatcher;
```

### 6.5 改动顺序保证（与 §5.4 README 一致）

`HandleMessageAsync` 分支按下面顺序排列，确保不破坏现有路径：

1. `type == "pong"` → MarkPongReceived
2. **`type == "event"` → BroadcastDispatcher（新增）**
3. `type == "send" / op == "conversation.chat.message.send"` → HandleChatMessageAsync（v1.5 OQ3 A2 重命名）
4. `type == "stop" / op == "conversation.chat.generation.stop"` → HandleChatMessageAsync（v1.5 OQ3 A2 重命名）
5. `type == "system.notice"` → HandleChatMessageAsync
6. `type == "request" + op == model.capability.request` → ModelCapabilityDispatcher
7. `type == "subscribe"` → 订阅注册（含新增 subscribedOps 解析）
8. `type == "replay" / request + replay op` → 历史回放分支
9. `type == "response/error" + memory.context.supply` → MemorySupplyDispatcher
10. fallback `LogWarning("未知消息类型")`

---

## 七、单元测试矩阵

### 7.1 PluginBusBroadcastDispatcherTests

参考 [PluginBusVoiceEventDispatcherTests](../../../Tests/Netor.Cortana.Networks.Tests/PluginBusVoiceEventDispatcherTests.cs) 风格（伪 IPublisher / 伪 logger / 伪 sendAsync）。覆盖主路径：

| 用例 | 输入 | 期望 |
| --- | --- | --- |
| T1 正常转发 | 2 个订阅者订阅 `voice` topic，1 个发布者发 `voice.kws.detected.v1` | 2 个订阅者各收到一次帧；发布者自身不收到 |
| T2 自回环排除 | 发布者同时也订阅了 `voice` topic | 发布者自己不收到自己发的帧 |
| T3 hopCount 上限 | 帧带 `hopCount: 8` | 不转发，LogWarning 触发 |
| T4 subscribedOps 二次过滤 | 订阅者声明 `subscribedOps: ["voice.tts.subtitle.v1"]`，发布者发 `voice.kws.detected.v1` | 订阅者**不**收到 |
| T5 voice 桥接 | 发布 `voice.stt.partial.v1` | `IPublisher.Publish(Events.OnSttPartial, ...)` 被调用一次 |
| **T6 source==host 拒绝（F3）** | 帧带 `source: "host"`，type=event | LogWarning 不转发，订阅者不收到 |
| **T7 op 优先级（F2）** | 帧只填 `eventType: "voice.kws.detected.v1"`，无 op 字段 | 按 eventType 兜底解析，仍正确转发 |
| **T8 op 优先级（F2）** | 帧填 `op: "voice.kws.detected.v1"` 与 `eventType: "voice.tts.subtitle.v1"` 冲突 | 以 op 为准，桥接 `Events.OnWakeWordDetected` |
| T9 hopCount 重写 | 帧带 `hopCount: 3`，订阅者收到的帧 | 订阅者收到的 `hopCount == 4` |
| T10 弱声明校验 | 发布者 plugin.json 未声明 `publishedOps` 含该 op | LogWarning，但**仍转发**（v1 弱声明） |
| T11 缺省 subscribedOps | 订阅者未声明 subscribedOps（空数组） | 收到 topic 下所有 op |
| T12 非 voice topic | 发布 `memory.index.updated.v1` | 转发但**不**调用 VoiceEventBridge |
| T13 op 缺失 | 帧无 `op` / `eventType` / `type` 字段 | LogWarning，不转发，不抛 |
| **T14 后台投递（F6）** | 1 个慢订阅者 sendAsync 阻塞 2 秒 | `HandleAsync` 在 100ms 内返回；慢订阅者 1 秒后超时 LogWarning；其他订阅者正常收到 |
| **T15 JsonElement 生命周期（F-LIFETIME）** | 调用 `HandleAsync(id, frame, ct)` 后立即 `doc.Dispose()`，再让后台 Task.Run 继续投递 | 后台投递**不**抛 `ObjectDisposedException`；订阅者收到完整 forwarded JSON；`HandleAsync` 同步阶段已重写 hopCount + snapshot clientIds + voice 桥接（如需） |
| **T16 op 优先级（F-PRIORITY）** | 帧同时填 `op="voice.kws.detected.v1"` 与 `eventType="voice.tts.subtitle.v1"`，两者不一致 | 以 `op` 为权威，桥接 `Events.OnWakeWordDetected`（不是 `OnTtsSubtitle`）；订阅者收到的 forwarded 帧 op 字段保持 `voice.kws.detected.v1` |
| **T17 受保护 op 防御（v1.5 R-CHAT-MISFIRE）** | 帧标 `type="event"`、`op="conversation.chat.message.send"` 上行（客户端 SDK bug 误标） | LogWarning 拒收，**不**调用 `_subscriptions.GetSubscribers`、**不**进入 voice bridge、**不**进后台 Task.Run；订阅 conversation topic 的其他客户端不收到任何帧 |
| **T18 大 payload 拒绝（v1.5 R-PAYLOAD-AMP）** | `frame.GetRawText().Length > 1MB`（`payload.data` 填 1.5MB 字符串） | LogWarning 拒收；`HandleAsync` 立即返回 `Task.CompletedTask`；后台 Task.Run **未启动**；不调用 `ForwardToSubscribersAsync` |
| **T19 死链清理（v1.5 R-DEAD-SUBSCRIBER）** | 1 个订阅者 `sendAsync` 阻塞 2 秒（被 1 秒 linked CTS 取消） | `SendWithTimeoutAsync` 的 `OperationCanceledException` 分支 LogWarning + 调 `_subscriptions.Remove(cid)` + `_pluginBusConnections.RemoveAndCloseAsync(cid)`；下一次广播相同 op 时该 cid 不在订阅列表 |

### 7.2 VoiceEventBridgeTests

并入测试文件，覆盖 §3.3 全部 op 各自的 IPublisher.Publish 调用：

| 用例 | op | 期望事件 |
| --- | --- | --- |
| V1 | `voice.kws.detected.v1` | `Events.OnWakeWordDetected` |
| V2 | `voice.stt.partial.v1` | `Events.OnSttPartial` + 携带 text |
| V3 | `voice.stt.final.v1` | `Events.OnSttFinal` + 携带 text |
| V4 | `voice.stt.stopped.v1` | `Events.OnSttStopped` |
| V5 | `voice.tts.started.v1` | `Events.OnTtsStarted` |
| V6 | `voice.tts.subtitle.v1` | `Events.OnTtsSubtitle` + 携带 text |
| V7 | `voice.tts.completed.v1` | `Events.OnTtsCompleted` |
| V8 | 未识别 op `voice.foo.v1` | LogDebug，不发 IPublisher 事件 |
| V9 | `voice.xxx.error` | LogWarning，不发 IPublisher 事件 |
| **V10（F7）** | `voice.tts.greeting_ready.v1` | LogDebug，**不**发 IPublisher 事件（与旧 dispatcher [TryHandle_TtsGreetingReady_ReturnsTrue](../../../Tests/Netor.Cortana.Networks.Tests/PluginBusVoiceEventDispatcherTests.cs#L108-L123) 行为对齐） |
| **V11（F7）** | `voice.tts.error.v1` | LogWarning，不发 IPublisher 事件 |
| **V12（v1.5 OQ5 NormalizeVoiceOp）** | 旧版三件套发包 `op = "voice.tts.subtitle"`（无 `.v1` 后缀） | `NormalizeVoiceOp` 归一为 `voice.tts.subtitle.v1`，命中 case 桥接 `Events.OnTtsSubtitle`（与新版 .v1 帧行为等价；防 UI 字幕静默失声） |

### 7.3 PluginBusSubscriptionRegistryTests 增量

现有测试基础上追加：

| 用例 | 期望 |
| --- | --- |
| `SetSubscribedOps` 后 `MatchesSubscribedOp(op)` 命中 | true |
| `SetSubscribedOps` 后 `MatchesSubscribedOp(其他 op)` | false |
| 未调用 `SetSubscribedOps`（缺省） | `MatchesSubscribedOp(任意)` 全 true |
| `Remove(clientId)` 后查询 | 返回空 / false |
| `Clear()` 后查询 | 返回空 / false |

---

## 八、AOT 与序列化上下文

### 8.1 复用现有 PluginBusEventMessage（不新建 POCO）

**v1.1 修订（F1）**：v1.0 误判要新建 `PluginBusEventFrame` / `PluginBusEventSubscribeOptions`，实际仓库已有 [PluginBusEventMessage](../../../Src/Netor.Cortana.Networks/WebSockets/Serialization/WebSocketJsonContext.cs#L93)（含 `type/protocol/version/topic/requestId/op/source/target/timestamp/eventType/payload` 11 字段）已注册 JsonContext。本方案**仅在该 record 上新增 2 个可选字段**：

```csharp
// Src/Netor.Cortana.Networks/WebSockets/Serialization/WebSocketJsonContext.cs:93-127
internal sealed record PluginBusEventMessage
{
    [JsonPropertyName("type")]            public string Type { get; init; } = string.Empty;
    [JsonPropertyName("protocol")]        public string Protocol { get; init; } = string.Empty;
    [JsonPropertyName("version")]         public string Version { get; init; } = string.Empty;
    [JsonPropertyName("topic")]           public string Topic { get; init; } = string.Empty;
    [JsonPropertyName("requestId")]       public string? RequestId { get; init; }
    [JsonPropertyName("op")]              public string? Op { get; init; }
    [JsonPropertyName("source")]          public string? Source { get; init; }
    [JsonPropertyName("target")]          public string? Target { get; init; }
    [JsonPropertyName("timestamp")]       public long? Timestamp { get; init; }
    [JsonPropertyName("eventType")]       public string EventType { get; init; } = string.Empty;
    [JsonPropertyName("payload")]         public JsonElement Payload { get; init; }

    // ★ 新增（v1.4.0）：plugin→plugin 转发跳数兜底
    [JsonPropertyName("hopCount")]        public int? HopCount { get; init; }

    // ★ 新增（v1.4.0）：与 source 互补，推荐插件填这个
    [JsonPropertyName("sourcePluginId")]  public string? SourcePluginId { get; init; }
}
```

约束：

- 新增属性类型限制在 `int?` / `string?`（已 AOT 验证）
- `Payload` 维持 `JsonElement` 透传，宿主**不**反序列化为业务类型
- 不新建 `PluginBusEventFrame` / `PluginBusEventSubscribeOptions` 等额外 POCO（v1.2 误判，v1.3 已纠正）

### 8.2 WebSocketJsonContext 注册

`PluginBusEventMessage` 已在 [WebSocketJsonContext.cs:15](../../../Src/Netor.Cortana.Networks/WebSockets/Serialization/WebSocketJsonContext.cs#L15) 注册：

```csharp
[JsonSerializable(typeof(PluginBusEventMessage))]   // ← 已存在，不要重复
internal partial class WebSocketJsonContext : JsonSerializerContext;
```

新增 `HopCount` / `SourcePluginId` 后源生成器自动产出新 metadata，**无需追加 `[JsonSerializable]` 行**。

### 8.3 实际使用方式

宿主侧大部分场景**不需要**反序列化为 POCO —— `BroadcastDispatcher.HandleAsync` 直接用 `JsonElement` 读字段（详见 §2.3 `ReadString` / `TryParse`），性能更好且零反射。POCO 仅在以下场景使用：

- 单元测试构造测试帧时（`JsonSerializer.Serialize(frame, WebSocketJsonContext.Default.PluginBusEventMessage)`）
- 现有 [Relay 服务](../../../Src/Netor.Cortana.Networks/WebSockets/Relays/) 已经在用此 POCO 构造 host→plugin event 帧（不变）
- 工具 / 调试代码生成示例帧

### 8.4 AOT 验证（v1.5 R-AOT-CMD 修订）

**事实纠正**：`Netor.Cortana.Networks` / `Netor.Cortana.Plugin` / `Netor.Cortana.Entitys` 都是 ClassLibrary（无 `OutputType=Exe`、无 `RuntimeIdentifier`、无 `PublishAot` 配置），**不能直接 `dotnet publish -p:PublishAot=true`**——会因缺可执行入口失败。库的 AOT 兼容性应通过 `<IsAotCompatible>true</IsAotCompatible>` + `EnableAotAnalyzer` / `EnableTrimAnalyzer` 在 build 阶段验证；真正 `publish` 验证落到可执行项目矩阵。

**库级（build 时验证 AOT analyzer）**：

```powershell
dotnet build Src/Netor.Cortana.Networks/Netor.Cortana.Networks.csproj -c Release
dotnet build Src/Netor.Cortana.Plugin/Netor.Cortana.Plugin.csproj -c Release
dotnet build Src/Netor.Cortana.Entitys/Netor.Cortana.Entitys.csproj -c Release
```

要求三个 csproj 都设置 `<IsAotCompatible>true</IsAotCompatible>`，build 0 trim/AOT warning。

**可执行项目级（publish 验证）**：

```powershell
# 主程序
dotnet publish Src/Netor.Cortana.UI/Netor.Cortana.UI.csproj -c Release -r win-x64

# Native Host
dotnet publish Src/Plugins/Netor.Cortana.NativeHost/Netor.Cortana.NativeHost.csproj -c Release

# voice 三件套（AOT 强约束）
dotnet publish Plugins/Src/Cortana.Plugins.Voice.Kws.Sherpa/Cortana.Plugins.Voice.Kws.Sherpa.csproj -c Release
dotnet publish Plugins/Src/Cortana.Plugins.Voice.Stt.Sherpa/Cortana.Plugins.Voice.Stt.Sherpa.csproj -c Release
dotnet publish Plugins/Src/Cortana.Plugins.Voice.Tts.Melo/Cortana.Plugins.Voice.Tts.Melo.csproj -c Release
```

每条命令必须 **0 个 trim warning、0 个 AOT warning**。任一新增 warning 必须在 PR 内修掉。可一次性走仓库统一发布脚本（如 `Build/publish.ps1` 或 `Plugins/Tools/Publish/`，PR 实施时确认入口），避免遗漏环节。

---

## 九、与 README 的对照索引

| 本文档章节 | 对应 README 章节 |
| --- | --- |
| §一 文件清单 | §3.1 协议层 + §3.2 宿主转发层 |
| §二 PluginBusBroadcastDispatcher | §2.4 宿主转发器 |
| §三 VoiceEventBridge | §5.4 VoiceEventDispatcher 重写 |
| §四 PluginManifestRegistry | §3.2（弱声明校验依据） |
| §五 SubscriptionRegistry 扩展 | §3.2 + §2.2 subscribe 帧扩展 |
| §六 服务接入 | §3.2 + §5.4 |
| §七 测试矩阵 | §3.2（测试文件位置） |
| §八 AOT | §3.6 AOT 改动清单（F1 复用） |

---

**v1.1 修订（2026-06-14）**：与 README v1.3 同步，按 F1-F7 修订：复用 `PluginBusEventMessage` / op 优先 eventType 兜底 / source==host 排除 / `PluginManifestRegistry` 改 DI 单例 + 真实 PluginLoader 接入点 / 后台投递 + per-subscriber 1 秒超时 / `voice.tts.greeting_ready` + `voice.tts.error` 显式 case；测试矩阵加 T6 / T7 / T8 / T14 / V10 / V11；§3.3 op 表对齐真实 voice 三件套发布点。

**v1.2 修订（2026-06-14）**：与 README v1.4 同步，按源码核验结果修订 7 条：F-LIFETIME（同步阶段重写 hopCount + snapshot clientIds + voice 桥接，后台只用 owned 数据）/ F-PRIORITY（1.4.0 有意变更优先级到 op > eventType > type，op 与 eventType 冲突时以 op 为权威）/ F-DIRECT（不为老 voice 直发式加兜底分支，三件套同 PR 升级，grace 期仅面向第三方插件）/ F-SCHEMA（`plugin.json` 对象数组 + `PublishedOpDeclaration` / `SubscribedOpDeclaration` record + `Register` 签名保留 `IEnumerable<string>`）/ F-LOADER（注册挂 `LoadProcessPluginAsync` line 416-420 + `LoadNativePluginAsync` line 445-449，注销挂 `UnloadPluginByPath` line 730-753，`UnloadAllPlugins` 加 `Clear()`，顶层方法不单独挂）/ F-FIELDCOUNT（README §2.1 字段计数订正为 2 个）/ F-SOURCE（v1 路由仅信任 clientId，弱声明从 `sourcePluginId` 读、缺省 fallback `source`，仅 LogWarning 不作安全依据）；测试矩阵加 T15 / T16；`PluginManifestRegistry` 加 `Clear()` 方法。

**最后更新**：2026-06-14（v1.3：与 README v1.5 同步，落地 OQ1-OQ5 + Top 高危 + 中危补丁——PluginManifestRegistry 改放 Plugin 程序集 / chat.\* 重命名 conversation.chat.\* / 受保护 op 黑名单 / 1MB 字节上限 / 死链清理 / NormalizeVoiceOp / AOT 命令矩阵 / 文件清单补全）

**v1.2**：2026-06-14（与 README v1.4 同步，基于源码核验修订 7 条）

**v1.1**：2026-06-14（与 README v1.3 同步，基于源码核验修订 7 条）

**初稿 v1**：2026-06-14（宿主转发器实施级骨架，含 v1.2 时期的几条事实级错误，已被 v1.1 / v1.2 取代）
