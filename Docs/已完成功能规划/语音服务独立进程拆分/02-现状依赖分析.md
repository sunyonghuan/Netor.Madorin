# 02 - 现状依赖分析

> 本文档与代码事实对齐于 2026-06-01。涉及行号以当前 `madorin` 分支为准；能力发现、设置项和 PluginBus 语音事件映射已经开始落地。

## Voice 模块文件清单

项目：[Src/Netor.Cortana.Voice/](Src/Netor.Cortana.Voice/)

依赖 NuGet（[Netor.Cortana.Voice.csproj:17-21](Src/Netor.Cortana.Voice/Netor.Cortana.Voice.csproj#L17-L21)）：

- `org.k2fsa.sherpa.onnx` 1.13.2
- `NAudio.WinMM` 2.3.0
- `Netor.EventHub` 1.2.8

依赖项目：`Netor.Cortana.Entitys`

**重要事实修正**：`Src/Netor.Cortana.Voice.Audio` 项目目前**不存在**，音频 P/Invoke (`AotWavePlayer.cs` / `AotWaveRecorder.cs`) 直接在 `Netor.Cortana.Voice` 项目内。本系列文档原稿在多处引用了 `Netor.Cortana.Voice.Audio`，应理解为"如需共享给插件，需要新抽出的共享库"，而非既有项目。

| 文件 | 职责 | 拆分去向 |
| --- | --- | --- |
| [WakeWordService.cs](Src/Netor.Cortana.Voice/WakeWordService.cs) | KeywordSpotter 唤醒词检测 + 麦克风长期监听 | **KWS 插件**（与 README / 01 / 05 一致；本字段在原稿中误填"留主进程"，已修正） |
| [SpeechRecognitionService.cs](Src/Netor.Cortana.Voice/SpeechRecognitionService.cs) | Paraformer Streaming 流式识别 + 编排逻辑 | 引擎核心→STT 插件；编排→主进程新建的 `VoiceOrchestrator` |
| [TextToSpeechService.cs](Src/Netor.Cortana.Voice/TextToSpeechService.cs) | MeloTTS VITS 合成 + 双 Channel 流水线 + 播放 + 问候语缓存 | **TTS 插件** |
| [VoiceChatOutputChannel.cs](Src/Netor.Cortana.Voice/VoiceChatOutputChannel.cs) | AI token → 断句 → `OnTtsEnqueue` 事件 | **替换**为 `TtsPluginOutputChannel`（命名沿用 01 文档；原稿出现的 `TtsProxyOutputChannel` 别名同义） |
| [VoiceInputChannel.cs](Src/Netor.Cortana.Voice/VoiceInputChannel.cs) | 订阅 `OnSttFinal` → `chatEngine.SendMessageAsync` | **替换**为 `SttPluginInputChannel`（同上） |
| [IVoiceCoordinator.cs](Src/Netor.Cortana.Voice/IVoiceCoordinator.cs) | 取消所有语音/AI 任务统一入口 | 接口形态需要扩充，详见 [07-接口与命名变更.md](07-接口与命名变更.md) |
| [AotWavePlayer.cs](Src/Netor.Cortana.Voice/AotWavePlayer.cs) | winmm `waveOut*` P/Invoke 播放 | **TTS 插件**（如不抽共享库则插件内复制一份） |
| [AotWaveRecorder.cs](Src/Netor.Cortana.Voice/AotWaveRecorder.cs) | winmm `waveIn*` P/Invoke 录制 | **KWS 与 STT 插件各一份**；麦克风在 Win32 通常不可同时被两个进程独占，需在 03 文档协议层定义让渡 |
| [SegmentedWaveProvider.cs](Src/Netor.Cortana.Voice/SegmentedWaveProvider.cs) | 分段音频提供器（字幕同步） | **TTS 插件** |
| [VoiceServiceExtensions.cs](Src/Netor.Cortana.Voice/VoiceServiceExtensions.cs) | DI 扩展 `AddCortanaVoice()` | **改造**：注销旧 `WakeWordService` / `SpeechRecognitionService` / `TextToSpeechService` / `VoiceInputChannel` / `VoiceChatOutputChannel`，改为注册插件适配器 |
| [VoiceChatService.cs](Src/Netor.Cortana.Voice/VoiceChatService.cs) | 空壳类（未使用） | **删除** |

## 详细依赖关系图

### SpeechRecognitionService（耦合最重，拆分核心）

```text
SpeechRecognitionService
  ├── 构造注入
  │   ├── ILogger<SpeechRecognitionService>
  │   ├── IAppPaths                    ← 模型路径
  │   ├── SystemSettingsService        ← 读取 STT 参数
  │   ├── IPublisher                   ← 发布事件
  │   ├── ISubscriber                  ← 订阅事件
  │   ├── TextToSpeechService          ← ⚠️ 直接调 PlayGreetingAsync/Stop
  │   ├── IAiChatEngine                ← ⚠️ 直接调 CancelCurrentTask
  │   └── IWindowController            ← ⚠️ 判断 IsMainWindowVisible（同时是会议模式互斥的现状机制）
  │
  ├── 订阅事件
  │   ├── Events.OnWakeWordDetected    ← 触发：取消旧轮次 + 播问候 + 启动STT
  │   └── Events.OnChatCompleted       ← 触发：续杯（条件：窗口不可见 + 轮次未取消）
  │
  ├── 发布事件
  │   ├── Events.OnSttPartial          ← 识别中间结果
  │   ├── Events.OnSttFinal            ← 识别最终结果
  │   └── Events.OnSttStopped          ← 识别会话结束
  │
  └── 读取配置
      ├── SherpaOnnx.Rule1MinTrailingSilence
      ├── SherpaOnnx.Rule2MinTrailingSilence
      ├── SherpaOnnx.Rule3MinUtteranceLength
      └── SherpaOnnx.RecognitionTimeoutSeconds
```

**⚠️ 标记的 3 个依赖是拆分的核心障碍**，它们使 STT 引擎承担了编排职责。其中 `IWindowController.IsMainWindowVisible()` 也是当前会议模式不会和语音对话互相打架的隐性约束（会议视图存在时主窗一定可见 → 续杯被自动门控掉）。拆分后这条门控必须显式迁到 `VoiceOrchestrator` 与 `TtsPluginOutputChannel`，否则会议中可能误触发续杯/朗读。

### TextToSpeechService

```text
TextToSpeechService
  ├── 构造注入
  │   ├── ILogger<TextToSpeechService>
  │   ├── IAppPaths                    ← 模型路径
  │   ├── SystemSettingsService        ← 读取 Tts.Speed / Tts.WelcomeGreeting
  │   ├── IPublisher                   ← 发布事件
  │   └── ISubscriber                  ← 订阅事件
  │
  ├── 订阅事件
  │   ├── Events.OnTtsEnqueue          ← 收到句子文本，入队合成
  │   └── Events.OnTtsFinish           ← AI 推理完成，没有后续文本了
  │
  ├── 发布事件
  │   ├── Events.OnTtsStarted          ← 流水线启动
  │   ├── Events.OnTtsSubtitle         ← 当前播放的句子（字幕）
  │   ├── Events.OnTtsCompleted        ← 全部播放完成
  │   └── Events.OnChatCompleted       ← 完整对话流程结束
  │
  └── 读取配置
      ├── Tts.Speed
      └── Tts.WelcomeGreeting
```

**无 ⚠️ 标记**：TTS 自身耦合度低，通过事件驱动，拆分相对安全。问候语缓存目前由 TTS 服务自身维护，迁入插件后即与插件包绑定，主程序无法直接读写音频缓存（详见 03 文档"问候语缓存归属"）。

### WakeWordService

```text
WakeWordService
  ├── 构造注入
  │   ├── ILogger<WakeWordService>
  │   ├── IAppPaths                    ← 模型路径
  │   ├── SystemSettingsService        ← 读取 KWS 参数
  │   └── IPublisher                   ← 发布事件
  │
  ├── 发布事件
  │   └── Events.OnWakeWordDetected    ← 检测到唤醒词
  │
  └── 读取配置
      ├── SherpaOnnx.KeywordsThreshold
      ├── SherpaOnnx.KeywordsScore
      └── SherpaOnnx.NumTrailingBlanks
```

**自身无外部强依赖，但与 STT 共享麦克风**：当前实现里 KWS 和 STT 由同一个进程调度，winmm `waveIn*` 在两个 service 之间是串行打开/关闭的；拆成两个进程后必须在协议层引入"让麦"约定，详见 [03-通信协议.md](03-通信协议.md)。

### VoiceChatOutputChannel

```text
VoiceChatOutputChannel : IAiOutputChannel
  ├── 构造注入
  │   ├── ILogger
  │   ├── IPublisher                   ← 发布 OnTtsEnqueue / OnTtsFinish
  │   └── IWindowController            ← IsActive = !IsMainWindowVisible()
  │
  └── 实现方法
      ├── OnTokenAsync()               ← AI token → 断句 → OnTtsEnqueue
      ├── OnDoneAsync()                ← flush + OnTtsFinish
      ├── OnCancelledAsync()           ← clear + OnTtsFinish
      └── OnErrorAsync()               ← clear + OnTtsFinish
```

**拆分后替换为 `TtsPluginOutputChannel`**：

- 已新增 `TtsPluginAdapter` 与 `TtsPluginOutputChannel`，`OnTokenAsync` → 断句 → 调 TTS 插件 `voice_tts_enqueue`
- `OnDoneAsync` → 调 TTS 插件 `voice_tts_finish`
- `OnCancelledAsync` / `OnErrorAsync` → 调 TTS 插件 `voice_tts_stop`
- `IsActive` 仍由 `IWindowController.IsMainWindowVisible()` 决定，会议模式语义保持等价
- `VoiceChatOutputChannel` 不再注册为默认输出通道：当 `Voice.Tts.Enabled=false` 或没有可用 `voice.tts` 插件时，默认软件不朗读 AI 回复

### VoiceInputChannel

```text
VoiceInputChannel : IAiInputChannel, IHostedService
  ├── 构造注入
  │   ├── ILogger
  │   ├── IAiChatEngine                ← ⚠️ 直接调 SendMessageAsync
  │   ├── IPublisher
  │   └── ISubscriber                  ← 订阅 Events.OnSttFinal
  │
  └── 行为
      └── 收到 OnSttFinal → chatEngine.SendMessageAsync()
```

**拆分后替换为 `SttPluginInputChannel`**：仍订阅 `Events.OnSttFinal`（事件载荷由 `SttPluginAdapter` 从 WS 帧映射而来），收到后调用 `chatEngine.SendMessageAsync`。

### VoiceCoordinator

```text
VoiceCoordinator : IVoiceCoordinator
  ├── 构造注入
  │   ├── SpeechRecognitionService     ← ⚠️ 直接调 CancelCurrentRound()
  │   └── IPublisher                   ← 发布 OnSttStopped
  │
  └── 方法
      └── CancelEverything()
          ├─ stt.CancelCurrentRound()  ← 内部取消 AI + TTS
          └─ publish OnSttStopped
```

**拆分后改造**：`CancelEverything()` → `SttPluginAdapter.cancel()` + `TtsPluginAdapter.stop()` + `chatEngine.CancelCurrentTask()` + `publish OnSttStopped`。原本由 STT 内部"顺手"做的 TTS/AI 取消必须显式拉到 Coordinator，详细签名见 [07-接口与命名变更.md](07-接口与命名变更.md)。

## 主程序消费 Voice 模块的位置

| 文件 | 引用方式 | 说明 |
| --- | --- | --- |
| [App.axaml.cs:280](Src/Netor.Cortana.UI/App.axaml.cs#L280) | `.AddCortanaVoice()` | DI 注册，启动入口（注意：原稿写作 `AddMadorinVoice()`，实际方法名为 `AddCortanaVoice()`） |
| `MainWindow.axaml.cs:1260` | `IVoiceCoordinator.CancelEverything()` | 用户点停止 |
| `MainWindow.axaml.cs:1315` | `IVoiceCoordinator.CancelEverything()` | 用户点发送 |
| `GlobalUsings.cs` | `global using Netor.Cortana.Voice;` | 命名空间引用 |
| `Netor.Cortana.UI.csproj` | `ProjectReference` | 项目引用 |

## Networks 层消费 Voice 事件

`WebSocketEventRelayService.cs` 订阅以下事件并广播到前端 WebSocket：

| EventHub 事件 | WS 广播类型 |
| --- | --- |
| `OnSttPartial` | `stt_partial` |
| `OnSttFinal` | `stt_final` |
| `OnSttStopped` | `stt_stopped` |
| `OnTtsStarted` | `tts_started` |
| `OnTtsSubtitle` | `tts_subtitle` |
| `OnTtsCompleted` | `tts_completed` |
| `OnChatCompleted` | `chat_completed` |
| `OnWakeWordDetected` | `wakeword_detected` |

**拆分后这些全部不变**——当前已新增 `PluginBusVoiceEventDispatcher`，由 PluginBus 收到独立进程的 `voice.*` WS 消息后转发到 EventHub，Networks 层继续订阅 EventHub 事件广播。对前端 WebSocket 客户端完全透明。

## Entitys 层定义的语音相关接口和事件

### 接口（均在 `Netor.Cortana.Entitys/Interfaces/`）

| 接口 | 实现者 | 拆分影响 |
| --- | --- | --- |
| `IAiOutputChannel` | `VoiceChatOutputChannel` → `TtsPluginOutputChannel` | 接口不变，实现替换。本系列文档原稿出现的 `TtsProxyOutputChannel` 与 `TtsPluginOutputChannel` 同义，统一以 01 文档命名为准 |
| `IAiInputChannel` | `VoiceInputChannel` → `SttPluginInputChannel` | 接口不变，实现替换。同义别名 `SttProxyInputChannel` 已弃用 |
| `IAiChatEngine` | `AiChatHostedService`（AI 层） | 不变 |
| `IWindowController` | UI 层 | 不变 |
| `IVoiceCoordinator` | `VoiceCoordinator` | 接口形态需要扩充（新增 `CancelEverythingAsync` 异步重载等），见 [07-接口与命名变更.md](07-接口与命名变更.md) |

### 事件定义（`Events.cs`）

所有语音事件 ID 和参数类型定义在 Entitys 层，**全部保留不变**：

```text
OnWakeWordDetected  → VoiceSignalArgs
OnSttPartial        → VoiceTextArgs
OnSttFinal          → VoiceTextArgs
OnSttStopped        → VoiceSignalArgs
OnTtsStarted        → VoiceSignalArgs
OnTtsSubtitle       → VoiceTextArgs
OnTtsCompleted      → VoiceSignalArgs
OnChatCompleted     → VoiceSignalArgs
OnTtsEnqueue        → TtsEnqueueArgs    ← 拆分后主进程内部可能不再使用
OnTtsFinish         → VoiceSignalArgs    ← 拆分后主进程内部可能不再使用
```

`OnTtsEnqueue` 和 `OnTtsFinish` 原来是 AI→TTS 的进程内事件驱动，
拆分后改为 `TtsPluginAdapter` 直接调用 TTS 插件的 `voice_tts_enqueue` / `voice_tts_finish` 工具，这两个事件在主进程内可能变为冗余，但为了兼容性暂时保留。

## 代码事实核对（2026-06-01）

本次对 01–06 文档与现网代码做了一次全量对齐，列出原稿与代码不一致的关键点，作为后续修订依据。

| 项 | 原稿描述 | 代码事实 | 处理 |
| --- | --- | --- | --- |
| `Src/Netor.Cortana.Voice.Audio` 项目 | 在 01 / 06 文档中作为既有项目引用 | `Src/` 下不存在该目录，音频 P/Invoke 在 `Netor.Cortana.Voice` 内 | 视为"未来可能抽出的共享库"，引用全部加注 |
| `WakeWordService` 拆分去向 | 02 原稿写"留主进程" | README/01/05 一致写"拆为 KWS 插件" | 02 已与其余文档对齐 |
| `AddMadorinVoice()` | 02 原稿引用此方法名 | 实际方法名为 `AddCortanaVoice()`（[VoiceServiceExtensions.cs:16](Src/Netor.Cortana.Voice/VoiceServiceExtensions.cs#L16)） | 已修正 |
| 命名 `TtsProxyOutputChannel` / `SttProxyInputChannel` | 02 原稿命名 | 01 / 05 文档命名为 `TtsPluginOutputChannel` / `SttPluginInputChannel` | 全部统一为 `*PluginInputChannel` / `*PluginOutputChannel`，旧名作为别名记录在 07 文档 |
| 计划提及的"主进程类型"（`VoiceOrchestrator` / `VoiceCapabilityRegistry` / `KwsPluginAdapter` / `SttPluginAdapter` / `TtsPluginAdapter` / `SttPluginInputChannel` / `TtsPluginOutputChannel`） | 文档把它们当既有/封装组件叙述 | `VoiceCapabilityRegistry` 已新增；`VoiceOrchestrator`、各插件 Adapter、`SttPluginInputChannel`、`TtsPluginOutputChannel` 尚未实现 | 01 文档使用 DONE/NEW/EXISTING 标注 |
| PluginManifest 的 `capabilities` 字段 | 03 文档"短期可用 tags 替代" | [PluginManifest.cs](Src/Netor.Cortana.Plugin/Core/PluginManifest.cs) 已支持 `capabilities` 与 `tags`；运行时可通过 `VoiceCapabilityRegistry` 识别语音能力 | 已完成 schema 前置改造，后续插件可直接声明 `capabilities`，也可短期用 `tags` 兼容 |
| 语音插件事件入口 | 原方案要求新增或复用宿主 WS 消息入口 | 已新增 `PluginBusVoiceEventDispatcher` 并接入 `WebSocketPluginBusServerService.HandleMessageAsync` | `voice.kws/stt/tts.*` 事件已能映射到既有 EventHub 事件；仍需假插件/测试入口实测 |
| 麦克风资源共享 | 文档把 KWS 录音和 STT 录音画为两个独立、并存的进程 | winmm `waveIn*` 在 Win32 不可被两个进程同时独占；当前实现里两者是同一进程串行打开/关闭 | 03 文档新增"麦克风让渡协议"小节，约束 `voice_kws_pause` / `voice_kws_resume` 时机 |
| 编排流程"KWS 唤醒 → 调 STT cancel" | 01 文档原稿如此 | 唤醒时 STT 还未启动，真正需要打断的是上一轮 TTS / AI | 01 文档已修正动作目标 |
| 会议模式与语音通道的耦合 | 文档零提及 | `VoiceChatOutputChannel.IsActive` 与 `SpeechRecognitionService.cs:94` 都依赖 `IWindowController.IsMainWindowVisible()` 来回避会议中触发；会议视图属于主窗 → 当前是天然规避 | 在新通道里复制此门控；05 文档增加"会议模式回归"测试项 |
| 主程序模型来源 | 06 原稿"主程序发布包不再内置 sherpa_models" | 现况是 [Netor.Cortana.UI.csproj:131,140,149](Src/Netor.Cortana.UI/Netor.Cortana.UI.csproj#L131) 中以 `Always`/`PreserveNewest` 复制 `Src/sherpa_models/{KWS,STT,TTS}` | 在 05 Step 8 明确 csproj 改造范围 |
| `IVoiceCoordinator` 接口签名 | 02 原稿"接口不变，实现改造" | 实际签名仅一个同步 `void CancelEverything()`（[IVoiceCoordinator.cs:11-19](Src/Netor.Cortana.Voice/IVoiceCoordinator.cs#L11-L19)），插件化后包含 WS 往返，改异步更合适 | 在 07 文档列出推荐新增 `CancelEverythingAsync(CancellationToken)` 重载 |
