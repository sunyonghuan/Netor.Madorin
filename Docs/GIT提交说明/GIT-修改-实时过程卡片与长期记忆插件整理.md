# GIT-修改-实时过程卡片与长期记忆插件整理

## 修改范围

- `Src/Netor.Madorin.AI/`
- `Src/Netor.Madorin.UI/`
- `Src/Netor.Madorin.Entitys/`
- `Src/Netor.Madorin.Networks/`
- `Src/Netor.Madorin.Plugin/`
- `Plugins/Src/Madorin.Plugins.Memory/`
- `Docs/`
- `Plugins/docs/`

## 修改内容

### 1. 实时过程输出与 UI 过程卡片

- 新增实时过程输出接口与相关模型。
- 增加 UI 侧实时过程卡片控件与句柄逻辑。
- 调整 `AiChatHostedService` 的对话过程事件输出。
- 调整 UI 聊天输出通道，支持过程状态与最终回复协同显示。
- 调整主窗口消息和会话相关逻辑，适配实时过程展示。

### 2. 系统设置与插件授权调整

- 调整系统设置服务与设置页逻辑。
- 调整插件授权页面相关行为。
- 调整应用启动和 Prompt 服务相关注册逻辑。
- 优化 PowerShell 内置工具提供器行为。
- 调整 WebSocket 插件总线服务细节。

### 3. 长期记忆插件增强

- 调整长期记忆插件配置模型与启动逻辑。
- 增加插件级记忆设置文件与存储配置。
- 扩展记忆数据库配置接口和实现。
- 增强记忆写入工具处理器与存储能力。
- 调整记忆供应服务与设置服务，支持更完整的插件配置和写入流程。

### 4. 文档目录整理

- 将历史规划、未来版本策划、参考文档、已完成功能规划等文档重新归类。
- 整理 `Docs` 与 `Plugins/docs` 下的大量旧文档位置。
- 删除旧路径下的冗余文档，改由新目录结构承载。
- 补充文档分类说明与杂项记录。

## 影响说明

- 本次提交包含代码、插件和文档目录整理，改动范围较大。
- 文档存在大量路径迁移和旧文件删除，应以新目录结构为准。
- 实时过程卡片和长期记忆插件相关代码需要后续重点验证 UI 展示、设置持久化、插件写入和 WebSocket 过程事件链路。
