# 工具调用聚合折叠卡片 : 82%

## Step 1 公共聚合控件设计与落地 : 100%

- [x] 新增 `ToolProcessGroupCard.axaml`，实现外层折叠卡片布局（Header + 折叠 Body）。
- [x] 新增 `ToolProcessGroupCard.axaml.cs`，提供 `AddTool` / `TryGetTool` / `UpdateTool` / `CancelRunningTools` / `CompleteIfIdle` API，全部以 `RealtimeProcessCardHandle` 为对外句柄。
- [x] 在 `AddTool` 内对子卡 `RootBorder.Margin` 重置为 `0,0`，避免双层 50,0 缩进；不修改 `RealtimeProcessCard` 控件本身。
- [x] 实现聚合摘要：总数、运行中、成功、失败、取消、总耗时（取子卡 `DurationMs` 累加）。
- [x] 实现折叠策略：完成且无失败/取消时自动折叠；存在失败或取消时保持展开；用户手动操作后保护其选择。
- [x] 在 `ChatView` 上新增 `AddToolProcessGroupCard(string groupId)` 入口，沿用 `Margin=0,-18,0,0` 容器策略，并由 `MainWindow` 透传。
- [x] 在 `MeetingView` 上新增 `AddToolProcessGroupCard(ToolProcessGroupCard card)` 入口，沿用 `Margin=0,-18,0,0` 容器策略。

## Step 2 专家模式实时接入 : 100%

- [x] 在 `UiChatOutputChannel` 新增 `_currentTurnId`、`_toolGroupsByTurnId`、`_toolGroupsByProcessId` 字段。
- [x] 在收到第一个带非空 `TurnId` 的事件时落账 `_currentTurnId`。
- [x] 将 `Kind == tool/command/agent` 过程事件按 `TurnId → _currentTurnId → ProcessId` 顺序选键导入聚合卡。
- [x] 保持 `Kind == thinking` 走原有独立 `RealtimeProcessCard` 路径。
- [x] 处理工具结果先到（无对应子卡）的占位创建—立即完成路径。
- [x] `OnDoneAsync`：对所有聚合卡调 `CompleteIfIdle`。
- [x] `OnCancelledAsync`：先 `CancelRunningTools`，再 `CompleteIfIdle`。
- [x] `OnErrorAsync`：与 `OnCancelledAsync` 同语义收口。
- [x] `Reset`：清空 `_toolGroupsByTurnId` / `_toolGroupsByProcessId` / `_currentTurnId`。
- [x] 验证插件链路（无 TurnId）走 ProcessId 退化路径，不抛异常、不出现异常聚合卡。
- [x] 确认专家模式历史回放不新增聚合逻辑。

## Step 3 会议模式实时接入 : 100%

- [x] 在 `MeetingViewController` 新增 `_toolGroupsByMessageId`、`_toolHandlesByCallKey`、`_toolStopwatches` 字段，移除原 `_toolCards`。
- [x] `OnMeetingToolCall`：获取或创建 `MessageId` 聚合卡 → `AddTool` 拿到 Handle → 启动 Stopwatch。
- [x] `OnMeetingToolResult`：停止 Stopwatch，写入耗时，更新对应子卡状态（success/failed），刷新聚合摘要。
- [x] `CompleteProcessCards(messageId)`：对该消息聚合卡调 `CompleteIfIdle`，思考卡保持原行为。
- [x] `OnMeetingMessageDiscarded`：从消息流移除 bubble、聚合卡容器、思考卡容器；清理三个字典中对应条目。
- [x] `OnMeetingMessagePartialSaved`：调 `CompleteProcessCards`，让聚合卡正常收口。
- [x] AskUser / Pause 不主动收口运行中工具，等待恢复后 `OnMeetingToolResult` 自然完成。
- [x] 保持 `OnMeetingThinkingDelta` 思考卡逻辑独立。

## Step 4 会议历史回放可选对齐 : 100%

- [x] 评估是否将 `RenderHistoryMessage` 中的 `ToolCallsJson` 回放改为聚合卡。
- [x] 若执行历史对齐：把同一条会议消息的历史工具调用一次性塞入聚合卡并设为完成态；历史子卡 **不写入** `_toolHandlesByCallKey`，避免与未来实时调用撞键。
- [x] 历史卡入口若有耗时数据，直接读 `MeetingToolCallRecord.DurationMs`。
- [x] 若暂不执行历史对齐：在方案 §7 标注为后续优化。（已执行历史对齐，本项不适用）

## Step 5 验证与收尾 : 22%

- [x] 运行 UI 项目编译验证。
- [x] 运行 `Tests/Netor.Cortana.MeetingMode.Tests` 工程。
- [ ] 手动验证专家模式多工具调用只出现一张聚合卡。
- [ ] 手动验证专家模式工具失败 → 聚合卡保持展开 + 失败计数。
- [ ] 手动验证专家模式取消 → 运行中工具记为 cancelled + 聚合卡保持展开。
- [ ] 手动验证插件链路（无 TurnId）退化为独立卡，无异常聚合。
- [ ] 手动验证会议模式同一消息多工具调用只出现一张聚合卡。
- [ ] 手动验证会议消息丢弃后，聚合卡和思考卡同时从消息流移除。
- [ ] 手动验证工作模式展示不变。

## 修改文件清单

- `Src/Netor.Cortana.UI/Controls/Common/ToolProcessGroupCard.axaml`
- `Src/Netor.Cortana.UI/Controls/Common/ToolProcessGroupCard.axaml.cs`
- `Src/Netor.Cortana.UI/Controls/Common/RealtimeProcessCard.axaml.cs`
- `Src/Netor.Cortana.UI/Controls/Common/RealtimeProcessCardHandle.cs`
- `Src/Netor.Cortana.UI/Controls/ExpertMode/ChatView.axaml.cs`
- `Src/Netor.Cortana.UI/Views/MainWindow.axaml.cs`
- `Src/Netor.Cortana.UI/Channels/UiChatOutputChannel.cs`
- `Src/Netor.Cortana.UI/Controls/Meeting/MeetingView.axaml.cs`
- `Src/Netor.Cortana.UI/Controls/Meeting/MeetingViewController.cs`
- `Docs/未来版本策划/工具调用聚合折叠卡片/执行计划(工具调用聚合折叠卡片).md`

## 验证记录

- 2026-06-07：`dotnet build Src/Netor.Cortana.UI/Netor.Cortana.UI.csproj --no-restore` 通过，0 警告，0 错误。
- 2026-06-07：`dotnet test Tests/Netor.Cortana.MeetingMode.Tests/Netor.Cortana.MeetingMode.Tests.csproj --no-restore` 通过，72 个测试全部通过。
