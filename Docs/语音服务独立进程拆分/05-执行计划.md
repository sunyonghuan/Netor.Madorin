# 05 - 执行计划（语音服务独立进程拆分） : 0%

## Step 1 创建共享协议库 Netor.Cortana.Voice.Protocol : 0%

> 安全等级：🟢 安全（纯新建，零破坏）

- [×] 新建类库项目 `Src/Netor.Cortana.Voice.Protocol/`
- [×] 定义 `VoiceServiceMessage` record（type + data）
- [×] 定义 STT 命令/事件常量类 `SttProtocol`
- [×] 定义 TTS 命令/事件常量类 `TtsProtocol`
- [×] 创建 `VoiceProtocolJsonContext : JsonSerializerContext`（AOT 安全）
- [×] 添加到 slnx，编译验证

**输入**：03-通信协议.md 中的消息类型定义
**输出**：可编译的共享协议类库，无任何业务依赖

---

## Step 2 新增语音服务开关设置项 : 0%

> 安全等级：🟢 安全（仅新增配置，不影响现有行为）

- [×] 在 `SystemSettingsService.SeedDefaults()` 添加 3 个 `Voice.*.Enabled` 设置项
- [×] 在 `WakeWordService.StartAsync` 添加开关检查（关闭时跳过启动）
- [×] 编译验证
- [×] 手动测试：关闭唤醒开关后重启，确认不再监听麦克风

**输入**：04-设置项设计.md 中的 Seed 定义
**输出**：设置页面出现三个开关，WakeWord 开关立即生效

---

## Step 3 创建共享音频库 Netor.Cortana.Voice.Audio（可选） : 0%

> 安全等级：🟢 安全（提取复用，不改变行为）

- [×] 新建类库项目 `Src/Netor.Cortana.Voice.Audio/`
- [×] 移入 `AotWavePlayer.cs`、`AotWaveRecorder.cs`、`SegmentedWaveProvider.cs`
- [×] 移入 P/Invoke `WinMmNative`、`WAVEFORMATEX`、`IAudioDataProvider` 等
- [×] Voice 项目改为引用此库，编译验证
- [×] 确认 WakeWord（主进程）和未来 STT/TTS 进程都可引用

**输入**：现有 Voice 项目中的音频相关文件
**输出**：音频 P/Invoke 独立为共享库，原有功能不变

---

## Step 4 新建 Cortana.Stt 独立进程 : 0%

> 安全等级：🟢 安全（纯新建，与现有代码并存）

- [×] 新建 Console App 项目 `Src/Cortana.Stt/`
- [×] 实现 WebSocket Server（`System.Net.WebSockets` 原生，AOT 安全）
- [×] 移植 STT 引擎核心（从 `SpeechRecognitionService` 提取纯识别部分）：
  - 模型加载（`EnsureModelLoaded`）
  - 录音（`AotWaveRecorder`）
  - 识别循环（`RecognitionLoop` 去除编排逻辑）
- [×] 实现 `start`/`cancel`/`config` 命令处理
- [×] 实现 `partial`/`final`/`stopped`/`error` 事件推送
- [×] 支持命令行参数 `--port 12858 --model-dir <path>`
- [×] 独立编译运行验证
- [×] 手动测试：用 WebSocket 客户端工具连接，发 start，说话，收到 final

**输入**：`SpeechRecognitionService.cs` 中的引擎代码 + 协议库
**输出**：可独立运行的 `Cortana.Stt.exe`

---

## Step 5 新建 Cortana.Tts 独立进程 : 0%

> 安全等级：🟢 安全（纯新建，与现有代码并存）

- [×] 新建 Console App 项目 `Src/Cortana.Tts/`
- [×] 实现 WebSocket Server
- [×] 移植 TTS 引擎核心（从 `TextToSpeechService` 提取）：
  - 模型加载（`EnsureModelReadyAsync`）
  - 双 Channel 流水线（`SynthesizeLoopAsync` + `PlaybackLoopAsync`）
  - 断句 + 文本清洗（`SplitSentences` + `SanitizeForTts`）
  - 问候语缓存（`_greetingAudioCache`）
- [×] 实现命令处理：`speak`/`enqueue`/`finish`/`stop`/`greeting_play`/`greeting_regen`/`config`
- [×] 实现事件推送：`started`/`subtitle`/`completed`/`error`
- [×] `greeting_play` 内部先 stop 再播放
- [×] 支持命令行参数 `--port 12859 --model-dir <path>`
- [×] 独立编译运行验证
- [×] 手动测试：WS 客户端发 enqueue + finish，确认播放 + subtitle 回传

**输入**：`TextToSpeechService.cs` 中的引擎代码 + 音频库 + 协议库
**输出**：可独立运行的 `Cortana.Tts.exe`

---

## Step 6 主程序新增代理层和编排器 : 0%

> 安全等级：🟡 中等风险（新增代码，暂不替换现有注册）

- [×] 新增 `SttProxyService`：WS 客户端连接 STT 进程
  - 自动重连（指数退避）
  - 收消息 → EventHub 事件转发
  - 提供 `SendCommandAsync(type, data)` 方法
- [×] 新增 `TtsProxyService`：WS 客户端连接 TTS 进程（同上）
- [×] 新增 `TtsProxyOutputChannel : IAiOutputChannel`
  - `OnTokenAsync` → 断句 → WS `enqueue`
  - `OnDoneAsync` → WS `finish`
  - `OnCancelledAsync` → WS `stop`
  - `IsActive` → `!IsMainWindowVisible() && ttsProxy.IsConnected`
- [×] 新增 `SttProxyInputChannel : IAiInputChannel, IHostedService`
  - 订阅 SttProxy 的 `final` 事件 → `chatEngine.SendMessageAsync`
- [×] 新增 `VoiceOrchestrator : IHostedService`
  - 订阅 `OnWakeWordDetected` → 编排完整流程
  - 订阅 TTS `completed` → 决定续杯或结束
  - 管理轮次 CancellationToken
- [×] 改造 `VoiceCoordinator`：`CancelEverything()` 通过 Proxy 发命令
- [×] 新增 `VoiceProcessManager : IHostedService`
  - 读取 `Voice.Stt.Enabled` / `Voice.Tts.Enabled` 开关
  - 启动/监控/重启子进程
  - 优雅关闭
- [×] 编译验证（此时新旧代码并存，不冲突）

**输入**：协议库 + 02-现状依赖分析.md 中的事件映射表
**输出**：完整的代理层代码，尚未接入 DI

---

## Step 7 切换注册 + 移除旧代码 + 全流程测试 : 0%

> 安全等级：🔴 高风险（替换核心链路）

- [×] 改造 `VoiceServiceExtensions.AddCortanaVoice()`：
  - 保留：`WakeWordService`（加开关检查）
  - 替换：`SpeechRecognitionService` → `SttProxyService` + `SttProxyInputChannel`
  - 替换：`TextToSpeechService` → `TtsProxyService` + `TtsProxyOutputChannel`
  - 替换：`VoiceChatOutputChannel` → `TtsProxyOutputChannel`
  - 替换：`VoiceInputChannel` → `SttProxyInputChannel`
  - 新增：`VoiceOrchestrator`、`VoiceProcessManager`
- [×] 从主程序 csproj 移除 `org.k2fsa.sherpa.onnx` 中 STT/TTS 相关依赖
  （注意：KWS 仍需要 sherpa.onnx，可能需要保留整个包或拆分引用）
- [×] 编译通过
- [×] 全流程测试清单：
  - [ ] 唤醒 → 问候 → 识别 → AI → 合成播放 → 续杯
  - [ ] 中途打断（二次唤醒）
  - [ ] 用户点停止按钮
  - [ ] 用户点发送按钮（发送时取消语音）
  - [ ] TTS subtitle 气泡显示正确
  - [ ] Networks WebSocket 广播正确（前端收到所有事件）
  - [ ] 关闭 KWS 开关 → 不监听麦克风
  - [ ] 关闭 STT 开关 → 不启动 Stt.exe
  - [ ] 关闭 TTS 开关 → 不启动 Tts.exe，AI 仅文字回复
  - [ ] 全部关闭 → 纯文字模式
  - [ ] STT 进程崩溃 → 主程序自动重启
  - [ ] TTS 进程崩溃 → 主程序自动重启
- [×] 清理旧代码（确认无回归后）：
  - 删除旧 `SpeechRecognitionService`（引擎代码已在 Stt 进程中）
  - 删除旧 `TextToSpeechService`（引擎代码已在 Tts 进程中）
  - 删除旧 `VoiceChatOutputChannel`
  - 删除旧 `VoiceInputChannel`
  - 删除空壳 `VoiceChatService.cs`

**输入**：Step 1~6 的所有成果
**输出**：完整的三进程语音架构，通过全部测试

---

## 修改文件清单（预估）

完成后在此处列出所有实际修改过的文件。

（执行完成后填写）
