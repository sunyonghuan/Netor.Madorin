# 阶段 1 执行计划（止血 + turn-aware 契约） : 100%

> 关联文档：[03-阶段路线.md](03-阶段路线.md) §阶段 1、[04-链路修复矩阵.md](04-链路修复矩阵.md) §7.1 / §7.2.A、[阶段0-实施前对齐.md](阶段0-实施前对齐.md) §二 / §五
> 当前状态：实施中
> 目标：先止住“取消后必须重启”和“旧 turn 污染新 turn”的灾难，再一次性把 `IAiOutputChannel` 升级为编译期强制的 turn-aware 契约。

## Step 1 边界对齐与实施约束确认 : 100%
- [√] 已核对阶段 1 范围：仅做 5s 超时止血、turn 终止事件去重、`IAiOutputChannel` 强制 turnId、`IRealtimeProcessOutput` 保持签名不变
- [√] 已核对 4 条实施纪律：不删公共契约、不改 `Events.cs`、不重写 `ChatHistoryDataProvider`、不改 `IRealtimeProcessOutput` 接口签名
- [√] 已核对现状代码触点：`AiChatHostedService`、4 个 `IAiOutputChannel` 实现、1 个 `IRealtimeProcessOutput` 实现

## Step 2 接口与通道改造 : 100%
- [√] 修改 `IAiOutputChannel`：所有输出方法首参增加 `string turnId`，删除旧签名
- [√] 同步改造 `UiChatOutputChannel`：按 turnId 隔离流式文本、工具卡片和取消拦截
- [√] 同步改造 `WebSocketChatOutputChannel` / `TtsPluginOutputChannel` / `VoiceChatOutputChannel`
- [√] 保持 `NonExpertSuppressingRealtimeOutput` 与 `IRealtimeProcessOutput` 签名不变，仅继续透传 `evt.TurnId`

## Step 3 AiChatHostedService 止血改造 : 100%
- [√] 给 `WaitForCurrentRunToStopAsync` 增加 5s 超时，超时后强制释放运行态并记录告警
- [√] 为当前 turn 增加终止事件去重守卫，保证 `OnConversationTurnCompleted` 同一 turnId 只发一次
- [√] 所有输出通道调用补齐 turnId 透传，覆盖聊天 / 图片 / 视频三条路径
- [√] 取消通知链路改为基于当前 turnId 下发，避免旧 turn 的取消回调污染新 turn

## Step 4 验证与收尾 : 100%
- [√] `dotnet build Netor.Cortana.slnx -p:UseSharedCompilation=false`
- [√] 已补充阶段 2/选择状态专项测试：`ChatAgentResolverTests`，覆盖默认解析、Provider/Model 联动、工具上下文失效
- [√] 回填本计划进度与已修改文件清单

## 已修改文件
- [√] `Src/Netor.Cortana.Entitys/Interfaces/IAiOutputChannel.cs`
- [√] `Src/Netor.Cortana.UI/Channels/UiChatOutputChannel.cs`
- [√] `Src/Netor.Cortana.Networks/WebSockets/Channels/WebSocketChatOutputChannel.cs`
- [√] `Src/Netor.Cortana.Voice/TtsPluginOutputChannel.cs`
- [√] `Src/Netor.Cortana.Voice/VoiceChatOutputChannel.cs`
- [√] `Src/Netor.Cortana.AI/AiChatHostedService.cs`
- [√] `Src/Netor.Cortana.AI/ChatAgentResolver.cs`
- [√] `Tests/Netor.Cortana.AI.Tests/Chat/ChatAgentResolverTests.cs`
- [√] `Docs/已完成功能规划/对话取消重发状态机修复/06-阶段1执行计划.md`

## 当前验证结论
- [√] 2026-06-13 已执行：`dotnet build Netor.Cortana.slnx -p:UseSharedCompilation=false`
- [√] 结果：通过，`0` 警告，`0` 错误
- [√] 2026-06-13 已执行：`dotnet test Tests/Netor.Cortana.AI.Tests/Netor.Cortana.AI.Tests.csproj --filter "FullyQualifiedName~ChatAgentResolverTests"`
- [√] 结果：通过，`5` 通过，`0` 失败
- [×] 仍待补第一阶段 UI 串流隔离 / 取消慢断 / turn 终止去重专项测试；当前已从“仅编译验证”提升到“编译 + resolver 自动化测试”
