# 阶段 0 — 实施前对齐（基准快照 + 链路行为基准）

> 子文档：[对话取消重发问题 — 架构债拆解方案](README.md) v7.4 系列之阶段 0
> 状态：**实施第一行代码前必须完成** — 这是阶段 5 验收对照的基准
> 上一篇：[README.md](README.md)（入口/导航）
> 下一篇：[03-阶段路线.md](03-阶段路线.md) 阶段 1
> 配套结构档：[对话引擎核心链路架构图.md](对话引擎核心链路架构图.md)

---

## 一、定位

阶段 0 不写一行代码，只做三件事：

1. **截图保存公共契约签名快照** — 阶段 5 完成后，diff 必须为零
2. **录制 13 条核心链路当前行为基准** — 阶段 5 完成后，逐条对照"行为不变 / 行为修复"
3. **团队对齐 4 条实施纪律** — 全部签字确认后才能进入阶段 1

**阶段 0 完成判定**（4 项全部 ✅ 才能进入阶段 1）：

- [ ] §二 公共契约签名基准快照已截图归档
- [ ] §三 外部调用方依赖快照已截图归档
- [ ] §四 13 条核心链路当前行为基准全部录制完成
- [ ] §五 实施纪律对齐确认（4 条硬约束）团队签字

预估工作量：**0.5–1 天**（不算实施时间，纯准备）。

---

## 二、公共契约签名基准快照

> **如何使用**：阶段 0 用 IDE / `git show` / `dotnet` 反编译工具截图保存当前签名；阶段 5 完成后用同样方式提取再 diff。**Diff 必须为零**才能触发整体迁移。

### 2.1 IAiChatEngine 接口（5 个公共成员，签名一字不动）

来自 [Src/Netor.Cortana.Entitys/Interfaces/IAiChatEngine.cs](../../../Src/Netor.Cortana.Entitys/Interfaces/IAiChatEngine.cs)：

```csharp
public interface IAiChatEngine
{
    Task SendMessageAsync(string userInput, CancellationToken cancellationToken,
        List<AttachmentInfo>? attachments = null, List<AgentMention>? mentions = null);

    Task NewSessionAsync();

    void Stop();

    void CancelCurrentTask();

    Task CancelCurrentTaskAsync(CancellationToken cancellationToken = default);
}
```

**阶段 5 验收**：以上 5 个签名一字不动；任何方法签名变化 = 阶段 5 不通过。

### 2.2 AiChatHostedService 公共方法（11 个，签名一字不动）

来自 [Src/Netor.Cortana.AI/AiChatHostedService.cs](../../../Src/Netor.Cortana.AI/AiChatHostedService.cs)：

```csharp
public sealed class AiChatHostedService(...) : IAiChatEngine, IHostedService, IDisposable
{
    public Task StartAsync(CancellationToken cancellationToken);              // line 101
    public async Task StopAsync(CancellationToken cancellationToken);         // line 125
    public void ChangeProvider(string providerId);                            // line 135
    public void ChangeModel(string modelId);                                  // line 144
    public void ChangeAgent(string agentId);                                  // line 158
    public async Task NewSessionAsync();                                      // line 167
    public async Task ResumeSessionAsync(string sessionId);                   // line 200
    public void Stop();                                                       // line 228
    public void CancelCurrentTask();                                          // line 240
    public async Task CancelCurrentTaskAsync(CancellationToken cancellationToken = default);  // line 249
    public async Task SendMessageAsync(string userInput, CancellationToken cancellationToken,
        List<AttachmentInfo>? attachments = null, List<AgentMention>? mentions = null);       // line 259
    public async Task GenerateImageAsync(string prompt, CancellationToken cancellationToken); // line 726
    public async Task GenerateVideoAsync(string prompt, CancellationToken cancellationToken); // line 952
    public void Dispose();                                                    // line 2071
}
```

> 实际公共方法 14 个（含 IHostedService Start/Stop + Dispose）。**阶段 5 验收**：以上 14 个签名一字不动。`Generate*` 在阶段 3.7 内部转发到 `ChatImage/VideoTurnExecutor`，签名不变。

### 2.3 AiChatHostedService 公共访问器（6 个，签名一字不动）

```csharp
public bool IsRunning => _isRunning;                                          // line 67
public string? CurrentSessionId => string.IsNullOrEmpty(_sessionId) ? null : _sessionId;  // line 74
public string? CurrentWorkspaceId => appPaths.WorkspaceDirectory.Md5Encrypt();// line 79
public AiProviderEntity? CurrentProvider => _currentProvider;                 // line 84
public AgentEntity? CurrentAgent => _currentAgent;                            // line 89
public AiModelEntity? CurrentModel => _currentModel;                          // line 94
```

**阶段 5 验收**：6 个访问器对外签名一字不动；阶段 2/3.4 之后内部实现变成 `=> _agentResolver.CurrentX` / `=> _sessionService.CurrentId` 转发，但签名层面零变化。

### 2.4 IAiOutputChannel 接口（破坏性变更前的基准）

来自 [Src/Netor.Cortana.Entitys/Interfaces/IAiOutputChannel.cs](../../../Src/Netor.Cortana.Entitys/Interfaces/IAiOutputChannel.cs)：

```csharp
public interface IAiOutputChannel
{
    string Name { get; }
    bool IsActive { get; }

    Task OnTokenAsync(string token, string sessionId, CancellationToken cancellationToken = default);
    Task OnDoneAsync(string sessionId, CancellationToken cancellationToken = default);
    Task OnCancelledAsync();
    Task OnErrorAsync(string message, CancellationToken cancellationToken = default);
}
```

**阶段 1 后的目标签名**（破坏性变更）：

```csharp
public interface IAiOutputChannel
{
    string Name { get; }
    bool IsActive { get; }

    Task OnTokenAsync(string turnId, string token, string sessionId, CancellationToken cancellationToken);
    Task OnDoneAsync(string turnId, string sessionId, CancellationToken cancellationToken);
    Task OnCancelledAsync(string turnId);
    Task OnErrorAsync(string turnId, string message, CancellationToken cancellationToken);
}
```

**阶段 1 验收**：4 个 IAiOutputChannel 源码实现（UiChatOutputChannel / WebSocketChatOutputChannel / TtsPluginOutputChannel / VoiceChatOutputChannel）全部按新签名编译通过。

### 2.5 IRealtimeProcessOutput 接口（基准 = 不变）

来自 [Src/Netor.Cortana.Entitys/Interfaces/IRealtimeProcessOutput.cs](../../../Src/Netor.Cortana.Entitys/Interfaces/IRealtimeProcessOutput.cs)：

```csharp
public interface IRealtimeProcessOutput
{
    Task OnProcessEventAsync(RealtimeProcessEvent evt, CancellationToken ct = default);
}
```

**阶段 5 验收**：签名一字不动。`UiChatOutputChannel` / `NonExpertSuppressingRealtimeOutput` 实现端按 `evt.TurnId` 字段路由（详见 [05-兼容性与决策.md §8.2](05-兼容性与决策.md)）。

### 2.6 EventHub 事件契约（基准 = 不变）

来自 [Src/Netor.Cortana.Entitys/Events.cs](../../../Src/Netor.Cortana.Entitys/Events.cs)：

```csharp
// 阶段 5 验收：以下 4 类事件结构一字不动
public static ConversationTurnStartedEvent OnConversationTurnStarted = new("conversation.turn.started");
public static ConversationUserMessageEvent OnConversationUserMessage = new("conversation.user.message");
public static ConversationAssistantDeltaEvent OnConversationAssistantDelta = new("conversation.assistant.delta");
public static ConversationTurnCompletedEvent OnConversationTurnCompleted = new("conversation.turn.completed");

// ConversationTurnStatus 枚举字段保持
public enum ConversationTurnStatus { Succeeded, Cancelled, Failed }

// ConversationTurnCompletedArgs 字段保持（含 Status / ErrorMessage）
```

**阶段 5 验收**：`git diff Src/Netor.Cortana.Entitys/Events.cs` 阶段 0 vs 阶段 5 = 空。新增 EventType / 改字段 = 阶段 5 不通过。

### 2.7 ChatHistoryDataProvider 公共方法（基准 = 不变）

来自 [Src/Netor.Cortana.AI/Providers/ChatHistoryDataProvider.cs](../../../Src/Netor.Cortana.AI/Providers/ChatHistoryDataProvider.cs)：

```csharp
public sealed class ChatHistoryDataProvider(...) : ChatHistoryProvider, IDisposable
{
    // AF 钩子（重写基类）
    protected override async ValueTask StoreChatHistoryAsync(InvokedContext context, CancellationToken cancellationToken = default);  // line 58
    protected override async ValueTask<IEnumerable<AIChatMessage>> ProvideChatHistoryAsync(InvokingContext context, CancellationToken cancellationToken = default);  // line 381

    // 公共方法
    public async Task SavePartialResponseAsync(string partialResponse, AgentSession? session, AIAgent? agent,
        string modelName = "Unknown", string? messageId = null, CancellationToken cancellationToken = default);  // line 208
    public async Task AppendWorkflowFinalReportAsync(string sessionId, string content, string? sourceTaskId,
        ...);  // line 283
}
```

**阶段 5 验收**：以上 4 个签名一字不动；阶段 3.1 抽 `ChatMessagePersistence` 是**薄包装**，内部仍调这些方法。

---

## 三、外部调用方依赖快照（23 处外部调用一字不动）

> **如何使用**：阶段 0 用 `grep` / `git ls-files | xargs grep` 取得每处调用的实际行号；阶段 5 完成后再次取得，验证依赖关系完整保留。

### 3.1 IAiChatEngine 方法外部调用（11 处，签名不变）

```text
# 命令：grep -rn "chatEngine\.\(SendMessage\|Cancel\|Stop\|NewSession\|ResumeSession\)" Src --include="*.cs"
# 阶段 0 输出：
Src/Netor.Cortana.Networks/WebSockets/Channels/WebSocketInputChannel.cs:69:    chatEngine.Stop();
Src/Netor.Cortana.Voice/IVoiceCoordinator.cs:27:                                chatEngine.CancelCurrentTask();
Src/Netor.Cortana.Voice/SpeechRecognitionService.cs:345:                       chatEngine.CancelCurrentTask();
Src/Netor.Cortana.Voice/VoicePipelineCoordinator.cs:159:                        chatEngine.CancelCurrentTask();
# UI 层调用（_chatService 字段名）：
Src/Netor.Cortana.UI/ViewModels/ExpertMode/ChatInputVm.cs:323:    await _chatService.GenerateImageAsync(text, cancellationToken);
Src/Netor.Cortana.UI/ViewModels/ExpertMode/ChatInputVm.cs:327:    await _chatService.GenerateVideoAsync(text, cancellationToken);
Src/Netor.Cortana.UI/ViewModels/ExpertMode/ChatInputVm.cs:331:    await _chatService.SendMessageAsync(text, cancellationToken, attachments);
Src/Netor.Cortana.UI/ViewModels/ExpertMode/ChatInputVm.cs:353:    await _chatService.CancelCurrentTaskAsync(cancellationToken);
Src/Netor.Cortana.UI/ViewModels/ExpertMode/ChatInputVm.cs:302:    _chatService.ChangeAgent(_selectedAgent.Id);
Src/Netor.Cortana.UI/ViewModels/ExpertMode/ChatInputVm.cs:304:    _chatService.ChangeProvider(_selectedProvider.Id);
Src/Netor.Cortana.UI/ViewModels/ExpertMode/ChatInputVm.cs:306:    _chatService.ChangeModel(_selectedModel.Id);
Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:378:    await _chatService.NewSessionAsync();
# MainWindow Sessions
Src/Netor.Cortana.UI/Views/Main/MainWindow.Sessions.cs:97:        _ = Task.Run(() => chatService.ResumeSessionAsync(sessionId));
```

**阶段 5 验收**：上述行号每处仍然能编译通过；`grep` 匹配数量不变。

### 3.2 AiChatHostedService 公共访问器外部调用（12 处）

```text
# 命令：grep -rn "_chatService\.Current\|chatService\.Current" Src --include="*.cs"
# 阶段 0 输出（已核对）：

CurrentSessionId（1 处）：
  Src/Netor.Cortana.AI/WorkMode/CurrentSessionResolver.cs:18

CurrentWorkspaceId（6 处）：
  Src/Netor.Cortana.AI/WorkMode/CurrentSessionResolver.cs:23
  Src/Netor.Cortana.UI/ViewModels/Meeting/MeetingInputVm.cs:396
  Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:421
  Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:573
  Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:586
  Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:646

CurrentAgent（2 处）：
  Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:406
  Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:698

CurrentProvider（2 处）：
  Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:407
  Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:702

CurrentModel（2 处）：
  Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:408
  Src/Netor.Cortana.UI/ViewModels/WorkMode/WorkModeInputVm.cs:711
```

**阶段 5 验收**：每处调用仍然能编译；`grep -c` 数量不变（13 行匹配）。

### 3.3 IAiOutputChannel DI 注册（3 处运行时注册 / 4 处源码实现）

```text
# 阶段 0 输出（已核对）：

DI 注册（3 处）：
  Src/Netor.Cortana.UI/App.axaml.cs:322                AddSingleton<IAiOutputChannel>(... UiChatOutputChannel)
  Src/Netor.Cortana.Networks/Extensions/NetworkServiceExtensions.cs:37  AddSingleton<IAiOutputChannel>(... WebSocketChatOutputChannel)
  Src/Netor.Cortana.Voice/VoiceServiceExtensions.cs:22  AddSingleton<IAiOutputChannel>(... TtsPluginOutputChannel)

源码实现（4 处）：
  Src/Netor.Cortana.UI/Channels/UiChatOutputChannel.cs
  Src/Netor.Cortana.Networks/WebSockets/Channels/WebSocketChatOutputChannel.cs
  Src/Netor.Cortana.Voice/TtsPluginOutputChannel.cs
  Src/Netor.Cortana.Voice/VoiceChatOutputChannel.cs   ← DI 未注册但源码存在，阶段 1 仍要改
```

**阶段 1 验收**：4 个源码实现按新签名（带 turnId 首参）编译通过。

### 3.4 IRealtimeProcessOutput 调用方（插件层不改）

```text
# 阶段 0 输出（已核对）：

调用方：
  Src/Netor.Cortana.Plugin/BuiltIn/PowerShell/PowerShellProvider.cs:309
    _realtimeOutput.OnProcessEventAsync(new RealtimeProcessEvent { TurnId = string.Empty, ... }, ct);
  Src/Netor.Cortana.AI/AiChatHostedService.cs：内部 PublishRealtimeProcessEvent* 系列方法

实现方：
  Src/Netor.Cortana.UI/Channels/UiChatOutputChannel.cs        （主实现，按 evt.TurnId 路由）
  Src/Netor.Cortana.UI/Channels/NonExpertSuppressingRealtimeOutput.cs  （NonExpertProcessScope.Active 短路）
```

**阶段 5 验收**：调用方零改动（PowerShellProvider:309 行号不变）；实现方按 evt.TurnId 字段路由（不是首参）。

---

## 四、13 条核心链路当前行为基准

> **如何使用**：阶段 0 按下表逐条手动操作 + 录屏（或截屏）+ 文字记录"当前实际行为"；阶段 5 完成后用同步骤再做一次，对比预期 vs 实际。
>
> 录制位置建议：本仓库 `Docs/已完成功能规划/对话取消重发状态机修复/baseline-recordings/`（git LFS 管理或 `.gitignore` 后云盘备份；本文档不直接 commit 大文件）。

### 4.1 链路基准记录表（13 行）

| # | 链路 | 测试步骤 | 当前行为（阶段 0 录制） | 阶段 5 期望行为 | 录像/截图 |
| --- | --- | --- | --- | --- | --- |
| 1 | 取消传播 | 发消息 → AI 流式输出中点 Stop | 待录：必须重启？UI 卡死？输入框可用？ | 立即停 + 输入框可用 + 5s 内 publish Completed(Cancelled) | `link-1.mp4` |
| 2 | 流式输出 | 发消息 → 观察 UI 气泡逐字增长 | 待录：单 turn 正常增长 | 不变 + 取消旧 turn 后新 turn 不被污染 | `link-2.mp4` |
| 3 | 历史回灌 | 切换历史会话 → 第一条新消息 | 待录：AI 是否引用历史 | 不变（AF 钩子保留） | `link-3.mp4` |
| 4 | 持久化（正常） | 完整完成一轮对话 | 待录：DB 写入成功 | 不变（AF 钩子保留） | `link-4a.mp4` |
| 4b | 持久化（取消） | AI 流式中点 Stop → 查 DB ChatMessages | 待录：**绝大多数情况无部分回复行**（v7.3 §2.2 主路径丢失） | **必有部分回复行** + 下一轮 AI 引用 | `link-4b.mp4` |
| 5 | EventHub 事件总线 | Memory 插件订阅 OnConversationTurnCompleted | 待录：Memory 插件能收到事件 | 不变（事件结构一字不动） | `link-5.mp4` |
| 6 | 配置同步 | UI 切换 Provider → 发新消息 | 待录：新 Provider 生效；旧 turn 是否丢回复 | 新 Provider 生效 + 旧 turn 部分回复保留 | `link-6.mp4` |
| 7 | SessionCreated 联动 | UI 点击"新建会话" | 待录：UI 显示空白 + 工作模式监听到 | 不变（publish OnSessionCreated 跟随迁移） | `link-7.mp4` |
| 8 | Workflow→Chat 回灌 | 工作模式完成任务 → 回 Chat 看历史 | 待录：finalReport 写入 ChatMessages 表 + Memory 去重 | 不变（AppendWorkflowFinalReport 保留） | `link-8.mp4` |
| 9 | TTS 跨 turn | 主窗口隐藏 → 触发语音模式 → AI 回复 → 取消 | 待录：TTS 是否中断 | 不变（按 sessionId 整把停） | `link-9.mp4` |
| 10 | 唤醒词中断 | AI 流式中说唤醒词 | 待录：AI 是否被唤醒中断 | 不变（IAiChatEngine.CancelCurrentTask 保留） | `link-10.mp4` |
| 11 | Token 统计 | 完整对话后看 UI 进度条 | 待录：Token 数显示正确 | 不变（AIAgentFactory 不动） | `link-11.mp4` |
| 12 | 工具上下文版本 | 加载/卸载插件后发新消息 | 待录：AI 是否看到新工具集 | 不变（ChatAgentResolver 接管） | `link-12.mp4` |
| 13 | PluginBus 跨进程事件 | 外部插件订阅 conversation topic | 待录：插件能收到事件 | 不变（事件结构一字不动） | `link-13.mp4` |

### 4.2 关键边界 case 录制（必做）

除了 13 条主链路，**以下 4 个边界 case 必须单独录制**（这些是 v1-v7.3 演进中识别出的高频 bug 场景）：

| # | 边界 case | 测试步骤 | 阶段 0 现状 | 阶段 5 期望 |
| --- | --- | --- | --- | --- |
| E1 | **必须重启** | 发消息 → AI 流式中点 Stop → 立即输入新消息发送 | 待录：经常需要重启 | 输入框立即可用 + 新消息正常发送 |
| E2 | **取消重发丢回复** | 发消息 → AI 输出半句被 Stop → 输入新方向重发 | 待录：旧的半句**丢失**（v7.3 §2.2 主路径） | 旧的半句保留在 DB + UI + AI 看到 |
| E3 | **多次点击堆积** | 快速连击发送按钮 5 次 | 待录：可能堆积孤儿 task | SemaphoreSlim 互斥，只发一条 |
| E4 | **跨 turn 串流污染** | 发长消息 → AI 流式中点 Stop → 立即发新消息 | 待录：新气泡可能含旧 token | 新 turn 完全干净 |

### 4.3 录制实操脚本

```bash
# 1. 准备录制目录（不进入 git）
mkdir -p Docs/已完成功能规划/对话取消重发状态机修复/baseline-recordings/
echo "baseline-recordings/" >> Docs/已完成功能规划/对话取消重发状态机修复/.gitignore

# 2. 用录屏工具（OBS / Win+G / ScreenToGif）录制 13 + 4 = 17 段

# 3. 每段录完后填写本文档 §4.1 / §4.2 表格的"当前行为"列

# 4. 把"录像/截图"列的相对路径写实（如 link-1.mp4 → baseline-recordings/link-1.mp4）

# 5. 阶段 5 完成时用同样脚本再录一遍，文件名加 -after（如 link-1-after.mp4），左右对照
```

---

## 五、实施纪律对齐确认（4 条硬约束）

详见 [README.md §实施纪律](README.md)。**4 条全部签字才能进入阶段 1**：

- [ ] **纪律 1：公共契约只转发不删除** — 已阅读、已理解、签字：__________
  - 不删除 IAiChatEngine 的 5 个接口方法签名
  - 不删除 AiChatHostedService 的 14 个公共方法 + 6 个公共访问器签名
  - 阶段 5 验收通过 = 签名 diff 为零

- [ ] **纪律 2：EventHub 事件结构不动** — 已阅读、已理解、签字：__________
  - 不修改 Events.cs（不新增事件类型、不改 Args record 字段）
  - 5s 超时复用 OnConversationTurnCompleted(Failed, ErrorMessage)
  - 阶段 5 验收通过 = `git diff Events.cs` 为空

- [ ] **纪律 3：AF 历史钩子不重写** — 已阅读、已理解、签字：__________
  - ChatHistoryDataProvider 的 4 个公共方法（StoreChatHistoryAsync / ProvideChatHistoryAsync / SavePartialResponseAsync / AppendWorkflowFinalReportAsync）签名一字不动
  - 新增 ChatMessagePersistence 仅是 turn 取消路径的薄包装
  - 不新增任何形式的"DB → AgentSession 回灌"服务（v6 误判作废）

- [ ] **纪律 4：IRealtimeProcessOutput 单独纳入接口改造清单** — 已阅读、已理解、签字：__________
  - 接口签名一字不动（OnProcessEventAsync(evt, ct) 不改）
  - 实现端按 evt.TurnId 字段做路由
  - PowerShellProvider:309 等插件调用方零改动
  - 误改首参 = PR 拒绝

---

## 六、阶段 0 完成判定

**4 项全部 ✅ 才能进入阶段 1**：

- [ ] §二 公共契约签名基准快照已截图归档（建议存放 `Docs/已完成功能规划/对话取消重发状态机修复/baseline-snapshots/` + git 提交）
  - [ ] IAiChatEngine 接口签名截图
  - [ ] AiChatHostedService 公共方法 + 访问器截图
  - [ ] IAiOutputChannel 接口签名截图（破坏性变更前的基准）
  - [ ] IRealtimeProcessOutput 接口签名截图
  - [ ] Events.cs 全文截图（基准 = 不变）
  - [ ] ChatHistoryDataProvider 公共方法签名截图
- [ ] §三 外部调用方依赖快照已截图归档
  - [ ] 11 处 IAiChatEngine 方法调用 grep 输出
  - [ ] 12 处 AiChatHostedService 访问器调用 grep 输出
  - [ ] 4 + 3 个 IAiOutputChannel 实现/注册 grep 输出
- [ ] §四 13 条核心链路当前行为基准全部录制完成
  - [ ] 13 条主链路录像（4.1 表）
  - [ ] 4 个边界 case 录像（4.2 表）
- [ ] §五 实施纪律对齐确认 4 条全部签字

---

## 七、与阶段 1 的衔接

阶段 0 完成后，进入 [03-阶段路线.md 阶段 1](03-阶段路线.md)（止血 + 接口契约一次到位，0.5 周）。

阶段 1 的 PR 描述末尾必须显式 check 4 条实施纪律：

```markdown
## 实施纪律对齐确认（README §实施纪律）

- [ ] 纪律 1：本 PR 不删除 IAiChatEngine / AiChatHostedService 公共契约签名
- [ ] 纪律 2：本 PR 不修改 Events.cs（新增/改字段都不允许）
- [ ] 纪律 3：本 PR 不重写 ChatHistoryDataProvider 已有公共方法
- [ ] 纪律 4：本 PR 不改 IRealtimeProcessOutput 接口签名（仅修改实现端的 evt.TurnId 路由）
```

阶段 5 完成时，按本文档 §六 反向验证：

- §二 签名快照 diff = 0
- §三 grep 数量与阶段 0 一致
- §四 13 + 4 = 17 段录像逐条对照"行为不变 / 行为修复"

**阶段 0 是合同**，阶段 5 验收时违反 = 整轮重做。
