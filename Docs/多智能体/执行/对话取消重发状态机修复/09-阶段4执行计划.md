# 阶段 4 执行计划（VM 事件驱动） : 100%

> 关联文档：[03-阶段路线.md](03-阶段路线.md) §阶段 4、[04-链路修复矩阵.md](04-链路修复矩阵.md) §7.4
> 当前状态：实施中
> 目标：删除专家模式输入区的乐观运行态更新，改为由 `OnConversationTurnStarted` / `OnConversationTurnCompleted` 单源驱动 `IsRunning`，并为发送入口增加互斥，避免重复点击堆积孤儿任务。

## Step 1 边界对齐与现状确认 : 100%
- [√] 已核对阶段 4 范围：仅调整 `ChatInputVm` / `InputAreaView` 的运行态切换与发送互斥，不改工作模式 / 会议模式现有事件流
- [√] 已核对当前现状：`ChatInputVm` 仍订阅 `OnAiStarted` / `OnAiCompleted`，`CancelAsync` 仍乐观设置 `IsRunning=false`
- [√] 已核对视图触点：`InputAreaView.OnSendClick` 当前无互斥保护，Send/Stop 按钮显隐仍由 `IsRunning` / `IsTaskActive` 组合控制

## Step 2 专家模式 VM 事件单源改造 : 100%
- [√] `ChatInputVm` 改为订阅 `OnConversationTurnStarted` / `OnConversationTurnCompleted`
- [√] 删除 `CancelAsync` 中的乐观 `IsRunning=false`
- [√] 同步更新注释与运行态语义，确保图片 / 视频 / 聊天三条路径统一由 conversation turn 事件驱动

## Step 3 输入区发送互斥与按钮状态收口 : 100%
- [√] 为 `InputAreaView.OnSendClick` 增加 `SemaphoreSlim(1,1)` 互斥，避免重复点击并发提交
- [√] 保持 Send/Stop 按钮仍以单值 `IsRunning` / `IsTaskActive` 切换，不引入新的乐观状态源
- [√] 确认工作模式 / 会议模式不受本轮互斥与显隐调整影响
- [√] `MainWindow` 已删除专家模式不再需要的 `OnAiStarted` 空订阅，并将 `OnAiCompleted` 注释改为仅负责标题 / Token 刷新

## Step 4 验证与收尾 : 100%
- [√] 运行 AI 定向回归测试并确认无回归
- [√] 运行 `dotnet build Netor.Cortana.slnx -p:UseSharedCompilation=false`
- [√] 回填本文档进度、修改文件与验证结果

## 已修改文件
- [√] `Src/Netor.Cortana.UI/ViewModels/ExpertMode/ChatInputVm.cs`
- [√] `Src/Netor.Cortana.UI/Controls/ExpertMode/InputAreaView.axaml.cs`
- [√] `Src/Netor.Cortana.UI/Views/MainWindow.axaml.cs`
- [√] `Docs/已完成功能规划/对话取消重发状态机修复/09-阶段4执行计划.md`

## 当前验证结论
- [√] 2026-06-13 已执行：`dotnet test Tests/Netor.Cortana.AI.Tests/Netor.Cortana.AI.Tests.csproj --filter "FullyQualifiedName~ChatAgentResolverTests|FullyQualifiedName~AiChatHostedServiceTests|FullyQualifiedName~ChatSessionServiceTests|FullyQualifiedName~ChatTurnCancellationRegistryTests|FullyQualifiedName~ChatGenerationContextBuilderTests|FullyQualifiedName~ChatGenerationTurnCoordinatorTests"`
- [√] 结果：通过，`25` 通过，`0` 失败
- [√] 2026-06-13 已执行：`dotnet build Netor.Cortana.slnx -p:UseSharedCompilation=false`
- [√] 结果：通过，`0` 警告，`0` 错误
