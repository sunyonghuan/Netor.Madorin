# 阶段 2 执行计划（根除“留”主路径丢失） : 100%

> 关联文档：[03-阶段路线.md](03-阶段路线.md) §阶段 2、[04-链路修复矩阵.md](04-链路修复矩阵.md) §7.2.C / §7.2.E / §7.2.I、[06-阶段1执行计划.md](06-阶段1执行计划.md)
> 当前状态：实施中
> 目标：把默认 Provider / Agent / Model 选择态和 Agent 重建判定从 `AiChatHostedService` 收口到 `ChatAgentResolver`，确保取消重发后“留”路径不再因字段被清空而丢失部分回复。

## Step 1 边界对齐与现状确认 : 100%
- [√] 已核对阶段 2 范围：新增 `ChatAgentResolver`、`AiChatHostedService` 改薄转发、取消场景改用局部 `session/agent/model`
- [√] 已核对阶段 2 不动边界：不删 `IAiChatEngine` / `AiChatHostedService` 公共契约、不改 `Events.cs`、不重写 `ChatHistoryDataProvider`
- [√] 已核对主路径触点：`AiChatHostedService`、`ChatAgentResolver`、`ChatInputVm`、`WorkModeInputVm`、历史回灌 / 取消部分持久化链路

## Step 2 Resolver 主干收口 : 100%
- [√] 新增 `ChatAgentResolver`，接管默认 Provider / Agent / Model 选择态与工具上下文版本快照
- [√] `AiChatHostedService.StartAsync` 改为从 resolver 加载默认选择态
- [√] `CurrentProvider` / `CurrentAgent` / `CurrentModel` 与 `ChangeProvider` / `ChangeModel` / `ChangeAgent` 已改为薄转发
- [√] `NewSessionAsync` / `ResumeSessionAsync` / `SendMessageAsync` / 图片生成 / 视频生成已切到 resolver 参与解析

## Step 3 局部态与回归测试补齐 : 100%
- [√] turn 内聊天主路径已改为使用局部 `session/agent/model`，避免旧 turn catch 读取到被新默认值覆盖的字段
- [√] 补齐 `ChatAgentResolver`“复用 vs 重建”专项测试：同态复用、切换默认选择后重建、工具上下文变更后重建
- [√] 已补 `mentions.Count > 0` 时强制重建分支回归；`mentions.Count >= 2` 的 `BuildWithSubAgents` 路径留待后续多智能体专项测试补强

## Step 4 验证与收尾 : 100%
- [√] 已完成首轮主干验证：`ChatAgentResolverTests` 现有 5 条、`AiChatHostedServiceTests` 2 条、UI turn 隔离测试 4 条通过
- [√] 执行阶段 2 新增测试：`dotnet test Tests/Netor.Cortana.AI.Tests/Netor.Cortana.AI.Tests.csproj --filter "FullyQualifiedName~ChatAgentResolverTests"`
- [√] 已补跑 `ChatAgentResolverTests|AiChatHostedServiceTests` 组合验证，并回填本文档结果

## 已修改文件
- [√] `Src/Netor.Cortana.AI/ChatAgentResolver.cs`
- [√] `Src/Netor.Cortana.AI/AIServiceExtensions.cs`
- [√] `Src/Netor.Cortana.AI/AiChatHostedService.cs`
- [√] `Tests/Netor.Cortana.AI.Tests/Chat/ChatAgentResolverTests.cs`
- [√] `Docs/已完成功能规划/对话取消重发状态机修复/07-阶段2执行计划.md`

## 当前验证结论
- [√] 2026-06-13 已执行：`dotnet test Tests/Netor.Cortana.AI.Tests/Netor.Cortana.AI.Tests.csproj --filter "FullyQualifiedName~ChatAgentResolverTests|FullyQualifiedName~AiChatHostedServiceTests"`
- [√] 结果：通过，`11` 通过，`0` 失败
- [√] 2026-06-13 已执行：`dotnet test Tests/Netor.Cortana.AI.Tests/Netor.Cortana.AI.Tests.csproj --filter "FullyQualifiedName~ChatAgentResolverTests"`
- [√] 结果：通过，`9` 通过，`0` 失败
- [√] 2026-06-13 已执行：`dotnet build Netor.Cortana.slnx -p:UseSharedCompilation=false`
- [√] 结果：通过，`0` 警告，`0` 错误
- [√] 阶段 2 的 resolver 复用 / 重建行为测试已补齐，当前文档与代码进度一致
