# 08 — P4 补充：交互模型重设计（对话驱动 + 统一对话流）

> **文档定位**：基于实际测试反馈，对 P4 任务执行引擎的 **UI 交互模型** 进行重新设计。
> 本文档覆盖以下变更：
>
> 1. 废弃审批卡片模式，改为对话驱动
> 2. ~~双模态界面切换~~ → 统一对话流（所有内容在同一个 Feed 中按时间顺序呈现）
> 3. ~~危险工具授权的浮动意图面板~~ → 内联授权消息卡片
> 4. 执行结果展示（折叠式任务总结卡片 + 文件列表）
> 5. 文件保存位置策略
> 6. 任务生命周期重定义（永续对话循环）
>
> **创建日期**：2026-05-25
>
> **最后更新**：2026-05-27（正文已同步为实际代码实现，附录 A 已合并到正文）
>
> **前置文档**：`04-P4方案设计`、`06-P4补充-提示词与UI时间线设计`、`07-P4补充-资源管理与工具授权`
>
> **废弃内容**：本文档生效后，以下既有设计 **废弃**：
> - P2-4 动态子智能体创建审批卡片（`DynamicAgentCreationApprovalVm`、三按钮交互）
> - P2-4 HITL 审批卡片的"批准/修改/拒绝"按钮式计划确认
> - 任务完成即终态的假设

---

## 1. 问题背景（实测发现）

### 1.1 现有交互模式的问题

当前 P4 的 UI 交互是 **"弹卡片确认"** 模式：

```
用户输入任务 → [后台分析] → 弹出审批卡片（计划详情 + ✓批准/✎修改/✕拒绝）
                           → 弹出子智能体创建审批卡片（提示词/工具 + ✓批准/✕拒绝）
```

实测问题：

| 问题 | 影响 |
| ------ | ------ |
| 审批卡片打断对话流 | 用户无法以自然语言讨论和修改需求 |
| 子智能体创建要求用户审批 | 用户不关心内部调度细节，过度干预 |
| 计划确认只能通过按钮 | 无法表达"大致OK但步骤2改一下"这种混合意图 |
| 任务完成即终态 | 用户对结果有疑问或不满意时没有继续交互的入口 |

### 1.2 期望的交互体验

> **用户的整个体验就是"跟一个项目经理对话"。**
>
> 讨论需求 → 对话；确认方案 → 对话中自然表达；看执行进度 → 进度面板；
> 看完结果 → 回到对话继续聊；不满意 → 继续对话讨论修改再跑一轮；
> 满意了 → 对话中说"可以了"。
>
> **卡片/按钮/审批 = 打断对话自然流动的中间层，应当移除。**

---

## 2. 核心设计：统一对话流

### 2.1 设计原则

工作流 Tab 采用 **统一对话流** 设计，不做显式模态切换。所有内容（对话消息、时间线进度、
步骤卡片、工具授权、结果展示）统一在同一个 `FeedItems` 对话流中，通过不同 `Role` 渲染不同样式。

- 需求分析/计划制定阶段：对话消息（`Role="user"` / `"ai"`）自然出现在流中
- 执行阶段：时间线节点（`Role="progress"`）+ 步骤卡片（`Role="step_card"`）追加到流中
- 执行完成后：继续追加对话消息，无需"切回对话模式"

> **设计理由**：统一流比双模态切换实现更简单、体验更连贯。用户无需感知"当前是什么模式"，
> 所有信息按时间顺序自然流动。

`Events.OnWorkflowModeChanged` 事件定义保留（引擎仍在关键节点发布），但 UI 不消费该事件、
不做显式界面切换。

### 2.2 对话消息规格

**与普通 Chat 模式共用 Feed 组件，但不显示头像。** 用户消息与 AI 消息通过气泡/背景色区分，
不依赖分隔线。

```
┌──────────────────────────────────────────────────────────────┐
│                     工作流 - 统一对话流                          │
│                                                              │
│  ╔══════════════════════════════════════════════════════╗   │
│  ║ 帮我做一份 AI 芯片市场分析报告                         ║   │
│  ╚══════════════════════════════════════════════════════╝   │
│    （用户消息：半透明蓝色边框气泡，右对齐）                     │
│                                                              │
│  好的，我分析了一下你的需求，有几个问题想确认：                   │
│  1. 报告覆盖哪些地区？                                        │
│  2. 目标受众是投资者还是技术团队？                               │
│  3. 需要包含竞品对比吗？                                       │
│    （AI 消息：无气泡纯文本，左对齐）                            │
│                                                              │
│  ╔══════════════════════════════════════════════════════╗   │
│  ║ 全球市场，给投资者看的，要有竞品对比                    ║   │
│  ╚══════════════════════════════════════════════════════╝   │
│                                                              │
│  明白了。我初步规划如下（共 4 步）：                            │
│  **步骤 1：数据采集**                                        │
│  从公开财报中采集全球 AI 芯片市场数据（NVIDIA/AMD/Intel）       │
│                                                              │
│  **步骤 2：竞品对比**                                        │
│  横向对比主要厂商的市场份额、性能和价格...                       │
│                                                              │
│  确认开始执行吗？还是需要调整？                                  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ 输入想法...                                          │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

关键特征：

- **无头像**：不显示 AI / 用户头像，聚焦内容
- **气泡区分**：用户消息用半透明蓝色边框气泡，AI 消息为纯文本，视觉上自然区分
- **计划内容以 AI 消息形式展示**：计划生成后直接在对话流中呈现步骤列表
- **输入框始终在底部**：用户随时可以输入，执行期间也不灰显

### 2.3 执行阶段界面规格

执行阶段的内容追加到同一个对话流中，由**竖线时间线 + 折叠式步骤卡片**组成。

```
┌──────────────────────────────────────────────────────────────┐
│  ... 上方是对话消息（需求讨论 + 计划确认）...                     │
│                                                              │
│  ●─ ▶ 需求分析                                    （#cccccc） │
│  ●─ ✓ 需求分析完成                                （#cccccc） │
│  ●─ ▶ 计划制定                                    （#cccccc） │
│  ●─ ✓ 计划制定完成                                （#cccccc） │
│  ●─ ▶ 步骤 1：数据采集                            （#cccccc） │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ ▼ 步骤 1 执行详情                                   │     │
│  │ ┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈ │     │
│  │ [AI 输出的完整 Markdown 内容，内含 ScrollViewer]     │     │
│  │ MaxHeight=320px，超出可滚动                         │     │
│  │ 流式输出时末尾显示暗绿色光标 ▌                       │     │
│  └─────────────────────────────────────────────────────┘     │
│  ●─ ✓ 步骤 1 完成                                 （#cccccc） │
│     └ 采集到 47 条数据，涵盖 NVIDIA/AMD/Intel      （灰色副文本）│
│                                                              │
│  ●─ ▶ 步骤 2：竞品对比分析                        （#cccccc） │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ ▶ 步骤 2 执行详情（已折叠，点击展开）                │     │
│  └─────────────────────────────────────────────────────┘     │
│  ●─ ✓ 步骤 2 完成                                 （#cccccc） │
│                                                              │
│  ... 后续步骤 ...                                             │
│                                                              │
│  ●─ ✓ 验证通过（90/100）                           （#cccccc） │
│     └ [问题列表作为副文本]                                    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ ▼ 任务总结                                          │     │
│  │ [BuildFinalReport() 生成的完整 Markdown]             │     │
│  └─────────────────────────────────────────────────────┘     │
│                                                              │
│  ●─ ✓ 任务已完成                                   （#cccccc） │
│                                                              │
│  有什么需要调整的吗？（AI 自动追加的对话消息）                   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ 输入想法...                                          │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

**时间线节点规格：**

| 元素 | 说明 |
| ------ | ------ |
| 圆点 | 统一 `#cccccc` 浅灰色，阶段级 10px / 步骤级 8px |
| 竖线 | 1.5px `#333333`，贯穿时间线节点之间（最后一条只画上半段） |
| 标题行 | 13px，颜色统一 `#cccccc` |
| 副文本（`TimelineSubText`） | 11px 灰色小字，步骤完成时的 `ResultSummary` |
| 辅助状态行 | 重试/等待/跳过等保持 `#858585` |

**折叠式步骤卡片规格：**

| 元素 | 说明 |
| ------ | ------ |
| 角色 | `Role="step_card"` |
| 布局 | Grid 两列：左列虚线延续时间线，右列暗色折叠卡片 |
| 标题栏 | 可点击折叠/展开（`OnStepCardHeaderClick`） |
| 内容 | `MarkdownRenderer` + `ScrollViewer`（MaxHeight=320px） |
| 流式光标 | 暗绿 `#4A7A5A`，步骤执行中时显示 |
| 完成状态 | 自动折叠（`IsExpanded=false`），显示 ✓ 标记 |

**子过程内容来源：**

步骤执行期间，子智能体每轮输出（`onAiMessage` 回调，携带 `StepId`）通过
`TryRouteToStepCard()` 路由到对应的步骤卡片内部累积显示。
步骤完成后，`ResultSummary` 作为时间线 `StepEnd` 节点的副文本显示。

**相关代码：**
- `ConversationMessageVm.cs`：`Role="step_card"` + `StepId` / `CardTitle` / `IsExpanded` / `IsCardCompleted`
- `P4TaskDetailVm.cs`：`_activeStepId` / `_activeStepCard` / `TryRouteToStepCard()` / `CompleteStepCard()`
- `WorkflowDetailView.axaml`：`IsVisible="{Binding IsStepCard}"` 渲染分支

### 2.4 输入框行为

| 阶段 | 输入框状态 |
| ------ | ---------- |
| 对话/需求分析 | 可用，用户自由输入 |
| 执行中 | 可用，用户随时可以输入（输入会路由到永续对话处理） |
| 工具授权等待 | 可用（授权以内联卡片形式展示，不阻塞输入框） |
| 任务完成后 | 可用，支持永续对话 |

---

## 3. 对话驱动的意图识别

### 3.1 用户意图分类

对话模式中，用户的每条输入经过 LLM 意图识别，分为以下类别：

| 意图 | 示例 | 引擎行为 |
| ------ | ------ | --------- |
| `confirm_execute` | "开始"、"执行吧"、"确认"、"OK"、"就这样" | 切入执行模式 |
| `modify_plan` | "步骤2改成并行"、"加一步政策风险分析" | AI 修改计划后继续对话 |
| `question` | "政策风险是指什么？"、"步骤3具体做什么？" | AI 回答问题，留在对话 |
| `specify_path` | "报告保存到 D:/Reports/" | 记录路径偏好，继续对话 |
| `satisfied` | "可以了"、"满意"、"没问题了"、"结束吧" | 任务标记完成 |
| `cancel` | "不做了"、"取消" | 任务取消 |
| `other` | 任何不属于上述类别的输入 | AI 自行判断如何回应 |

### 3.2 意图识别实现策略

```
用户输入 → 编排主智能体（Orchestrator）
          ↓
        主智能体同时承担两个职责：
        1. 判断用户意图（基于对话上下文）
        2. 生成回复内容

        主智能体的输出格式：
        {
          "intent": "modify_plan",
          "response": "好的，已调整计划：...",
          "updatedPlan": { ... }   // 仅当 intent=modify_plan 时存在
        }
```

不需要单独的"意图分类器"——主智能体在理解上下文的过程中自然能判断意图。

### 3.3 对话上下文管理

对话模式中的所有消息（用户输入 + AI 回复）构成完整的对话历史。
切入执行模式后，执行过程的记录（步骤摘要）也会追加到上下文中。
切回对话模式时，AI 拥有完整的历史：需求讨论 + 执行过程 + 执行结果。

```
上下文结构：
  [对话] 用户：帮我做一份 AI 芯片市场分析报告
  [对话] AI：有几个问题想确认...
  [对话] 用户：全球市场，给投资者看的
  [对话] AI：规划如下：1. 采集数据 2. 竞品对比 3. 趋势预测 4. 撰写报告
  [对话] 用户：开始
  [执行] 步骤1 完成：采集到 47 条数据（摘要）
  [执行] 步骤2 完成：NVIDIA 78% / AMD 12% / Intel 8%（摘要）
  [执行] 步骤3 完成：市场趋势预测结论（摘要）
  [执行] 步骤4 完成：报告已生成（摘要）
  [对话] AI：执行完成，以下是结果...
  [对话] 用户：第三章数据来源是什么？    ← AI 可引用步骤1的结果回答
```

---

## 4. 危险工具授权：内联授权消息卡片

### 4.1 工具风险分级

| Level | 分类 | 行为 | 示例 |
| ------- | ------ | ------ | ------ |
| 0 | 安全 | 自动执行，不通知 | 文本生成、数据计算 |
| 1 | 低风险 | 自动执行，时间线记录 | 网页搜索、文件读取 |
| 2 | 高风险 | **暂停 + 内联授权卡片** | 文件写入、发送消息、API 调用 |
| 3 | 极高风险 | **暂停 + 内联授权卡片（无"全部授权"选项）** | 文件删除、支付、执行系统命令 |

### 4.2 内联授权卡片设计

当 Level 2-3 工具被调用时，在对话流中追加 `Role="tool_auth"` 消息卡片：

```text
┌──────────────────────────────────────────────────────────────┐
│  ... 时间线进度 ...                                            │
│                                                              │
│  ┌─── 工具授权请求 ───────────────────────────────────────┐  │
│  │                                                        │  │
│  │  ⚠️ 步骤2 请求调用 [发送邮件]                           │  │
│  │     收件人: team@company.com                           │  │
│  │     主题: "AI 芯片市场分析报告 v1"                       │  │
│  │     内容摘要: 包含 4 章分析结论 + 3 张图表...            │  │
│  │                                                        │  │
│  │   [✓ 确认执行]  [✓✓ 全部授权]  [✕ 拒绝]               │  │
│  │                                                        │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ 输入想法...（输入框始终可用，不灰显）                   │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

**与原浮动面板设计的区别：**

- 授权请求作为对话流中的普通卡片消息，不是浮动层
- 输入框不灰显，用户可以在授权等待期间继续输入其他内容
- 按钮交互后卡片变为已处理状态（灰色），保留在对话流中作为历史记录

### 4.3 卡片选项定义

**Level 2（高风险但可恢复）：**

| 选项 | 行为 |
| ------ | ------ |
| ✓ 确认执行 | 执行该工具调用，继续后续步骤 |
| ✓✓ 本任务中该工具全部授权 | 执行，且后续本任务内**同一工具**的调用不再询问 |
| ✕ 拒绝本次调用 | 不执行，将"用户拒绝使用 [工具名]"反馈给子智能体，**AI 自主决定替代方案** |

**Level 3（极高风险/不可逆）：**

| 选项 | 行为 |
| ------ | ------ |
| ✓ 确认执行 | 执行（建议 UI 增加二次确认提示） |
| ✕ 拒绝本次调用 | 不执行，AI 自主替代方案 |

> **Level 3 不提供"全部授权"选项**，每次调用必须单独确认。

### 4.4 待实现：自定义输入能力

> **状态：待实现**

当前实现仅支持三按钮交互（确认/全部授权/拒绝），缺少自定义输入能力。
预期功能：用户可以在授权卡片中通过自然语言修改授权参数（如"改一下收件人为 boss@..."），
LLM 识别意图后执行相应操作。

实现方案（待开发）：
- 在授权卡片底部增加文本输入行
- 用户输入后路由到 Orchestrator 做意图识别
- 识别结果可能是：修改参数后执行、拒绝并给出替代指令等

### 4.5 卡片交互规则

| 规则 | 说明 |
| ------ | ------ |
| 出现时机 | Level 2-3 工具被调用时追加到对话流 |
| 消失时机 | 不消失，保留在流中作为历史（按钮变灰） |
| 不阻塞输入 | 输入框始终可用 |
| 位置 | 对话流中，作为普通消息卡片 |

### 4.6 拒绝后的 AI 行为

```text
子智能体收到反馈："用户拒绝调用 [工具名]，请用其他方式完成当前步骤"

AI 可能的替代决策（由 LLM 自主判断）：
  · 换用低风险替代工具（如：拒绝发邮件 → 保存到本地文件）
  · 将结果输出到报告正文中让用户自行处理
  · 跳过当前子步骤，在摘要中说明原因
  · 向主智能体汇报无法完成，由主智能体重新规划
```

### 4.7 "全部授权"的粒度

- **粒度**：本任务内**该工具**全部授权
- **作用域**：从授权时刻起，到本任务结束（包括重新执行的循环）
- **存储**：内存级，任务结束即清除（不持久化）
- **示例**：用户对 `file_write` 选择"全部授权"后，本任务内所有后续的文件写入操作自动执行，但 `send_email` 仍然会询问

**相关代码：**
- `WorkflowDetailView.axaml`：`Role="tool_auth"` 渲染模板（Border + 按钮组）
- `P4TaskDetailVm.cs`：工具授权事件订阅 + 信号发送

---

## 5. 子智能体创建：自动执行 + 对话告知

### 5.1 废弃内容

以下 P2-4 实现的组件 **废弃**：

| 组件 | 文件 | 废弃原因 |
| ------ | ------ | --------- |
| 动态子智能体创建审批卡片 | `DynamicAgentCreationApprovalVm.cs` | 用户不需要审批内部调度细节 |
| 审批闸 | `DynamicAgentCreationGate.cs` | 不再需要阻塞等待用户决策 |
| 三按钮交互（批准/全部批准/拒绝）| `WorkflowDetailView.axaml` 审批区域 | 改为自动执行 |
| 审批事件 | `Events.cs` 中 `OnDynamicAgentCreationRequested/Resolved` | 不再需要 |

### 5.2 新的展示方式

子智能体的创建过程在时间线中以**普通文本行**展示：

```
正在创建数据采集专家，负责从公开财报中提取 AI 芯片数据...
正在创建市场分析师，负责横向对比竞品数据...

[步骤1] ████████░░ 80%  数据采集专家正在工作...
```

- 无审批，无阻塞
- 用户可以看到创建了什么、负责什么
- 但不需要（也不应该）干预创建过程

---

## 6. 执行结果展示

### 6.1 结果展示方式

执行完成后，结果通过以下两种形式在对话流中内联展示：

1. **任务总结卡片**（`Role="step_card"` + `CardTitle="任务总结"`）：
   由 `BuildFinalReport()` 生成完整 Markdown 报告，包含验证分数/摘要/问题/建议。
   卡片初始为**展开状态**（`IsExpanded=true`），用户可手动折叠。

2. **产出文件列表**（`Role="result"` 消息内的 `ResultFiles`）：
   展示文件图标、文件名、文件大小，hover 显示完整路径。

```text
┌──────────────────────────────────────────────────────────────┐
│  ... 时间线进度（步骤完成节点）...                               │
│                                                              │
│  ●─ ✓ 验证通过（90/100）                                     │
│     └ [问题列表]                                             │
│                                                              │
│  ┌─── 任务总结 ───────────────────────────────────────────┐  │
│  │  ## 执行完成                                            │  │
│  │  **验证结果**：✓ 通过（90/100）                          │  │
│  │  [Summary 正文]                                         │  │
│  │  ### 发现的问题                                          │  │
│  │  - [issue 1]                                            │  │
│  │  ### 改进建议                                            │  │
│  │  - [suggestion 1]                                       │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌─ 产出文件 ──────────────────────────────────────────────┐ │
│  │  📄 reports/AI芯片市场分析报告.md       12 KB            │ │
│  │  📊 data/processed/竞品数据对比.xlsx    48 KB            │ │
│  │  📈 charts/市场趋势图.png              156 KB            │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  ●─ ✓ 任务已完成                                             │
│                                                              │
│  有什么需要调整的吗？（AI 对话消息）                            │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ 输入想法...                                          │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

### 6.2 结果展示规则

| 元素 | 规则 |
| ------ | ------ |
| 任务总结卡片 | 初始展开，用户可折叠；内含 `MarkdownRenderer` 渲染完整报告 |
| 文件列表 | 图标 + 相对路径 + 文件大小（hover 显示完整路径） |
| 路径显示 | 默认显示相对路径（相对于工作目录） |
| 不自动弹窗 | 执行完成不自动打开资源管理器 |

### 6.3 待实现：文件操作按钮

> **状态：待实现**

当前文件列表仅展示信息，缺少交互操作按钮。预期功能：

| 操作 | 行为 |
| ------ | ------ |
| [打开] | 调用系统默认程序打开（.md→编辑器、.xlsx→Excel、.png→看图） |
| [在资源管理器中显示] | 打开文件所在目录并选中文件 |

实现方案（待开发）：
- 为每个 `ResultFileVm` 增加 `OpenFileCommand` 和 `ShowInExplorerCommand`
- 通过 `Process.Start` 调用系统默认程序或 Explorer
- XAML 中在文件行右侧添加按钮

### 6.4 结果类型处理

| 类型 | 展示方式 |
| ------ | --------- |
| 纯文本结论 | 任务总结卡片展示，无文件列表 |
| 生成文件 | 任务总结卡片 + 文件列表 |
| 混合（文本 + 文件） | 任务总结卡片 + 文件列表（最常见） |

**相关代码：**
- `TaskExecutionEngine.BuildFinalReport()`：Markdown 报告生成
- `P4TaskDetailVm.cs`：`OnTaskEngineCompleted` 中创建 `step_card` 类型的任务总结卡片
- `ConversationMessageVm.cs`：`ResultFiles` / `ResultFileVm` 文件列表模型

---

## 7. 文件保存位置策略

### 7.1 优先级

```
优先级 1：用户在对话中明确指定路径
          "报告保存到 D:/Reports/"
          → 所有产出文件保存到指定路径

优先级 2：LLM 根据工作目录结构语义推断
          扫描工作目录的目录树 → 按已有文件夹的语义选择最合适的位置
```

### 7.2 LLM 推断示例

```
工作目录结构：
  E:/Projects/AI-Chip-Research/
    ├── data/
    │   ├── raw/
    │   └── processed/
    ├── reports/
    │   ├── 2025-Q4-分析.md
    │   └── 2026-Q1-分析.md
    ├── charts/
    └── src/

LLM 扫描目录树后推断：
  · AI芯片市场分析报告.md  → reports/          （已有同类报告文件）
  · 竞品数据对比.xlsx      → data/processed/   （处理后的数据）
  · 市场趋势图.png         → charts/           （图表类文件归这里）
```

### 7.3 规则

| 规则 | 说明 |
| ------ | ------ |
| 默认保存位置 | 当前工作目录内，LLM 按目录语义选择子文件夹 |
| 用户指定优先 | 对话中指定的路径优先级最高 |
| 目录不存在 | AI 自动创建（合理的情况下）|
| 文件重名 | AI 自动加后缀避免覆盖（如 `报告-v2.md`）|
| 路径展示 | 结果展示中显示相对路径 |

---

## 8. 任务生命周期重定义：永续对话循环

### 8.1 旧模型（废弃）

```
用户输入 → 分析 → 计划 → 用户点批准 → 执行 → 完成（终态）
```

### 8.2 新模型

```
┌─────────────────────────────────────────────────────────────┐
│                       永续对话循环                             │
│                                                             │
│   ┌───────────────────────────────────────────────────┐     │
│   │          对话模式（纯数据流界面）                     │     │
│   │                                                   │     │
│   │   · 首次提出需求                                   │     │
│   │   · AI 分析 + 澄清提问                            │     │
│   │   · 讨论/修改执行计划                              │     │
│   │   · 执行完成后追问报告内容                          │     │
│   │   · 对结果不满意提出修改                            │     │
│   │                                                   │     │
│   │   退出条件：LLM 识别到"确认执行"意图               │     │
│   └─────────────────────┬─────────────────────────────┘     │
│                         │                                   │
│                         ▼                                   │
│   ┌───────────────────────────────────────────────────┐     │
│   │          执行模式（时间线进度面板）                    │     │
│   │                                                   │     │
│   │   · 步骤进度展示                                   │     │
│   │   · 子智能体创建/运行（仅展示，无需确认）           │     │
│   │   · 危险工具 → 浮动意图面板（§4）                  │     │
│   │   · 并行调度 + 重试（用户可暂停）                   │     │
│   │                                                   │     │
│   │   退出条件：所有步骤完成 / 用户暂停                 │     │
│   └─────────────────────┬─────────────────────────────┘     │
│                         │                                   │
│                         ▼                                   │
│           自动切回对话模式（展示结果 + 等待反馈）              │
│                         │                                   │
│           ┌─────────────┼──────────────┐                    │
│           │             │              │                    │
│       追问细节      提出修改      "满意了"                    │
│       (留在对话)   (回到对话→    (任务真正结束)               │
│                    重新计划→                                 │
│                    再次执行)                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 8.3 任务终态条件

任务标记为 `Completed` 的 **唯一条件**：

> 用户在对话中表达了"满意/完成/结束"的语义（`intent = satisfied`）。

在此之前，无论执行了多少轮，任务始终处于活跃状态。

---

## 9. ~~长任务执行：异步工具协议与轮询调度~~ ⚠️ 已废弃

> **状态：已废弃，不实现。**
>
> 本节设计的"异步工具协议"（`IAsyncToolProtocol` / `AsyncToolCheckpoint` / 轮询调度器）在软件架构层面不适配当前运行环境：
>
> - 当前架构中，**引擎从不直接调用工具**。工具由 LLM 在推理过程中自主调用（MCP/内置工具），引擎仅管理 LLM 调用的生命周期。"引擎 → 工具.StartAsync() → 轮询 QueryAsync()" 这条路径根本不存在。
> - 工具的异步特征（如返回 job_id 后轮询）是工具自身与 LLM 之间的协商，不需要也不应该由引擎层介入。
> - 强行在引擎层定义工具协议，反而无法适配语义上具备异步特征但未按固定接口实现的现有工具。
>
> **决策**：本节所有接口、类型、调度逻辑均不实现。原有决策表 D-12 / D-13 同步废弃。

---

### 9.1 问题背景（已废弃，仅保留历史记录）

部分任务步骤耗时极长（分钟到小时级），例如：

| 步骤 | 耗时 | 特征 |
| ------ | ------ | ------ |
| 视频剪辑 | 30 分钟 | 本地工具后台执行 |
| 文件上传（多文件并发） | 1-2 小时 | 网络 I/O，进度可查询 |
| 平台处理/发布 | 10-30 分钟 | 远程服务异步处理 |
| 发布验证 | 5-10 分钟 | 需等待平台就绪后检查 |

**核心约束**：LLM 不能长时间保持连接（超时会断开），但任务必须持续推进。

**解决方案**：LLM 只在需要决策时被唤醒（秒级），工具在后台执行（分钟-小时级），引擎用纯程序代码轮询工具状态来衔接两者。

### 9.2 工具的两种执行模式

| 模式 | 调用方式 | 适用场景 | 耗时 |
| ------ | ------ | ------ | ------ |
| **同步**（现有） | 调用 → 等待 → 返回结果 | web_search、file_read、文本生成 | 秒级 |
| **异步**（新增） | 调用 → 返回 handle → 后台执行 → 轮询/回调获取结果 | 视频剪辑、文件上传、远程任务 | 分钟 - 小时级 |

### 9.3 异步工具协议

异步工具需实现三个方法：

```csharp
/// <summary>
/// 异步工具协议。长耗时工具实现此接口，引擎通过轮询驱动执行。
/// </summary>
public interface IAsyncToolProtocol
{
    /// <summary>
    /// 启动后台任务。
    /// </summary>
    /// <param name="parameters">工具参数。</param>
    /// <param name="ct">取消令牌。</param>
    /// <returns>任务句柄 + 预估耗时。</returns>
    Task<AsyncToolStartResult> StartAsync(
        IDictionary<string, object> parameters, CancellationToken ct);

    /// <summary>
    /// 查询后台任务状态。引擎定时调用此方法轮询进度。
    /// 此方法必须是轻量级的（毫秒级返回），不能阻塞。
    /// </summary>
    /// <param name="taskHandle">StartAsync 返回的句柄。</param>
    /// <param name="ct">取消令牌。</param>
    /// <returns>当前状态 + 进度 + 结果（完成时）。</returns>
    Task<AsyncToolQueryResult> QueryAsync(string taskHandle, CancellationToken ct);

    /// <summary>
    /// 取消后台任务（用户暂停/取消时调用）。
    /// </summary>
    Task CancelAsync(string taskHandle, CancellationToken ct);
}

public sealed class AsyncToolStartResult
{
    /// <summary>后台任务唯一标识（引擎保存到 checkpoint 用于恢复）。</summary>
    public required string TaskHandle { get; init; }

    /// <summary>预估耗时（引擎据此计算轮询间隔）。</summary>
    public TimeSpan EstimatedDuration { get; init; }

    /// <summary>任务描述（UI 展示用，如"正在上传 video_1.mp4"）。</summary>
    public string DisplayText { get; init; } = string.Empty;
}

public sealed class AsyncToolQueryResult
{
    /// <summary>
    /// 任务状态。
    /// </summary>
    public AsyncToolStatus Status { get; init; }

    /// <summary>进度百分比 0-100（UI 进度条用）。</summary>
    public int Progress { get; init; }

    /// <summary>进度描述文本（UI 展示用，如"1.2GB / 2.8GB"）。</summary>
    public string ProgressText { get; init; } = string.Empty;

    /// <summary>预估剩余时间（可选，UI 展示"预计剩余 45 分钟"）。</summary>
    public TimeSpan? EstimatedRemaining { get; init; }

    /// <summary>任务结果（仅 Status=Completed 时有值）。</summary>
    public string? Result { get; init; }

    /// <summary>错误信息（仅 Status=Failed 时有值）。</summary>
    public string? ErrorMessage { get; init; }
}

public enum AsyncToolStatus
{
    /// <summary>已提交，尚未开始。</summary>
    Pending,

    /// <summary>执行中。</summary>
    Running,

    /// <summary>已完成。</summary>
    Completed,

    /// <summary>失败。</summary>
    Failed,

    /// <summary>已取消。</summary>
    Cancelled,
}
```

### 9.4 引擎轮询调度策略

异步工具启动后，引擎进入"等待-轮询"模式。轮询间隔根据 `EstimatedDuration` 智能调整：

| 预估耗时 | 轮询间隔 | 理由 |
| --------- | --------- | ------ |
| < 1 分钟 | 每 5 秒 | 快速任务，尽早感知完成 |
| 1 - 10 分钟 | 每 30 秒 | 平衡响应性和资源开销 |
| 10 - 60 分钟 | 每 60 秒 | 长任务不需要高频查询 |
| > 1 小时 | 每 5 分钟 | 超长任务（大文件上传）|

```csharp
/// <summary>
/// 根据预估耗时计算轮询间隔。
/// </summary>
public static TimeSpan ComputePollInterval(TimeSpan estimatedDuration)
{
    return estimatedDuration.TotalMinutes switch
    {
        < 1 => TimeSpan.FromSeconds(5),
        < 10 => TimeSpan.FromSeconds(30),
        < 60 => TimeSpan.FromSeconds(60),
        _ => TimeSpan.FromMinutes(5),
    };
}
```

轮询过程中的关键约束：

| 约束 | 说明 |
| ------ | ------ |
| 不持有 LLM 连接 | 轮询是纯程序代码，不调用 LLM |
| UI 实时更新 | 每次查询结果推送到时间线（进度百分比 + 描述文本）|
| 用户可暂停/取消 | 引擎调用 `CancelAsync` 通知工具停止 |
| 进度持久化 | 每次查询后更新 checkpoint（崩溃后可恢复）|
| 不阻塞其他步骤 | 异步工具等待期间，无依赖的其他步骤正常调度执行 |

### 9.5 LLM 参与点：只在决策时唤醒

一个完整的长任务（2-3 小时），LLM 实际只被唤醒几次：

```
时间轴（视频制作发布任务示例）：

0:00   LLM唤醒(3s) → 决策："调用 video_edit 工具" → 释放连接
0:00 - 0:30  引擎轮询 video_edit 进度（纯程序，无 LLM）
0:30   工具完成 → 引擎调度 → 发现步骤2/3/4 就绪 → 同时启动

0:30   LLM唤醒(3s) → 决策步骤2："调用 upload_A" → 释放
0:30   LLM唤醒(3s) → 决策步骤3："调用 upload_B" → 释放
0:30   LLM唤醒(3s) → 决策步骤4："生成封面图" → 释放

0:32   步骤4 完成 ✓（同步工具，秒级）
0:30 - 1:45  引擎轮询 upload_A / upload_B（纯程序，无 LLM）

1:15   步骤3 完成 ✓（upload_B 先完成）
1:45   步骤2 完成 ✓（upload_A 完成）

1:45   LLM唤醒(3s) → 决策步骤5："验证发布状态" → 释放
1:55   步骤5 完成 ✓
1:55   LLM唤醒(3s) → 决策步骤6："汇总结果" → 释放
1:58   步骤6 完成 ✓ → 任务结束 → 切回对话模式

─────────────────────────────────────
LLM 唤醒次数：7 次
LLM 总连接时间：约 21 秒
任务总耗时：约 2 小时
```

### 9.6 并行调度与异步工具的配合

异步工具等待期间，步骤状态为 `Running`。调度器只看 `dependsOn` 中的步骤是否 `Completed`，**不关心步骤是同步还是异步完成的**。

#### 场景：多文件并发上传 + 无依赖步骤提前执行

```
执行计划：
  步骤1：视频剪辑                    dependsOn: []
  步骤2：上传到平台A（3个文件并发）   dependsOn: [步骤1]
  步骤3：上传到平台B（2个文件并发）   dependsOn: [步骤1]
  步骤4：生成封面图                  dependsOn: [步骤1]     ← 不依赖上传
  步骤5：发布验证                    dependsOn: [步骤2, 步骤3]
  步骤6：结果总结                    dependsOn: [步骤4, 步骤5]
```

调度行为：

```
步骤1 完成 → 调度器检查就绪步骤：
  步骤2 dependsOn[步骤1] ✓ → 就绪，启动
  步骤3 dependsOn[步骤1] ✓ → 就绪，启动
  步骤4 dependsOn[步骤1] ✓ → 就绪，启动

步骤2/3 在后台上传（异步工具），步骤4 同时执行（同步工具）→ 步骤4 先完成

步骤4 完成 → 调度器检查就绪步骤：
  步骤5 dependsOn[步骤2, 步骤3] → 步骤2 Running, 步骤3 Running → 不就绪
  步骤6 dependsOn[步骤4, 步骤5] → 步骤5 还没开始 → 不就绪

步骤2 完成 → 调度器检查：步骤5 dependsOn 中步骤3 仍 Running → 不就绪
步骤3 完成 → 调度器检查：步骤5 dependsOn 全部 Completed → 就绪，启动

步骤5 完成 → 步骤6 dependsOn[步骤4 ✓, 步骤5 ✓] → 就绪，启动
```

#### 步骤内部多文件并发

步骤2"上传到平台A"本身包含多个并发子任务：

```
步骤2：上传到平台A
  executionMode: "parallel"
  subTasks:
    - subTask-a: 上传 video_1.mp4 (800MB)  → 异步工具 → 后台上传
    - subTask-b: 上传 video_2.mp4 (1.2GB)  → 异步工具 → 后台上传
    - subTask-c: 上传 video_3.mp4 (600MB)  → 异步工具 → 后台上传

  引擎同时启动 3 个异步工具实例
  各自独立轮询进度
  全部 subTask 完成后 → 步骤2 标记 Completed
```

UI 时间线展示：

```
[步骤1] ██████████ 完成 ✓  视频剪辑完成
[步骤2] ████████░░ 72%     上传到平台A
          ├── video_1.mp4  ████████░░ 89%   预计剩余 2 分钟
          ├── video_2.mp4  ██████░░░░ 61%   预计剩余 12 分钟
          └── video_3.mp4  ██████████ 完成 ✓
[步骤3] ██████░░░░ 55%     上传到平台B
[步骤4] ██████████ 完成 ✓  封面图已生成
[步骤5] ⏳ 等待中            依赖步骤 2, 3
[步骤6] ⏳ 等待中            依赖步骤 4, 5
```

### 9.7 断点恢复：异步工具状态持久化

进程崩溃/关闭时，异步工具的运行状态必须持久化到 checkpoint，重启后可恢复轮询。

```csharp
/// <summary>
/// ExecutionCheckpoint 扩展：记录异步工具状态。
/// </summary>
public sealed class AsyncToolCheckpoint
{
    /// <summary>步骤 ID。</summary>
    public string StepId { get; set; } = string.Empty;

    /// <summary>子任务 ID（如果是 parallel 步骤内的子任务）。</summary>
    public string? SubTaskId { get; set; }

    /// <summary>工具名称。</summary>
    public string ToolName { get; set; } = string.Empty;

    /// <summary>后台任务句柄（恢复轮询的关键）。</summary>
    public string TaskHandle { get; set; } = string.Empty;

    /// <summary>上次查询到的进度。</summary>
    public int LastProgress { get; set; }

    /// <summary>上次查询时间（用于计算下次轮询时间）。</summary>
    public DateTimeOffset LastQueryAt { get; set; }

    /// <summary>轮询间隔。</summary>
    public TimeSpan PollInterval { get; set; }
}
```

恢复逻辑：

```
进程重启 → 读取 checkpoint → 发现有 AsyncToolCheckpoint 记录
  ↓
对每个异步工具记录：
  1. 调用 tool.QueryAsync(taskHandle) 获取当前状态
  2. 状态为 Running → 恢复轮询循环
  3. 状态为 Completed → 读取结果，标记步骤完成，触发调度器
  4. 状态为 Failed → 进入重试策略
  5. 状态为 Cancelled/不可达 → 标记步骤为 Pending（重新执行）
```

### 9.8 与 GlobalLlmThrottle 的关系

异步工具的轮询过程**不占用 LLM 信号量**（因为不调用 LLM）。只有在以下时刻才需要获取 LLM 信号量：

- 步骤开始时，唤醒 LLM 做执行决策
- 异步工具完成后，如果需要 LLM 做结果分析/下一步决策

```
GlobalLlmThrottle 管辖范围：
  ✅ 主智能体决策调用
  ✅ 子智能体执行调用
  ✅ 需求分析/计划制定/验证
  ❌ 异步工具轮询（纯程序代码，不占 LLM 槽位）
  ❌ 同步工具执行（如 web_search 本身不调 LLM）
```

---

## 10. 决策汇总

| # | 决策项 | 结论 | 备注 |
| --- | -------- | ------ | --------- |
| D-1 | 界面架构 | 统一对话流（不做双模态切换） | 所有内容在同一 FeedItems 中按时间顺序呈现 |
| D-2 | 确认执行 | LLM 意图识别，自然语言驱动 | 废弃 P2-4 审批卡片三按钮 |
| D-3 | 子智能体创建 | 自动进行，文本行告知，无需审批 | 废弃 `DynamicAgentCreationGate` + 审批卡片 |
| D-4 | 执行过程展示 | 竖线时间线 + 折叠式步骤卡片 | 圆点统一 `#cccccc`，子过程在卡片内累积 |
| D-5 | 危险工具授权 | 内联授权消息卡片（`Role="tool_auth"`） | 待实现：自定义输入能力 |
| D-6 | 全部授权粒度 | 本任务内该工具 | — |
| D-7 | 拒绝后行为 | AI 自主决定替代方案 | — |
| D-8 | 执行完成后 | 继续在同一对话流中追加消息 | 废弃"切回对话模式"概念 |
| D-9 | 结果展示 | 折叠式任务总结卡片（初始展开）+ 文件列表 | 待实现：[打开]/[资源管理器] 按钮 |
| D-10 | 文件保存位置 | 用户指定优先 > LLM 按工作目录语义推断 | — |
| D-11 | 任务真正结束 | 用户在对话中表达满意 | — |
| D-12 | 输入框行为 | 始终可用，执行期间不灰显 | — |
| ~~D-13~~ | ~~长任务执行~~ | ~~异步工具协议 + 引擎轮询调度~~ | **已废弃**：架构不匹配 |
| ~~D-14~~ | ~~异步工具与并行调度~~ | ~~无依赖步骤可直接启动~~ | **已废弃**：随 D-13 一同废弃 |

---

## 11. 对现有代码的影响评估

### 11.1 需要废弃的代码

| 文件 | 原因 |
| ------ | ------ |
| `ViewModels/Workspace/DynamicAgentCreationApprovalVm.cs` | 审批卡片 VM，改为自动执行 |
| `Workflow/DynamicAgents/DynamicAgentCreationGate.cs` | 审批闸机制，不再需要阻塞等待 |
| `Events.cs` 中 `OnDynamicAgentCreationRequested/Resolved` | 审批事件，不再需要 |
| `WorkflowDetailView.axaml` 中子智能体审批卡片区域 | 改为文本行展示 |
| `WorkflowDetailView.axaml` 中 HITL 审批卡片的批准/修改/拒绝按钮 | 改为对话驱动 |

### 11.2 需要新增的代码

| 模块 | 内容 | 状态 |
| ------ | ------ | ------ |
| UI | 工作流统一对话流组件（纯数据流，无头像） | ✅ 已实现 |
| UI | 内联授权消息卡片（`Role="tool_auth"`） | ✅ 已实现（待补充自定义输入） |
| UI | 折叠式步骤卡片（`Role="step_card"`） | ✅ 已实现 |
| UI | 竖线时间线节点（`Role="progress"` + `ProgressKind`） | ✅ 已实现 |
| UI | 任务总结卡片 + 文件列表 | ✅ 已实现（待补充 [打开]/[资源管理器] 按钮） |
| 后端 | 意图识别集成（主智能体输出 intent 字段） | ✅ 已实现 |
| 后端 | 工具风险分级注册表（Level 0-3） | ✅ 已实现 |
| 后端 | 工具授权状态管理（内存级，任务生命周期） | ✅ 已实现 |
| 后端 | 文件保存位置推断（扫描目录树 + LLM 选择） | ✅ 已实现 |
| ~~后端~~ | ~~`IAsyncToolProtocol` 接口 + 异步工具轮询调度器~~ | **已废弃（§9）** |
| ~~后端~~ | ~~`AsyncToolCheckpoint` 持久化 + 恢复逻辑~~ | **已废弃（§9）** |
| ~~后端~~ | ~~轮询间隔智能计算（`ComputePollInterval`）~~ | **已废弃（§9）** |

### 11.3 需要修改的代码

| 文件 | 改动 | 状态 |
| ------ | ------ | ------ |
| `TaskExecutionEngine.cs` | 执行循环中集成工具授权检查 + 暂停/恢复 | ✅ 已完成 |
| `OrchestratorAgent.cs` | 输出格式增加 `intent` 字段 | ✅ 已完成 |
| `CreateSubAgentTool.cs` | 移除审批闸依赖，改为直接创建 | ✅ 已完成 |
| `AIAgentFactory.cs` | 移除 Gate/审批相关注入 | ✅ 已完成 |
| `WorkflowInputVm.cs` | 集成对话历史管理 + 意图识别调用 | ✅ 已完成 |
| `WorkflowDetailView.axaml` | 统一对话流布局（时间线 + 步骤卡片 + 授权卡片） | ✅ 已完成 |

---

## 12. 与现有文档的关系

| 文档 | 关系 |
| ------ | ------ |
| `01-P2方案设计.md` | P2-4 审批卡片部分被本文档 §5 废弃 |
| `04-P4方案设计-任务执行引擎.md` | 引擎核心不变，交互层被本文档重新定义 |
| `05-P4补充-上下文传递与恢复机制.md` | §3.3 对话上下文管理补充了新要求 |
| `06-P4补充-提示词与UI时间线设计.md` | 时间线视图保留，提示词需补充意图识别指令 |
| `07-P4补充-资源管理与工具授权.md` | 工具授权机制被本文档 §4 重新定义 |
| `99-P2-2-Bug-修复进度交接.md` | 审批闸相关实现标记为废弃 |

---

## 附录 A：历史差异记录（已合并到正文）

> **说明**：本附录原记录编码实现过程中对初版设计的偏离。
> 2026-05-27 文档更新后，所有偏离已合并到正文对应章节（以实际代码为准），
> 本附录仅保留索引供追溯。

| 编号 | 原始差异 | 已合并到 |
| --- | -------- | -------- |
| A.1 | 步骤子过程展示：`└` 详情行 → 折叠式步骤卡片 | §2.3 执行阶段界面规格 |
| A.2 | 待执行步骤预渲染（`○─ ⏳`）：设计为预渲染，实际不预渲染 | §2.3（当前行为即正文描述） |
| A.3 | 危险工具授权：浮动意图面板 → 内联授权消息卡片 | §4 全文重写 |
| A.4 | 时间线圆点颜色：语义色 → 统一 `#cccccc` | §2.3 时间线节点规格表 |
| A.5 | 任务终态与永续对话循环：完全按设计实现 | §8（无变更） |
| A.6 | 执行结果展示：底部固定区域 → 对话流内折叠卡片 | §6 全文重写 |
| A.7 | 双模态切换 → 统一对话流 | §2 全文重写 |
