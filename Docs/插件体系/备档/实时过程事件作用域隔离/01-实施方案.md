# 实施方案

## 一、新增 `NonExpertProcessScope`

`Src/Netor.Cortana.Entitys/Realtime/NonExpertProcessScope.cs`：

```csharp
namespace Netor.Cortana.Entitys;

public static class NonExpertProcessScope
{
    private static readonly AsyncLocal<bool> _active = new();

    public static bool Active => _active.Value;

    public static IDisposable Enter()
    {
        var previous = _active.Value;
        _active.Value = true;
        return new Pop(previous);
    }

    private sealed class Pop(bool previous) : IDisposable
    {
        private bool _disposed;
        public void Dispose()
        {
            if (_disposed) return;
            _disposed = true;
            _active.Value = previous;
        }
    }
}
```

## 二、新增 `NonExpertSuppressingRealtimeOutput`

`Src/Netor.Cortana.UI/Channels/NonExpertSuppressingRealtimeOutput.cs`：

```csharp
namespace Netor.Cortana.UI.Channels;

internal sealed class NonExpertSuppressingRealtimeOutput(
    UiChatOutputChannel expertChannel) : IRealtimeProcessOutput
{
    public Task OnProcessEventAsync(RealtimeProcessEvent evt, CancellationToken ct = default)
    {
        if (NonExpertProcessScope.Active)
        {
            return Task.CompletedTask;
        }

        return expertChannel.OnProcessEventAsync(evt, ct);
    }
}
```

## 三、调整 DI

`Src/Netor.Cortana.UI/App.axaml.cs:321-323`：

```csharp
.AddSingleton<UiChatOutputChannel>()
.AddSingleton<IAiOutputChannel>(sp => sp.GetRequiredService<UiChatOutputChannel>())
.AddSingleton<IRealtimeProcessOutput, NonExpertSuppressingRealtimeOutput>()
```

## 四、入口插桩

### 4.1 `WorkflowExecutor.ExecuteCoreAsync`

`Src/Netor.Cortana.AI/WorkMode/WorkflowExecutor.cs`，函数第一行：

```csharp
private async Task ExecuteCoreAsync(...)
{
    using var _ = NonExpertProcessScope.Enter();
    // 原有代码不变
}
```

### 4.2 `MeetingExecutor.ExecuteAsync`

`Src/Netor.Cortana.AI/MeetingMode/MeetingExecutor.cs`，参数校验之后第一行：

```csharp
public async Task ExecuteAsync(string meetingId, ...)
{
    ArgumentException.ThrowIfNullOrWhiteSpace(meetingId);
    using var _ = NonExpertProcessScope.Enter();
    // 原有代码不变
}
```

## 五、实施顺序

1. 新增 `NonExpertProcessScope`。
2. 新增 `NonExpertSuppressingRealtimeOutput`。
3. 改 `App.axaml.cs` DI 注册。
4. `WorkflowExecutor.ExecuteCoreAsync` 入口加 `Enter()`。
5. `MeetingExecutor.ExecuteAsync` 入口加 `Enter()`。
6. 跑 [02-验收清单.md](02-验收清单.md)。
