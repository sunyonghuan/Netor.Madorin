# 3D 渲染实现计划 : 95%

> 当前状态：D3D11 3D mesh 最小绘制通道已接入，GLB mesh 提取/材质/纹理/动画/VRM Profile 均打通。`--gltf-model <name>` 可加载带动画、带 VRM humanoid 数据的模型。GPU vertex/index buffer 已缓存复用，动画帧仅更新 WorldTransform。AOT 发布通过，0 警告，41 个测试全绿。

## R1 3D Render Pipeline : 100%

目标：先打通 D3D11 原生 3D 绘制链路，再接 GLB/VRM 模型数据。

### Step 1 Mesh 提交通道 : 100%

- [x] 新增 `D3D11MeshVertex`
- [x] 新增 `D3D11MeshItem`
- [x] `IRenderHost.SubmitMeshItems`
- [x] D3D11 mesh vertex shader / pixel shader
- [x] input layout
- [x] constant buffer
- [x] camera / projection MVP
- [x] depth stencil
- [x] `--preview-3d` 运行时预览

### Step 1 完成记录

- 当前 3D 预览使用彩色顶点，不依赖贴图。
- Live2D 和 3D mesh 可共用同一 render host；当前绘制顺序为先 3D、后 Live2D。
- buffer 仍为每帧创建，便于验证；后续应改为可复用 GPU buffer / dirty update。

### Step 2 GLB Mesh Extractor : 100%

- [x] 从 `GltfModel` 读取 bufferView / accessor
- [x] 提取 POSITION
- [x] 提取 NORMAL（有则映射为顶点颜色，无则白色）
- [x] 提取 TEXCOORD_0
- [x] 提取 indices（uint16 和 uint32 均支持）
- [x] 映射到 `D3D11MeshItem`（`GltfMeshItemMapper`）
- [x] `GltfMeshSubmissionLoop` 每帧旋转提交
- [x] `--gltf-model` 运行时接入
- [x] 单元测试（Triangle.gltf / .glb 提取验证，共 6 个）

### Step 2 完成记录

- `GltfMeshExtractor`：内部 static，直接操作 accessor/bufferView/binaryChunk span，零额外分配。
- stride 支持 interleaved buffer；accessor byteOffset 在 GetAccessorSpan 中正确叠加。
- `GltfMeshItemMapper`：Step 2 法线着色（normal→RGB）；Step 3 贴图接入后替换为 UV+texture。
- `GltfMeshSubmissionLoop`：启动后提取一次 primitives，每帧只更新 RotationYRadians，不重新解析。

### Step 3 Texture / Material : 100%

- [x] `GltfManifest` 补充 `GltfPbrMetallicRoughness` / `GltfTextureRef`
- [x] `GltfModel.GetBaseColorFactor` / `GetBaseColorImageIndex` / `ExtractImageBytes`
- [x] `GltfImageExtractor`：bufferView 内嵌、外部 URI、data URI (base64) 均支持
- [x] `D3D11MeshVertex` 加 UV (U, V)，input layout 同步更新
- [x] `D3D11MeshTexture` 携带 CacheKey / RGBA 像素 / BaseColorFactor
- [x] `D3D11MeshItem` 加 `Texture?` 和 `BaseColorFactor`
- [x] `D3D11MeshConstants` 加 `BaseColorFactor` + `HasTexture`（16 字节对齐）
- [x] mesh pixel shader 支持纹理采样（t1/s1，不影响 Live2D t0/s0）
- [x] `DrawMeshItem` 按需绑定纹理 SRV，`_meshTextureViews` 缓存
- [x] `GltfMeshItemMapper` 使用 UV + 纹理；PNG 解码由 `PngRgbaDecoder` 完成

### Step 3 完成记录

- JPEG 暂不支持（纯 .NET 实现复杂；VRM 多用 PNG，后续按需补充）。
- 无纹理时顶点颜色回退：有法线→法线映射 RGB；无法线→白色。
- `BaseColorFactor` 默认 `Vector4.One`（白色，无乘法损耗）。
- 纹理缓存 key 格式：`{sourcePath}:img{imageIndex}:{meshId}`，避免重复上传 GPU。

### Step 4 Animation Sampler : 100%

- [x] `GltfManifest` 扩展：`GltfAnimationChannel` / `GltfAnimationTarget` / `GltfAnimationSampler`；`GltfNode` 加 Translation/Rotation/Scale/Matrix
- [x] `GltfAnimationEvaluator`：预加载所有 channel keyframe；LINEAR/STEP 插值；`Evaluate(t)` 返回 per-node TRS override；时间自动取模循环
- [x] `GltfNodeTransformTree`：从 scene root 深度优先走节点层级；叠加动画 override；输出 nodeIndex → world Matrix4x4
- [x] `GltfMeshPrimitive` 加 `NodeIndex`；`GltfMeshExtractor` 从场景图走节点赋 NodeIndex
- [x] `GltfModel.CreateAnimationEvaluator` / `ComputeNodeWorldMatrices`
- [x] `D3D11MeshItem` 换 `WorldTransform`（Matrix4x4），替代 RotationYRadians/Scale
- [x] `GltfMeshSubmissionLoop` 有动画时每帧评估 → 重建 world matrices → 提交；无动画时只算一次静态 world
- [x] `GltfMeshItemMapper` 使用 nodeIndex 对应的 world matrix
- [x] `Preview3DSubmissionLoop` 自行计算 world = Scale * RotationY

### Step 4 完成记录

- 动画时间取模 `Duration`（channel 最后一帧时间），实现 idle 循环。
- `GltfMeshSubmissionLoop` 对无动画模型只在启动时计算一次 world matrices，每帧直接复用，无 CPU 开销。
- `GltfMeshExtractor` 支持同一 mesh 被多节点引用（实例化），每个 node-mesh 对生成独立 primitive。

### Step 5 VRM Profile : 100%

- [x] `VrmHumanoidBone` 骨骼名常量（全 55 根标准骨骼）
- [x] `VrmProfileParser`：VRM 0.x（`VRM` 扩展，humanBones 数组）和 VRM 1.0（`VRMC_vrm`，humanBones 对象）均支持
- [x] `VrmProfile.HumanoidBoneNodeIndices`：bone 名 → glTF nodeIndex 字典
- [x] `VrmProfile.ExpressionPresets`：VRM 1.0 preset / VRM 0.x blendShapeGroups 解析（预留，运行时暂不驱动）
- [x] `VrmProfile.LookAt`：first-person/look-at 字段提取（预留）
- [x] `VrmProfile.SpringBone`：joint 数 / collider 数统计（预留，`VRMC_springBone` 也支持）
- [x] `GltfModel.TryGetVrmProfile()` 公开 API
- [x] 单元测试：空 VRMC_vrm、VRM 1.0 humanoid、VRM 0.x humanoid、expressions、无 VRM（共 5 个）

### Step 5 完成记录

- 全部通过 `JsonElement` 导航，无反射，AOT 安全。
- 表情/LookAt/SpringBone 为"预留槽"：数据已解析并可访问，运行时驱动留给后续迭代。
- 两种 VRM 版本均通过测试；`VRMC_vrm` 优先于 `VRM`。

## R1 完成

R1 3D Render Pipeline 所有 5 个 Step 均已完成（100%）。

## R2 性能优化 : 完成

### GPU buffer 复用 : 100%

- [x] `D3D11RenderHost._meshBufferCache`：按 `item.Id` 缓存 vertex/index buffer；VCount/ICount 不变则直接复用，不重新上传 GPU
- [x] `GltfMeshSubmissionLoop`：startup 时调用 `GltfMeshItemMapper.ToBaseItems` 提取顶点/UV/纹理数据一次；动画帧仅调用 `ComputeNodeWorldMatrices` + `with { WorldTransform = world }` 更新变换
- [x] 静态模型每帧零 CPU/GPU buffer 分配；动画模型每帧仅更新 constant buffer

### JPEG 纹理 : 100%

- [x] `JpegRgbaDecoder`：纯 .NET 实现，无外部依赖，AOT 安全
  - 支持 Baseline DCT (SOF0)，YCbCr 4:4:4 / 4:2:0，灰度，8-bit，重启标记
  - 可分离浮点 IDCT（行→列），JFIF YCbCr→RGB 公式
  - byte stuffing (0xFF 0x00 → 0xFF) 正确处理
- [x] `GltfMeshItemMapper.DecodeImage`：自动检测 PNG / JPEG magic bytes，路由到对应解码器
- [x] 单元测试：非 JPEG 输入抛异常、1×1 灰度 128 解码验证（共 2 个）

## R3 高级特性

### R3-1 表情驱动 : 100%

- [x] `GltfPrimitive` 加 `IReadOnlyList<Dictionary<string, int>>? Targets`（AOT source gen 同步更新）
- [x] `GltfMeshPrimitive` 加 `IReadOnlyList<float[]>? MorphPositionDeltas { get; init; }`
- [x] `GltfMeshExtractor`：提取 `targets[i]["POSITION"]` 增量 float[]
- [x] `VrmMorphTargetBind`：`(MeshName, MorphTargetIndex, NormalizedWeight)`
- [x] `VrmExpressionPreset` 加 `IReadOnlyList<VrmMorphTargetBind> MorphTargetBinds`
- [x] `VrmProfileParser`：VRM 1.0 通过 `node→mesh→Name` 解析 `morphTargetBinds`；VRM 0.x `binds[j].weight / 100`
- [x] 新建 `VrmExpressionEvaluator`：`SetExpression`/`ClearExpressions`/`BlendPositions`；`Version` 属性用于 GPU 缓存 key 失效
- [x] `GltfModel.CreateExpressionEvaluator()`
- [x] `GltfMeshItemMapper.ToBaseItems` 接受可选 `VrmExpressionEvaluator?`；有 morph 时 item.Id 加 `#m{version}`
- [x] `GltfMeshSubmissionLoop`：`SetExpression`/`ClearExpressions` 公开 API，`_morphDirty` volatile flag，后台线程检测后调用 `RebuildBaseItems`（带锁）
- [x] 单元测试：VRM 1.0 `morphTargetBinds` 解析（mesh name、index、weight）；VRM 0.x `binds` 解析（weight/100 归一化）；13 个测试全绿

### R3-1 完成记录

- 多表情叠加：`_active` dict 中多个表情的 bind 累加（additive blending）。
- GPU 缓存失效：morphed item.Id = `"Name[0]#m{version}"`；每次表情变化 version++，旧版本缓存项不再被访问（极少次切换，stale 积累可接受）。
- 线程安全：`_morphLock` 保护 `_expressionEvaluator._active` 的读写；`_morphDirty` 为 volatile。
- 静态模型（无 animator）表情变化后同样重建 `_staticItems`。

### R3-2 LookAt : 100%

- [x] 新建 `VrmLookAtController`：`SetAngles(yaw, pitch)`；`SetScreenPosition(nx, ny)`（归一化坐标 0-1）；`ApplyOverrides(dict)` 注入眼球骨骼旋转
- [x] 眼球旋转限幅：yaw ±30°，pitch ±20°；`Quaternion.CreateFromYawPitchRoll` 计算旋转四元数，与已有动画 override 叠加
- [x] `GltfModel.CreateLookAtController()`：无眼骨时返回 null
- [x] `GltfMeshSubmissionLoop`：`SetLookAtScreenPosition(nx, ny)` / `ClearLookAt()` 公开 API；`_lookAtActive` volatile flag；统一动态帧路径 `BuildDynamicItems`（animator + lookAt 叠加）

### R3-2 完成记录

- 无眼骨的模型（无 VRM 或 leftEye/rightEye 骨骼缺失）`CreateLookAtController()` 返回 null，`SetLookAtScreenPosition` 为 no-op。
- 静态模型（无 animator）激活 lookAt 后切换到逐帧 `BuildDynamicItems` 路径，CPU 开销仅多一次 `ComputeNodeWorldMatrices`。
- yaw/pitch 字段为 plain float，跨线程读写最多导致一帧视觉误差，对动画可接受。

### R3-3 SpringBone 物理 : 100%

- [x] 新建 `VrmSpringBoneData`：解析 VRM 1.0 (`VRMC_springBone`) 和 VRM 0.x (`secondaryAnimation`) 的 chains/colliders；支持 sphere collider（不含 capsule）；VRM 0.x 通过 `WalkChain`（顺着 firstChild 走）建链
- [x] 新建 `VrmSpringBoneSimulator`：Verlet 积分（惯性 + drag + stiffness spring + gravity + sphere 碰撞推开）；per-frame 更新 `workingWorldMatrices` 并注入 `GltfNodeOverride.Rotation`；`BuildParentMap` 构建节点父子关系图，使 local rotation 转换正确
- [x] `GltfModel.CreateSpringBoneSimulator()`
- [x] `GltfMeshSubmissionLoop`：`EnableSpringBone()` / `DisableSpringBone()`；在 `BuildDynamicItems(dt)` 中调用 `_springBoneSimulator.Update(dt, worldMatrices, overrides)`；dt 由真实帧时间计算，限幅 10-120fps

### R3-3 完成记录

- 骨骼 rotation override 采用 `QuaternionBetween(restDir, newDir)` → 世界空间 delta → 转换到父骨骼 local space。
- 初始化在第一次 `Update` 时完成（需要 rest world matrices）；后续帧使用上一帧的 simulated positions 作为速度来源。
- 对于纯蒙皮模型（骨骼不直接挂 mesh），spring bone 更新了骨骼 world matrices；当蒙皮支持加入后可直接生效。

### R3-4 Dirty-flag GPU upload : 100%

- [x] `D3D11MeshConstants` 实现 `IEquatable<D3D11MeshConstants>`（字段精确比较）
- [x] `D3D11RenderHost._meshConstantsInBuffer`：跟踪常量缓冲当前内容；`DrawMeshItem` 中若新 constants == 缓冲内容则跳过 `UpdateSubresource`

### R3-4 完成记录

- 优化效果：同一帧连续绘制具有相同 constants（相同 WorldTransform + BaseColorFactor + HasTexture）的多个 primitive 时，仅第一个触发上传，后续跳过。
- 对于单 item 静态模型：第一帧上传一次后，后续帧（buffer 内容不变）完全跳过 UpdateSubresource。
- 对于 N 个各有不同变换的 primitive（典型 VRM）：每帧仍需 N 次上传（buffer 在 item 间循环更新），无法跳过，但无额外开销。

## R3 完成

全部 4 个 R3 任务均已完成（100%）。
